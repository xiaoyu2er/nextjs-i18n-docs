---
title: 安装
description: 使用 `create-next-app` 创建一个新的 Next.js 应用程序。设置 TypeScript，样式，并配置您的 `next.config.js` 文件.
related:
  title: Next Steps
  description: Learn about the files and folders in your Next.js project.
  links:
    - getting-started/project-structure
---

系统要求：

- [Node.js 18.17](https://nodejs.org/) 或更高版本。
- 支持 macOS、Windows（包括 WSL）和 Linux。

## 自动安装

我们推荐使用 [`create-next-app`](/docs/app/api-reference/cli/create-next-app) 启动新的 Next.js 应用，它会自动为您设置所有内容。要创建项目，请运行：

```bash filename="Terminal"
npx create-next-app@latest
```

安装时，您将看到以下提示：

```txt filename="Terminal"
What is your project named? my-app
Would you like to use TypeScript? No / Yes
Would you like to use ESLint? No / Yes
Would you like to use Tailwind CSS? No / Yes
Would you like to use `src/` directory? No / Yes
Would you like to use App Router? (recommended) No / Yes
Would you like to customize the default import alias (@/*)? No / Yes
What import alias would you like configured? @/*
```

提示完成后，[`create-next-app`](/docs/app/api-reference/cli/create-next-app) 将创建一个以您的项目名称命名的文件夹并安装所需的依赖项。

如果您是 Next.js 新手，请参阅 [项目结构](/docs/getting-started/project-structure) 文档，了解应用中所有可能的文件和文件夹概览。

> **提示**:
>
> - Next.js 现已内置 [TypeScript](/docs/app/building-your-application/configuring/typescript)、[ESLint](/docs/app/building-your-application/configuring/eslint) 和 [Tailwind CSS](/docs/app/building-your-application/styling/tailwind-css) 配置。
> - 您可选择在项目根目录使用 [`src` 目录](/docs/app/building-your-application/configuring/src-directory) 将应用代码与配置文件分离。

## 手动安装

要手动创建新的 Next.js 应用，请安装所需的包：

```bash filename="Terminal"
npm install next@latest react@latest react-dom@latest
```

打开你的 `package.json` 文件，并添加以下 `scripts`

```json filename="package.json"
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

这些脚本对应于开发应用程序的不同阶段：

- `dev`: 运行 [`next dev`](/docs/app/api-reference/cli/next#next-dev-options) 以 Next.js 开发模式启动。
- `build`: 运行 [`next build`](/docs/app/api-reference/cli/next#next-build-options) 构建用于生产环境的应用。
- `start`: 运行 [`next start`](/docs/app/api-reference/cli/next#next-start-options) 启动 Next.js 生产服务器。
- `lint`: 运行 [`next lint`](/docs/app/api-reference/cli/next#next-lint-options) 设置 Next.js 内置的 ESLint 配置。

### 创建目录

Next.js 使用文件系统路由，这意味着您应用程序中的路由由文件结构决定。

#### `app` 目录

对于新项目，我们推荐使用 [应用路由 (App Router)](/docs/app)。该路由允许您使用 React 的最新功能，并是基于社区反馈对 [页面路由 (Pages Router)](/docs/pages) 的演进。

创建一个 `app/` 文件夹，然后添加 `layout.tsx` 和 `page.tsx` 文件。当用户访问您应用的根目录 (`/`) 时，这些文件将被渲染。

<Image
  alt="应用文件夹结构"
  srcLight="/docs/light/app-getting-started.png"
  srcDark="/docs/dark/app-getting-started.png"
  width="1600"
  height="363"
/>

在 `app/layout.tsx` 中创建一个 [根布局 (root layout)](/docs/app/building-your-application/routing/pages-and-layouts#root-layout-required)，包含必需的 `<html>` 和 `<body>` 标签：

```tsx filename="app/layout.tsx" switcher
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

最后，创建一个首页 `app/page.tsx`，并添加一些初始内容：

```tsx filename="app/page.tsx" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

```jsx filename="app/page.js" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

> **提示**: 如果您忘记创建 `layout.tsx`，Next.js 将在使用 `next dev` 运行开发服务器时自动创建此文件。

了解有关 [使用应用路由 (App Router)](/docs/app/building-your-application/routing/defining-routes) 的更多信息。

#### `pages` 目录（可选）

如果您更喜欢使用页面路由 (Pages Router) 而不是应用路由 (App Router)，可以在项目根目录创建一个 `pages/` 目录。

然后，在您的 `pages` 文件夹内添加一个 `index.tsx` 文件。这将是您的首页 (`/`)：

```tsx filename="pages/index.tsx" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

接下来，在 `pages/` 内添加一个 `_app.tsx` 文件来定义全局布局。了解更多关于[自定义 App 文件](/docs/pages/building-your-application/routing/custom-app)的信息。

```tsx filename="pages/_app.tsx" switcher
import type { AppProps } from 'next/app'

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />
}
```

```jsx filename="pages/_app.js" switcher
export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
```

最后，在 `pages/` 内添加一个 `_document.tsx` 文件来控制服务器的初始响应。了解更多关于[自定义 Document 文件](/docs/pages/building-your-application/routing/custom-document)的信息。

```tsx filename="pages/_document.tsx" switcher
import { Html, Head, Main, NextScript } from 'next/document'

export default function Document() {
  return (
    <Html>
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}
```

了解有关 [使用页面路由 (Pages Router)](/docs/pages/building-your-application/routing/pages-and-layouts) 的更多信息。

> **提示**: 虽然您可以在同一项目中使用两个路由，但 `app` 中的路由优先级高于 `pages`。我们建议在新的项目中只使用一个路由，以避免混淆。

#### `public` 文件夹（可选）

创建一个 `public` 文件夹以存储静态资源，例如图片、字体等。位于 `public` 目录内的文件随后可由您的代码从基础 URL (`/`) 引用。

## 运行开发服务器

1. 运行 `npm run dev` 启动开发服务器。
2. 访问 `http://localhost:3000` 查看您的应用。
3. 编辑 `app/page.tsx`（或 `pages/index.tsx`）文件并保存，在浏览器中查看更新结果。
