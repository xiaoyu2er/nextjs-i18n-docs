---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:29:43.647Z
title: generateBuildId
description: تكوين معرف البناء الذي يُستخدم لتحديد البناء الحالي الذي يتم تقديم تطبيقك من خلاله.
---

{/* المحتوى في هذا المستند مشترك بين موجه التطبيق وموجه الصفحات. يمكنك استخدام مكون `<PagesOnly>المحتوى</PagesOnly>` لإضافة محتوى خاص بموجه الصفحات. أي محتوى مشترك لا يجب أن يكون مغلفًا في مكون. */}

يقوم Next.js بإنشاء معرف أثناء عملية `next build` لتحديد أي إصدار من تطبيقك يتم تقديمه. يجب استخدام نفس البناء وتشغيله عبر حاويات متعددة.

إذا كنت تقوم بإعادة البناء لكل مرحلة من بيئتك، فستحتاج إلى إنشاء معرف بناء متسق لاستخدامه بين الحاويات. استخدم أمر `generateBuildId` في ملف `next.config.js`:

```jsx filename="next.config.js"
module.exports = {
  generateBuildId: async () => {
    // يمكن أن يكون هذا أي شيء، مثل استخدام آخر هاش من git
    return process.env.GIT_HASH
  },
}
```