---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T16:51:51.975Z
title: trailingSlash
description: Настройка обработки завершающего слеша в URL-адресах страниц Next.js.
---

{/* Содержание этого документа используется как для роутера приложения (app), так и для роутера страниц (pages). Вы можете использовать компонент `<PagesOnly>Контент</PagesOnly>` для добавления контента, специфичного для роутера страниц. Общий контент не должен быть обёрнут в компонент. */}

По умолчанию Next.js перенаправляет URL-адреса с завершающим слешем на соответствующие адреса без слеша. Например, `/about/` будет перенаправлен на `/about`. Вы можете изменить это поведение, чтобы URL-адреса без завершающего слеша перенаправлялись на соответствующие адреса со слешем.

Откройте файл `next.config.js` и добавьте конфигурацию `trailingSlash`:

```js filename="next.config.js"
module.exports = {
  trailingSlash: true,
}
```

При включённой этой опции URL-адреса вида `/about` будут перенаправляться на `/about/`.

При использовании с конфигурацией [`output: "export"`](/docs/app/building-your-application/deploying/static-exports) страница `/about` будет экспортироваться как `/about/index.html` (вместо стандартного `/about.html`).

## История версий

| Версия   | Изменения                |
| -------- | ------------------------ |
| `v9.5.0` | Добавлен `trailingSlash`. |