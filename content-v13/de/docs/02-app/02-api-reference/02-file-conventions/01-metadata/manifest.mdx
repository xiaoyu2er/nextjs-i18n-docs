---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T18:12:15.320Z
title: manifest.json
description: API-Referenz für die manifest.json-Datei.
---

Fügen Sie eine `manifest.(json|webmanifest)`-Datei, die der [Web Manifest-Spezifikation](https://developer.mozilla.org/docs/Web/Manifest) entspricht, im **Stammverzeichnis** des `app`-Ordners hinzu oder generieren Sie sie, um Informationen über Ihre Webanwendung für den Browser bereitzustellen.

## Statische Manifest-Datei

```json filename="app/manifest.json | app/manifest.webmanifest"
{
  "name": "My Next.js Application",
  "short_name": "Next.js App",
  "description": "An application built with Next.js",
  "start_url": "/"
  // ...
}
```

## Manifest-Datei generieren

Fügen Sie eine `manifest.js`- oder `manifest.ts`-Datei hinzu, die ein [`Manifest`-Objekt](#manifest-object) zurückgibt.

```ts filename="app/manifest.ts" switcher
import { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'Next.js App',
    short_name: 'Next.js App',
    description: 'Next.js App',
    start_url: '/',
    display: 'standalone',
    background_color: '#fff',
    theme_color: '#fff',
    icons: [
      {
        src: '/favicon.ico',
        sizes: 'any',
        type: 'image/x-icon',
      },
    ],
  }
}
```

```js filename="app/manifest.js" switcher
export default function manifest() {
  return {
    name: 'Next.js App',
    short_name: 'Next.js App',
    description: 'Next.js App',
    start_url: '/',
    display: 'standalone',
    background_color: '#fff',
    theme_color: '#fff',
    icons: [
      {
        src: '/favicon.ico',
        sizes: 'any',
        type: 'image/x-icon',
      },
    ],
  }
}
```

### Manifest-Objekt

Das Manifest-Objekt enthält eine umfangreiche Liste von Optionen, die aufgrund neuer Webstandards aktualisiert werden können. Informationen zu allen aktuellen Optionen finden Sie im Typ `MetadataRoute.Manifest` in Ihrem Code-Editor, wenn Sie [TypeScript](https://nextjs.org/docs/app/building-your-application/configuring/typescript#typescript-plugin) verwenden, oder in den [MDN](https://developer.mozilla.org/docs/Web/Manifest)-Dokumenten.