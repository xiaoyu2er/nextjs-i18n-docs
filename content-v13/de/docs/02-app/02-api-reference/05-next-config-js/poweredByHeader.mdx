---
title: poweredByHeader
description: Next.js fügt standardmäßig den Header `x-powered-by` hinzu. Hier erfahren Sie, wie Sie dies deaktivieren können.
---

{/* Der Inhalt dieses Dokuments wird sowohl für den App- als auch für den Pages-Router verwendet. Sie können die Komponente `<PagesOnly>Inhalt</PagesOnly>` verwenden, um Inhalte hinzuzufügen, die spezifisch für den Pages Router sind. Gemeinsam genutzte Inhalte sollten nicht in eine Komponente eingeschlossen werden. */}

Standardmäßig fügt Next.js den Header `x-powered-by` hinzu. Um dies zu deaktivieren, öffnen Sie `next.config.js` und setzen die Konfiguration `poweredByHeader` auf false:

```js filename="next.config.js"
module.exports = {
  poweredByHeader: false,
}
```