---
title: poweredByHeader
description: Next.js по умолчанию добавляет заголовок `x-powered-by`. Узнайте, как отключить его.
---

{/* Содержание этого документа используется как в App Router, так и в Pages Router. Для добавления контента, специфичного для Pages Router, используйте компонент `<PagesOnly>Content</PagesOnly>`. Общий контент не должен быть обёрнут в компонент. */}

По умолчанию Next.js добавляет заголовок `x-powered-by`. Чтобы отключить его, откройте файл `next.config.js` и отключите настройку `poweredByHeader`:

```js filename="next.config.js"
module.exports = {
  poweredByHeader: false,
}
```