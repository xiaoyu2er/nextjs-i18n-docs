---
title: httpAgentOptions
description: Next.js по умолчанию автоматически использует HTTP Keep-Alive. Узнайте, как отключить HTTP Keep-Alive.
---

{/* Содержание этого документа является общим для App Router и Pages Router. Вы можете использовать компонент `<PagesOnly>Content</PagesOnly>` для добавления контента, специфичного для Pages Router. Любой общий контент не должен быть обёрнут в компонент. */}

В версиях Node.js до 18 Next.js автоматически добавляет полифил для `fetch()` с помощью [undici](/docs/architecture/supported-browsers#polyfills) и по умолчанию включает [HTTP Keep-Alive](https://developer.mozilla.org/docs/Web/HTTP/Headers/Keep-Alive).

Чтобы отключить HTTP Keep-Alive для всех вызовов `fetch()` на стороне сервера, откройте `next.config.js` и добавьте конфигурацию `httpAgentOptions`:

```js filename="next.config.js"
module.exports = {
  httpAgentOptions: {
    keepAlive: false,
  },
}
```