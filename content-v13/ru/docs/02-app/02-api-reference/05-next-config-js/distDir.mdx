---
title: distDir
description: Настройка пользовательской директории сборки вместо стандартной папки .next.
---

{/* Содержание этого документа используется как для маршрутизации приложений (app), так и для страниц (pages). Вы можете использовать компонент `<PagesOnly>Содержимое</PagesOnly>` для добавления контента, специфичного для маршрутизатора Pages. Любой общий контент не должен быть обёрнут в компонент. */}

Вы можете указать имя пользовательской директории сборки вместо `.next`.

Откройте файл `next.config.js` и добавьте конфигурацию `distDir`:

```js filename="next.config.js"
module.exports = {
  distDir: 'build',
}
```

Теперь при запуске `next build` Next.js будет использовать папку `build` вместо стандартной `.next`.

> `distDir` **не должен** указывать за пределы директории вашего проекта. Например, `../build` является **недопустимым** путём.