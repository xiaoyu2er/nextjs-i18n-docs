---
title: transpilePackages
description: Transpila y empaqueta automáticamente dependencias de paquetes locales (como monorepos) o dependencias externas (`node_modules`).
---

{/* El contenido de este documento se comparte entre el enrutador de la aplicación y el de páginas. Puedes usar el componente `<PagesOnly>Contenido</PagesOnly>` para añadir contenido específico del Enrutador de Páginas. Cualquier contenido compartido no debe estar envuelto en un componente. */}

Next.js puede transpilar y empaquetar automáticamente dependencias de paquetes locales (como monorepos) o dependencias externas (`node_modules`). Esto reemplaza al paquete `next-transpile-modules`.

```js filename="next.config.js"
/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@acme/ui', 'lodash-es'],
}

module.exports = nextConfig
```

## Historial de versiones

| Versión   | Cambios                    |
| --------- | -------------------------- |
| `v13.0.0` | Se añadió `transpilePackages`. |