---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:13:55.867Z
title: distDir
description: Définir un répertoire de build personnalisé à utiliser au lieu du répertoire .next par défaut.
---

{/* Le contenu de ce document est partagé entre le routeur app et pages. Vous pouvez utiliser le composant `<PagesOnly>Content</PagesOnly>` pour ajouter du contenu spécifique au routeur Pages. Tout contenu partagé ne doit pas être encapsulé dans un composant. */}

Vous pouvez spécifier un nom à utiliser pour un répertoire de build personnalisé au lieu de `.next`.

Ouvrez `next.config.js` et ajoutez la configuration `distDir` :

```js filename="next.config.js"
module.exports = {
  distDir: 'build',
}
```

Maintenant, si vous exécutez `next build`, Next.js utilisera `build` au lieu du dossier `.next` par défaut.

> `distDir` **ne doit pas** quitter le répertoire de votre projet. Par exemple, `../build` est un répertoire **invalide**.