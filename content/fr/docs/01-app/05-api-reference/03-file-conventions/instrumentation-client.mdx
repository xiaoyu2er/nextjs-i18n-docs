---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:05:47.630Z
title: instrumentation-client.js
description: Découvrez comment ajouter une instrumentation côté client pour suivre et surveiller les performances frontend de votre application Next.js.
---

Le fichier `instrumentation-client.js|ts` vous permet d'ajouter du code de surveillance et d'analytique qui s'exécute avant que le code frontend de votre application ne démarre. Cela est utile pour configurer le suivi des performances, la surveillance des erreurs ou tout autre outil d'observabilité côté client.

Pour l'utiliser, placez le fichier à la **racine** de votre application ou dans un dossier `src`.

## Utilisation

Contrairement à [l'instrumentation côté serveur](/docs/app/guides/instrumentation), vous n'avez pas besoin d'exporter de fonctions spécifiques. Vous pouvez écrire votre code de surveillance directement dans le fichier :

```ts filename="instrumentation-client.ts" switcher
// Configurer le suivi des performances
performance.mark('app-init')

// Initialiser l'analytique
console.log('Analytics initialized')

// Configurer le suivi des erreurs
window.addEventListener('error', (event) => {
  // Envoyer à votre service de suivi des erreurs
  reportError(event.error)
})
```

```js filename="instrumentation-client.js" switcher
// Configurer le suivi des performances
performance.mark('app-init')

// Initialiser l'analytique
console.log('Analytics initialized')

// Configurer le suivi des erreurs
window.addEventListener('error', (event) => {
  // Envoyer à votre service de suivi des erreurs
  reportError(event.error)
})
```

## Historique des versions

| Version | Changements                          |
| ------- | ------------------------------------ |
| `v15.3` | Introduction de `instrumentation-client` |