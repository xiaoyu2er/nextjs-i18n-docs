---
title: 如何優化您的本地開發環境
nav_title: 開發環境
description: 學習如何使用 Next.js 優化您的本地開發環境。
---

Next.js 旨在提供優異的開發者體驗。隨著您的應用程序增長，您可能會注意到本地開發期間編譯時間較慢。本指南將幫助您識別並修復常見的編譯期性能問題。

## 本地開發與生產環境

使用 `next dev` 的開發過程與 `next build` 和 `next start` 不同。
---

`next dev` 在您打開或導航到應用程序中的路由時編譯該路由。這使您可以啟動開發服務器而無需等待應用程序中的所有路由都完成編譯，這既更快又使用更少的內存。運行生產構建會應用其他優化，如縮小文件大小和創建內容雜湊，這些在本地開發中不需要。
---

## 提升本地開發效能

### 1. 檢查電腦的防毒軟體

防毒軟體可能會減緩檔案存取。雖然這在 Windows 電腦上更為常見，但安裝了防毒工具的任何系統都可能遇到這個問題。

在 Windows 上，您可以將專案加入 [Microsoft Defender 防毒軟體排除清單](https://support.microsoft.com/en-us/windows/virus-and-threat-protection-in-the-windows-security-app-1362f4cd-d71a-b52a-0b66-c2820032b65e#bkmk_threat-protection-settings)。

1. 開啟 **"Windows Security"** 應用程式，然後選擇 **"病毒與威脅防護"** &rarr; **"管理設定"** &rarr; **"新增或移除排除項目"**。
2. 新增 **"資料夾"** 排除項目。選擇您的專案資料夾。

在 macOS 上，您可以在終端機內停用 [Gatekeeper](https://support.apple.com/guide/security/gatekeeper-and-runtime-protection-sec5599b66df/web)。

1. 在終端機中執行 `sudo spctl developer-mode enable-terminal`。
2. 開啟 **"System Settings"** 應用程式，然後選擇 **"隱私權與安全性"** &rarr; **"開發人員工具"**。
3. 確認您的終端機已列舉並啟用。如果您使用的是第三方終端機（例如 iTerm 或 Ghostty），請將其加入清單。
4. 重新啟動您的終端機。

<Image
  alt="截圖顯示 macOS 系統設定中隱私權與安全性面板"
  srcLight="/docs/light/macos-gatekeeper-privacy-and-security.png"
  srcDark="/docs/dark/macos-gatekeeper-privacy-and-security.png"
  width="723"
  height="250"
/>

<Image
  alt="截圖顯示 macOS 系統設定中開發人員工具選項"
  srcLight="/docs/light/macos-gatekeeper-developer-tools.png"
  srcDark="/docs/dark/macos-gatekeeper-developer-tools.png"
  width="723"
  height="250"
/>

如果您或您的雇主在系統上配置了其他防毒解決方案，您應該檢查這些產品的相關設定。

### 2. 更新 Next.js 並使用 Turbopack

請確保您使用的是最新版本。每個新版本通常都包含效能改進。
---

Turbopack 目前已是 Next.js 開發的預設打包工具，相較於 webpack 提供了顯著的效能提升。

```bash package="pnpm"
pnpm add next@latest
pnpm dev  # Turbopack is used by default
```

```bash package="npm"
npm install next@latest
npm run dev  # Turbopack is used by default
```

```bash package="yarn"
yarn add next@latest
yarn dev  # Turbopack is used by default
```

```bash package="bun"
bun add next@latest
bun dev  # Turbopack is used by default
```

如果您需要改用 Webpack 而非 Turbopack，您可以選擇啟用：

```bash package="pnpm"
pnpm dev --webpack
```

```bash package="npm"
npm run dev -- --webpack
```

```bash package="yarn"
yarn dev --webpack
```

```bash package="bun"
bun run dev --webpack
```

`[深入了解 Turbopack](/blog/turbopack-for-development-stable)`。參閱我們的 `[升級指南](/docs/app/guides/upgrading)` 和 codemods 以獲取更多資訊。
---

### 3. 檢查您的引入方式

您引入程式碼的方式會大大影響編譯和打包時間。更多內容請參閱 `[優化套件打包](/docs/app/guides/package-bundling)` 並探索 `[Dependency Cruiser](https://github.com/sverweij/dependency-cruiser)` 或 `[Madge](https://github.com/pahen/madge)` 等工具。
---

#### 圖示庫

像 `@material-ui/icons`, `@phosphor-icons/react`, 或 `react-icons` 這樣的庫會引入數千個圖示，即使您只使用少量圖示。請嘗試只引入您需要的圖示：
---

```jsx
// Instead of this:
import { TriangleIcon } from '@phosphor-icons/react'

// Do this:
import { TriangleIcon } from '@phosphor-icons/react/dist/csr/Triangle'
```

您通常可以在所使用的圖示庫的文檔中找到應使用什麼引入模式。此範例遵循 [`@phosphor-icons/react`](https://www.npmjs.com/package/@phosphor-icons/react#import-performance-optimization) 建議。
---

像 `react-icons` 這樣的庫包含許多不同的圖示集。選擇一個集合併堅持使用該集。
---

例如，如果您的應用程序使用 `react-icons` 並導入以下所有：
---

- `pi` (Phosphor 圖示)
- `md` (Material Design 圖示)
- `tb` (Tabler Icons)
- `cg` (Css gg)

總計將是數萬個模組，即使您只從每個模組導入一個。
---

#### Barrel 檔案 (Barrel files)

“Barrel 檔案 (Barrel files)” 是用於從其他檔案匯出許多項目的檔案。它們可能會減緩構建速度，因為編譯器必須解析它們來檢查模組範圍內的副作用（`import` 是否會導致副作用）。
---

請盡可能嘗試直接從特定檔案引入。[`深入了解 Barrel 檔案 (Barrel files)`](https://vercel.com/blog/how-we-optimized-package-imports-in-next-js) 和 Next.js 內建的優化。
---

#### 優化套件引入

Next.js 可以自動優化某些套件的引入。如果您正在使用利用 Barrel 檔案的套件，請將它們加入您的 `next.config.js`：
---

```jsx
module.exports = {
  experimental: {
    optimizePackageImports: ['package-name'],
  },
}
```

Turbopack 會自動分析引入並進行優化。它不需要此設定。
---

### 4. 檢查您的 Tailwind CSS 設定

如果您使用 Tailwind CSS，請確保設定正確。
---

一個常見的錯誤是您配置 `content` 陣列的方式包含了 `node_modules` 或其他不應該掃描的大型目錄。
---

Tailwind CSS 3.4.8 或更新版本將警告您關於可能減慢構建的設定。
---

1. 在您的 `tailwind.config.js` 中，明確指定要掃描的檔案：

   ```jsx
   module.exports = {
     content: [
       './src/**/*.{js,ts,jsx,tsx}', // Good
       // This might be too broad
       // It will match `packages/**/node_modules` too
       // '../../packages/**/*.{js,ts,jsx,tsx}',
     ],
   }
   ```

2. 避免掃描不必要檔案：

   ```jsx
   module.exports = {
     content: [
       // Better - only scans the 'src' folder
       '../../packages/ui/src/**/*.{js,ts,jsx,tsx}',
     ],
   }
   ```

### 5. 檢查自訂 webpack 設定

如果您添加了自訂 webpack 設定，它們可能減慢編譯速度。
---

考慮您是否在本地開發時確實需要它們。您可以選擇僅在生產環境建置中包含某些工具，或者探索使用預設的 Turbopack 打包工具，並改用配置 [載入器 (loaders)](/docs/app/api-reference/config/next-config-js/turbopack#configuring-webpack-loaders)。

### 6. 優化記憶體使用

如果您的應用程序非常大，可能需要更多記憶體。
---

[深入了解優化記憶體使用](/docs/app/guides/memory-usage)。
---

### 7. 伺服器組件 (Server Components) 和數據獲取

Server Components 的變更會導致整個頁面在本地重新渲染，以顯示新變更，這包括獲取元件的新數據。
---

實驗性 `serverComponentsHmrCache` 選項允許您在本地開發期間的熱模組替換 (HMR) 重新整理之間緩存 Server Components 中的 `fetch` 回應。這導致更快的回應和減少已計費 API 呼叫的成本。
---

`[深入了解此實驗性選項](/docs/app/api-reference/config/next-config-js/serverComponentsHmrCache)`。
---

### 8. 考慮使用本地開發而非 Docker

如果您在 Mac 或 Windows 上使用 Docker 進行開發，可能會比在本地運行 Next.js 經歷顯著較慢的效能。
---

Docker 在 Mac 和 Windows 上的檔案系統存取可能會導致熱模組替換 (HMR) 需要數秒甚至數分鐘，而同一應用程序在本地開發時運行快速 HMR。
---

此效能差異歸因於 Docker 如何處理 Linux 環境以外的檔案系統操作。為了獲得最佳開發體驗：
---

- 開發期間使用本地開發 (`npm run dev` 或 `pnpm dev`) 而非 Docker
- 保留 Docker 用於生產部署和測試生產構建
- 如果您必須使用 Docker 進行開發，考慮在 Linux 機器或 VM 上使用 Docker

[深入了解 Docker 部署](/docs/app/getting-started/deploying#docker) 以供生產使用。
---

## 查找問題的工具

### 詳細數據獲取日誌記錄

在您的 `next.config.js` 檔案中使用 `logging.fetches` 選項，以獲得有關開發期間發生什麼事情的更多詳細資訊：
---

```js
module.exports = {
  logging: {
    fetches: {
      fullUrl: true,
    },
  },
}
```

[深入了解數據獲取日誌記錄](/docs/app/api-reference/config/next-config-js/logging)。
---

### Turbopack 追蹤

Turbopack 追蹤是一種工具，可幫助您瞭解本地開發期間應用程序的效能。
它提供有關每個模組編譯耗時及其關係的詳細資訊。
---

1. 確保您已安裝最新版本的 Next.js。
1. 產生 Turbopack 追蹤檔案：

   ```bash package="pnpm"
   NEXT_TURBOPACK_TRACING=1 pnpm dev
   ```

   ```bash package="npm"
   NEXT_TURBOPACK_TRACING=1 npm run dev
   ```

   ```bash package="yarn"
   NEXT_TURBOPACK_TRACING=1 yarn dev
   ```

   ```bash package="bun"
   NEXT_TURBOPACK_TRACING=1 bun dev
   ```

1. 在您的應用程式中瀏覽或編輯檔案以重現問題。
1. 停止 Next.js 開發伺服器。
1. 一個名為 `trace-turbopack` 的檔案位於 `.next/dev` 資料夾中可供使用。
1. 您可以使用 `npx next internal trace [路徑到檔案]` 來解釋檔案：

   ```bash
   npx next internal trace .next/dev/trace-turbopack
   ```

   在 `trace` 不可用的版本中，該命令命名為 `turbo-trace-server`：

   ```bash
   npx next internal turbo-trace-server .next/dev/trace-turbopack
   ```

1. 一旦追蹤伺服器啟動，您可以在 https://trace.nextjs.org/ 查看追蹤。
1. 預設情況下，追蹤檢視器會聚合計時，為了查看每個單獨的時間，您可以在檢視器右上角從「聚合順序 (Aggregated in order)」切換為「順序跨度 (Spans in order)」。

> **值得注意**：追蹤檔案位於開發伺服器目錄下，預設為 `.next/dev`。

### 還有問題嗎？

分享在 [Turbopack 追蹤](#turbopack-tracing) 部分生成的追蹤檔案，並在 [GitHub Discussions](https://github.com/vercel/next.js/discussions) 或 [Discord](https://nextjs.org/discord) 上分享。
