---
title: robots.txt
description: robots.txt 檔案的 API 參考
---

新增或生成一個 `robots.txt` 檔案，使其符合 [Robots Exclusion Standard (機器人排除標準)](https://en.wikipedia.org/wiki/Robots.txt#Standard)，並置於 `app` 目錄的 **根目錄** 中，以告知搜尋引擎爬蟲可以存取您網站上的哪些 URL。

## 靜態 `robots.txt`

```txt filename="app/robots.txt"
User-Agent: *
Allow: /
Disallow: /private/

Sitemap: https://acme.com/sitemap.xml
```

## 生成 Robots 檔案

新增一個 `robots.js` 或 `robots.ts` 檔案，該檔案需傳回 [`Robots` 物件](#robots-object)。

> **值得注意**：`robots.js` 是一個特殊的路由處理器 (Route Handler)，預設會進行快取，除非使用 [Request-time API (請求時 API)](/docs/app/glossary#request-time-apis) 或 [dynamic config (動態配置)](/docs/app/guides/caching-without-cache-components#dynamic) 選項。

```ts filename="app/robots.ts" switcher
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    sitemap: 'https://acme.com/sitemap.xml',
  }
}
```

```js filename="app/robots.js" switcher
export default function robots() {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    sitemap: 'https://acme.com/sitemap.xml',
  }
}
```

輸出：

```txt
User-Agent: *
Allow: /
Disallow: /private/

Sitemap: https://acme.com/sitemap.xml
```

### 自訂特定使用者代理

您可以將使用者代理陣列傳遞至 `rules` 屬性，自訂個別搜尋引擎爬蟲爬取您網站的方式。例如：

```ts filename="app/robots.ts" switcher
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: 'Googlebot',
        allow: ['/'],
        disallow: '/private/',
      },
      {
        userAgent: ['Applebot', 'Bingbot'],
        disallow: ['/'],
      },
    ],
    sitemap: 'https://acme.com/sitemap.xml',
  }
}
```

```js filename="app/robots.js" switcher
export default function robots() {
  return {
    rules: [
      {
        userAgent: 'Googlebot',
        allow: ['/'],
        disallow: ['/private/'],
      },
      {
        userAgent: ['Applebot', 'Bingbot'],
        disallow: ['/'],
      },
    ],
    sitemap: 'https://acme.com/sitemap.xml',
  }
}
```

輸出：

```txt
User-Agent: Googlebot
Allow: /
Disallow: /private/

User-Agent: Applebot
Disallow: /

User-Agent: Bingbot
Disallow: /

Sitemap: https://acme.com/sitemap.xml
```

### Robots 物件

```tsx
type Robots = {
  rules:
    | {
        userAgent?: string | string[]
        allow?: string | string[]
        disallow?: string | string[]
        crawlDelay?: number
      }
    | Array<{
        userAgent: string | string[]
        allow?: string | string[]
        disallow?: string | string[]
        crawlDelay?: number
      }>
  sitemap?: string | string[]
  host?: string
}
```

## 版本歷史

| 版本   | 變更               |
| --------- | -------------------- |
| `v13.3.0` | 已新增 `robots`。|
