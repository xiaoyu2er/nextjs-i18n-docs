---
title: instrumentation.js
description: instrumentation.js 檔案的 API 參考文件。
related:
  title: 了解更多關於儀譜化 (Instrumentation)
  links:
    - app/guides/instrumentation
---

`instrumentation.js|ts` 檔案用於將可觀測性工具整合到您的應用程式中，讓您可以追蹤效能與行為，並在生產環境中除錯問題。

要使用它，請將檔案放置在應用程式的**根目錄**中，或者如果您使用 [`src` 資料夾](/docs/app/api-reference/file-conventions/src-folder)，則放置在該資料夾內。

## 匯出

### `register` (可選)

此檔案匯出 `register` 函式，該函式在啟動新的 Next.js 伺服器實例時會被呼叫 **一次**，且必須在伺服器準備好處理請求之前完成。`register` 可以是一個異步函式。

```ts filename="instrumentation.ts" switcher
import { registerOTel } from '@vercel/otel'

export function register() {
  registerOTel('next-app')
}
```

```js filename="instrumentation.js" switcher
import { registerOTel } from '@vercel/otel'

export function register() {
  registerOTel('next-app')
}
```

### `onRequestError` (可選)

您可以選擇性地匯出 `onRequestError` 函式，將**伺服器**錯誤追蹤至任何自訂可觀測性提供者。

- 如果您在 `onRequestError` 中執行任何非同步任務，請確保它們已被 `await`。當 Next.js 伺服器捕獲錯誤時，將觸發 `onRequestError`。
- `error` 實例可能並非原始拋出的錯誤實例，因為若在服务端組件 (Server Components) 渲染期間遇到，它可能會被 React 處理。如果發生此情況，您可以使用錯誤上的 `digest` 屬性來辨識實際的錯誤類型。

```ts filename="instrumentation.ts" switcher
import { type Instrumentation } from 'next'

export const onRequestError: Instrumentation.onRequestError = async (
  err,
  request,
  context
) => {
  await fetch('https://.../report-error', {
    method: 'POST',
    body: JSON.stringify({
      message: err.message,
      request,
      context,
    }),
    headers: {
      'Content-Type': 'application/json',
    },
  })
}
```

```js filename="instrumentation.js" switcher
export async function onRequestError(err, request, context) {
  await fetch('https://.../report-error', {
    method: 'POST',
    body: JSON.stringify({
      message: err.message,
      request,
      context,
    }),
    headers: {
      'Content-Type': 'application/json',
    },
  })
}
```

#### 參數

此函式接受三個參數：`error`、`request` 和 `context`。

```ts filename="Types"
export function onRequestError(
  error: { digest: string } & Error,
  request: {
    path: string // resource path, e.g. /blog?name=foo
    method: string // request method. e.g. GET, POST, etc
    headers: { [key: string]: string | string[] }
  },
  context: {
    routerKind: 'Pages Router' | 'App Router' // the router type
    routePath: string // the route file path, e.g. /app/blog/[dynamic]
    routeType: 'render' | 'route' | 'action' | 'proxy' // the context in which the error occurred
    renderSource:
      | 'react-server-components'
      | 'react-server-components-payload'
      | 'server-rendering'
    revalidateReason: 'on-demand' | 'stale' | undefined // undefined is a normal request without revalidation
    renderType: 'dynamic' | 'dynamic-resume' // 'dynamic-resume' for PPR
  }
): void | Promise<void>
```

- `error`: 捕獲到的錯誤本身 (類型總是 `Error`)，以及一個 `digest` 屬性，這是錯誤的唯一 ID。
- `request`: 與錯誤相關的唯讀 (Read-only) 請求資訊。
- `context`: 錯誤發生的上下文。這可以是路由的類型 (App 路由 (App Router) 或 Pages 路由 (Pages Router))，以及/或者 (伺服器組件 (Server Components) (`'render'`)、路由處理函式 (Route Handlers) (`'route'`)、伺服器動作 (Server Actions) (`'action'`)，或 Proxy (`'proxy')。`} ```js``` (Wait, I added extra code block in my thought, ignore. Final check on JSON validity.) 

Wait, one error in Node 2 draft: `Proxy ('proxy')`. Original has `Proxy (`'proxy'`)`. `Proxy (`'proxy'`)`. My draft `Proxy ('proxy')`. I missed the backticks around 'proxy' in the last item of my manual draft in the thought block above. Correct: `Proxy (`'proxy'`)`. 
Also the closing parenthesis count. `... Proxy ($'proxy$))`. 
Input: `... or Proxy (`'proxy'`)).` 
Wait, Input: `... or Proxy (`'proxy'`)).` 
Actually, the input string ends with `Proxy (`'proxy'`))` before the closing quote. 
I will ensure I match the punctuation count. 
Input: `Server Components (`'render'`), Route Handlers (`'route'`), Server Actions (`'action'`), or Proxy (`'proxy'`)).` 
Translation: `伺服器組件 (Server Components) (`'render'`), 路由處理函式 (Route Handlers) (`'route'`), 伺服器動作 (Server Actions) (`'action'`), 或 Proxy (`'proxy'`)).` 
Okay. 
One more check: `Next.js 伺服器實例`. Is `實例` needed? `Instance`. Yes. `Server Instance`. 
`Async function`. `異步函式`. 
`Register`. Keep backticks. 
`Once`. Bold. `**一次**`. 
Okay. 
Correcting Node 2 text closing `))` vs `)`. 
Input: `... Proxy (`'proxy'`)).` 
My text must end with `)).` 
Wait, looking at input again: `... or Proxy (`'proxy'`))`. 
Yes. 
Double checking spaces. `Read-only` -> `唯讀 (Read-only)`. Space before parenthesis? `資訊。` No space. `Read-only) 請求`. Space after parenthesis. `唯讀 (Read-only) 請求`. 
Chinese chars + English space. `Read-only` ends with `-`. `-` is symbol. `) ` is symbol. 
Okay. 
Finalizing. ``` json 
 {

### 指定執行環境

`instrumentation.js` 檔案在 Node.js 和 Edge 執行環境中均可運作，但您可以使用 `process.env.NEXT_RUNTIME` 來針對特定的執行環境。

```js filename="instrumentation.js"
export function register() {
  if (process.env.NEXT_RUNTIME === 'edge') {
    return require('./register.edge')
  } else {
    return require('./register.node')
  }
}

export function onRequestError() {
  if (process.env.NEXT_RUNTIME === 'edge') {
    return require('./on-request-error.edge')
  } else {
    return require('./on-request-error.node')
  }
}
```

## 版本歷史

| 版本 | 變更紀錄 |
| --------- | ------------------------------------------------------- |
| `v15.0.0` | 導入 `onRequestError`，`instrumentation` 穩定 |
| `v14.0.4` | `instrumentation` 支援 Turbopack |
| `v13.2.0` | `instrumentation` 作為實驗性功能導入 |
