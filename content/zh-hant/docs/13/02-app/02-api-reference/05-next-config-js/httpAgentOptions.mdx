---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-25T21:39:10.988Z
title: httpAgentOptions
description: Next.js 預設會自動使用 HTTP Keep-Alive。了解更多關於如何停用 HTTP Keep-Alive 的資訊。
---

{/* 此文件內容同時適用於 app 和 pages 路由。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於 Pages Router 的內容。任何共享內容不應被包裹在元件中。 */}

在 Node.js 18 之前的版本中，Next.js 會自動使用 [undici](/docs/architecture/supported-browsers#polyfills) 來 polyfill `fetch()` 並預設啟用 [HTTP Keep-Alive](https://developer.mozilla.org/docs/Web/HTTP/Headers/Keep-Alive)。

若要停用伺服器端所有 `fetch()` 呼叫的 HTTP Keep-Alive 功能，請開啟 `next.config.js` 並新增 `httpAgentOptions` 設定：

```js filename="next.config.js"
module.exports = {
  httpAgentOptions: {
    keepAlive: false,
  },
}
```