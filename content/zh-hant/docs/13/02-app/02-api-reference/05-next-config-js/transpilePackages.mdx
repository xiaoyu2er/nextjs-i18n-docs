---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-25T21:38:22.987Z
title: transpilePackages
description: 自動轉譯並打包來自本地套件（如 monorepos）或外部依賴（`node_modules`）的相依套件。
---

{/* 此文件內容在 app 和 pages 路由間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於 Pages Router 的內容。任何共享內容不應包裹在元件中。 */}

Next.js 能自動轉譯並打包來自本地套件（如 monorepos）或外部依賴（`node_modules`）的相依套件。此功能取代了 `next-transpile-modules` 套件。

```js filename="next.config.js"
/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@acme/ui', 'lodash-es'],
}

module.exports = nextConfig
```

## 版本歷史

| 版本       | 變更                        |
| ---------- | --------------------------- |
| `v13.0.0` | 新增 `transpilePackages` 功能。 |