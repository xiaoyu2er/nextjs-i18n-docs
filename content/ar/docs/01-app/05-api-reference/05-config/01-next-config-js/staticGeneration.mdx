---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:13:31.273Z
title: staticGeneration*
description: تعرف على كيفية تكوين التوليد الثابت (Static Generation) في تطبيق Next.js الخاص بك.
version: experimental
---

تتيح لك خيارات `staticGeneration*` تكوين عملية التوليد الثابت (Static Generation) لحالات الاستخدام المتقدمة.

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
const nextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

## خيارات التكوين

تتوفر الخيارات التالية:

- `staticGenerationRetryCount`: عدد مرات إعادة محاولة توليد صفحة فاشلة قبل فشل عملية البناء.
- `staticGenerationMaxConcurrency`: الحد الأقصى لعدد الصفحات التي يتم معالجتها لكل عامل (worker).
- `staticGenerationMinPagesPerWorker`: الحد الأدنى لعدد الصفحات التي يتم معالجتها قبل بدء عامل جديد.