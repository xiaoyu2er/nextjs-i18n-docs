---
source-updated-at: 2025-05-29T18:05:49.000Z
translation-updated-at: 2025-06-02T19:45:46.272Z
title: البدء
headline: 'موجه التطبيق (App Router): البدء'
description: >-
  إنشاء تطبيق Next.js جديد باستخدام مثال لوحة التحكم المبدئي واستكشاف المشروع.
image: 'https://nextjs.org/api/learn-og?title=Getting%20Started&amp;chapter=1'
---

[إنشاء مشروع جديد](#creating-a-new-project)
-------------------------------------------

نوصي باستخدام [`pnpm`](https://pnpm.io/) كمدير للحزم، لأنه أسرع وأكثر كفاءة من `npm` أو `yarn`. إذا لم يكن لديك `pnpm` مثبتًا، يمكنك تثبيته عالميًا عن طريق تنفيذ:

```bash filename="Terminal"
npm install -g pnpm
```

لإنشاء تطبيق Next.js، افتح طرفيتك، انتقل [`cd`](https://developer.mozilla.org/en-US/docs/Learn/Tools_and_testing/Understanding_client-side_tools/Command_line#basic_built-in_terminal_commands) إلى المجلد الذي ترغب في حفظ مشروعك فيه، ثم نفذ الأمر التالي:

```bash filename="Terminal"
npx create-next-app@latest nextjs-dashboard --example "https://github.com/vercel/next-learn/tree/main/dashboard/starter-example" --use-pnpm
```

يستخدم هذا الأمر [`create-next-app`](/docs/app/api-reference/create-next-app)، وهي أداة واجهة سطر الأوامر (CLI) تقوم بإعداد تطبيق Next.js لك. في الأمر أعلاه، أنت تستخدم أيضًا علم `--example` مع [المثال المبدئي](https://github.com/vercel/next-learn/tree/main/dashboard/starter-example) لهذه الدورة.

[استكشاف المشروع](#exploring-the-project)
-----------------------------------------

على عكس البرامج التعليمية التي تجعلك تكتب الكود من الصفر، فإن معظم كود هذه الدورة مكتوب مسبقًا لك. هذا يعكس بشكل أفضل التطوير في العالم الحقي، حيث من المحتمل أن تعمل مع قواعد كود موجودة.

هدفنا هو مساعدتك في التركيز على تعلم الميزات الرئيسية لـ Next.js، دون الحاجة إلى كتابة _كل_ كود التطبيق.

بعد التثبيت، افتح المشروع في محرر الأكواد الخاص بك وانتقل إلى `nextjs-dashboard`.

```bash filename="Terminal"
cd nextjs-dashboard
```

لنقضي بعض الوقت في استكشاف المشروع.

### [هيكل المجلدات](#folder-structure)

ستلاحظ أن المشروع لديه هيكل المجلدات التالي:

<Image
  alt="هيكل مجلدات مشروع لوحة التحكم، يظهر المجلدات والملفات الرئيسية: app، public، وملفات التكوين."
  srcLight="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/light/learn-folder-structure.png"
  srcDark="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/dark/learn-folder-structure.png"
  width="1600"
  height="606"
/>

*   **`/app`**: يحتوي على جميع المسارات والمكونات والمنطق لتطبيقك، هذا هو المكان الذي ستعمل منه غالبًا.
*   **`/app/lib`**: يحتوي على الدوال المستخدمة في تطبيقك، مثل دوال الأدوات القابلة لإعادة الاستخدام ودوال جلب البيانات.
*   **`/app/ui`**: يحتوي على جميع مكونات واجهة المستخدم لتطبيقك، مثل البطاقات والجداول والنماذج. لتوفير الوقت، قمنا بتصميم هذه المكونات مسبقًا لك.
*   **`/public`**: يحتوي على جميع الأصول الثابتة لتطبيقك، مثل الصور.
*   **ملفات التكوين**: ستلاحظ أيضًا ملفات تكوين مثل `next.config.ts` في جذر تطبيقك. معظم هذه الملفات يتم إنشاؤها وتكوينها مسبقًا عند بدء مشروع جديد باستخدام `create-next-app`. لن تحتاج إلى تعديلها في هذه الدورة.

لا تتردد في استكشاف هذه المجلدات، ولا تقلق إذا لم تفهم كل ما يفعله الكود بعد.

### [بيانات نائبة](#placeholder-data)

عندما تقوم ببناء واجهات المستخدم، من المفيد أن يكون لديك بعض البيانات النائبة. إذا لم يكن قاعدة البيانات أو واجهة برمجة التطبيقات (API) متاحة بعد، يمكنك:

*   استخدام بيانات نائبة بتنسيق JSON أو ككائنات JavaScript.
*   استخدام خدمة خارجية مثل [mockAPI](https://mockapi.io/).

لهذا المشروع، قمنا بتوفير بعض البيانات النائبة في `app/lib/placeholder-data.ts`. كل كائن JavaScript في الملف يمثل جدولاً في قاعدة البيانات الخاصة بك. على سبيل المثال، لجدول الفواتير:

```ts filename="/app/lib/placeholder-data.ts"
const invoices = [
  {
    customer_id: customers[0].id,
    amount: 15795,
    status: 'pending',
    date: '2022-12-06',
  },
  {
    customer_id: customers[1].id,
    amount: 20348,
    status: 'pending',
    date: '2022-11-14',
  },
  // ...
];
```

في فصل [إعداد قاعدة البيانات](/learn/dashboard-app/setting-up-your-database)، ستستخدم هذه البيانات لـ _بذر_ قاعدة البيانات (ملؤها ببعض البيانات الأولية).

### [TypeScript](#typescript)

قد تلاحظ أيضًا أن معظم الملفات لها لاحقة `.ts` أو `.tsx`. هذا لأن المشروع مكتوب بـ TypeScript. أردنا إنشاء دورة تعكس مشهد الويب الحديث.

لا بأس إذا كنت لا تعرف TypeScript - سنوفر مقاطع كود TypeScript عند الحاجة.

في الوقت الحالي، ألق نظرة على ملف `/app/lib/definitions.ts`. هنا، نحدد يدويًا الأنواع التي سيتم إرجاعها من قاعدة البيانات. على سبيل المثال، جدول الفواتير لديه الأنواع التالية:

```ts filename="/app/lib/definitions.ts"
export type Invoice = {
  id: string;
  customer_id: string;
  amount: number;
  date: string;
  // في TypeScript، هذا يسمى نوع اتحاد سلسلة.
  // يعني أن خاصية "status" يمكن أن تكون واحدة فقط من السلسلتين: 'pending' أو 'paid'.
  status: 'pending' | 'paid';
};
```

باستخدام TypeScript، يمكنك التأكد من أنك لن تمرر عن طريق الخطأ تنسيق بيانات خاطئ إلى مكوناتك أو قاعدة البيانات، مثل تمرير `string` بدلاً من `number` إلى `amount` الفاتورة.

> **إذا كنت مطور TypeScript:**
> 
> *   نحن نعلن أنواع البيانات يدويًا، ولكن لمزيد من الأمان النوعي، نوصي بـ [Prisma](https://www.prisma.io/) أو [Drizzle](https://orm.drizzle.team/)، والتي تولد الأنواع تلقائيًا بناءً على مخطط قاعدة البيانات الخاصة بك.
> *   يكتشف Next.js إذا كان مشروعك يستخدم TypeScript ويقوم تلقائيًا بتثبيت الحزم والتكوين اللازم. يأتي Next.js أيضًا مع [ملحق TypeScript](https://nextjs.org/docs/app/building-your-application/configuring/typescript#typescript-plugin) لمحرر الأكواد الخاص بك، للمساعدة في الإكمال التلقائي والأمان النوعي.

[تشغيل خادم التطوير](#running-the-development-server)
----------------------------------------------------

نفذ `pnpm i` لتثبيت حزم المشروع.

```bash filename="Terminal"
pnpm i
```

ثم نفذ `pnpm dev` لبدء خادم التطوير.

```bash filename="Terminal"
pnpm dev
```

يبدأ `pnpm dev` خادم تطوير Next.js الخاص بك على المنفذ `3000`. دعنا نتحقق مما إذا كان يعمل.

افتح [http://localhost:3000](http://localhost:3000/) في متصفحك. يجب أن تبدو صفحتك الرئيسية هكذا، وهي غير مصممة عن قصد:

<Image
  alt="صفحة غير مصممة بعنوان 'Acme'، ووصف، ورابط تسجيل الدخول."
  srcLight="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/light/acme-unstyled.png"
  srcDark="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/dark/acme-unstyled.png"
  width="1600"
  height="400"
/>