---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:15:16.179Z
title: httpAgentOptions
description: Next.js verwendet standardmäßig automatisch HTTP Keep-Alive. Erfahren Sie hier mehr darüber, wie Sie HTTP Keep-Alive deaktivieren können.
---

{/* Der Inhalt dieses Dokuments wird sowohl für den App- als auch für den Pages-Router verwendet. Sie können die Komponente `<PagesOnly>Inhalt</PagesOnly>` verwenden, um Inhalte hinzuzufügen, die spezifisch für den Pages Router sind. Gemeinsam genutzte Inhalte sollten nicht in eine Komponente eingeschlossen werden. */}

In Node.js-Versionen vor 18 verwendet Next.js automatisch ein Polyfill für `fetch()` mit [undici](/docs/architecture/supported-browsers#polyfills) und aktiviert standardmäßig [HTTP Keep-Alive](https://developer.mozilla.org/docs/Web/HTTP/Headers/Keep-Alive).

Um HTTP Keep-Alive für alle `fetch()`-Aufrufe auf der Serverseite zu deaktivieren, öffnen Sie `next.config.js` und fügen Sie die `httpAgentOptions`-Konfiguration hinzu:

```js filename="next.config.js"
module.exports = {
  httpAgentOptions: {
    keepAlive: false,
  },
}
```