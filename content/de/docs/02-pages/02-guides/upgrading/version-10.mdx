---
title: Upgrade auf Version 10
nav_title: Version 10
description: So aktualisieren Sie Ihre Next.js-Anwendung von Version 9 auf Version 10.
---

Es gab keine Breaking Changes zwischen den Versionen 9 und 10.

Um auf Version 10 zu aktualisieren, führen Sie folgenden Befehl aus:

```bash filename="Terminal"
npm i next@10
```

```bash filename="Terminal"
yarn add next@10
```

```bash filename="Terminal"
pnpm up next@10
```

```bash filename="Terminal"
bun add next@10
```

> **Wissenswert:** Wenn Sie TypeScript verwenden, stellen Sie sicher, dass Sie auch `@types/react` und `@types/react-dom` auf die entsprechenden Versionen aktualisieren.