---
title: 自定义 Next.js 缓存处理器
nav_title: cacheHandler
description: 配置用于存储和重新验证数据的 Next.js 缓存，以使用任何外部服务，如 Redis、Memcached 或其他服务。

如果你想将缓存的页面和数据持久化到持久存储，或者在多个 Next.js 应用程序容器或实例之间共享缓存，你可以配置 Next.js 缓存位置。

> **值得了解**：`cacheHandler`（单数）配置专门用于 Next.js 的服务器缓存操作，例如存储和重新验证 ISR、路由处理器响应以及优化后的图片。它**不**用于 `'use cache'` 指令。对于 `'use cache'` 指令，请使用 [`cacheHandlers`](/docs/app/api-reference/config/next-config-js/cacheHandlers)（复数）

```js filename="next.config.js"
module.exports = {
  cacheHandler: require.resolve('./cache-handler.js'),
  cacheMaxMemorySize: 0, // disable default in-memory caching
}
```

查看 [自定义缓存处理器](/docs/app/guides/self-hosting#configuring-caching) 示例，并了解更多实现信息。

## API 参考

缓存处理器可以实现以下方法：`get`、`set`、`revalidateTag` 和 `resetRequestCache`。

### `get()`

| 参数 | 类型     | 描述                             |
| --------- | -------- | --------------------------------------- |
| `key`     | `string` | 缓存值的键。            |
| `ctx`     | `object` | 上下文，包括缓存项类型。 |

「ctx」参数包含一个 `kind` 属性，该属性指示正在检索的缓存项类型。可能的值包括 `'APP_PAGE'`, `'APP_ROUTE'`, `'PAGES'`, `'FETCH'` 和 `'IMAGE'`。

返回缓存的值，如果未找到则返回 `null`。

### `set()`

| 参数 | 类型           | 描述                      |
| --------- | -------------- | -------------------------------- |
| `key`     | `string`       | 存储数据的键。 |
| `data`    | Data 或 `null` | 要缓存的数据。           |
| `ctx`     | `{ tags: [] }` | 提供的缓存标签。         |

`data` 对象包含一个 `kind` 属性，用于指示缓存项的类型。对于图片优化，`kind` 将为 `'IMAGE'`，并且数据将包含诸如 `buffer`, `etag`, `extension` 和 `revalidate` 之类的属性。

返回 `Promise<void>`。

### `revalidateTag()`

| 参数 | 类型                   | 描述                   |
| --------- | ---------------------- | ----------------------------- |
| `tag`     | `string` 或 `string[]` | 要重新验证的缓存标签。 |

返回 `Promise<void>`。了解有关 [重新验证数据](/docs/app/guides/incremental-static-regeneration) 或 [`revalidateTag()`](/docs/app/api-reference/functions/revalidateTag) 函数的更多信息。

### `resetRequestCache()`

此方法将在下一个请求之前重置单个请求的临时内存缓存。

返回 `void`。

**值得注意：**

- `revalidatePath` 是缓存标签的便利层。调用 `revalidatePath` 将调用你的 `revalidateTag` 函数，然后你可以选择是否根据路径为缓存键添加标签。

## 图片优化缓存

`cacheHandler` 也可用于缓存来自 `next/image` 的优化图片。要启用此功能，请在 `next.config.js` 中将 `images.customCacheHandler` 设置为 `true`：

```js filename="next.config.js"
module.exports = {
  cacheHandler: require.resolve('./cache-handler.js'),
  images: {
    customCacheHandler: true,
  },
}
```

> **值得了解**：此可选标志 (opt-in flag) 将在下一个主版本中成为默认行为。现在设置它可以让您的缓存处理程序 (cache handler) 为图像优化条目 (image optimization entries) 做好准备。

您可以使用 `kind` 属性区分缓存项类型，并在需要时单独处理图片，例如实施驱逐策略或将图片存储在不同的位置。

在处理图片缓存条目时，`kind` 将为 `'IMAGE'`，数据将包含 `buffer`, `etag`, `extension` 和 `revalidate` 属性。

## 平台支持

请参阅[自托管指南](/docs/app/guides/self-hosting#streaming-and-suspense)获取详细的配置说明。

了解如何在自托管 Next.js 时[配置 ISR](/docs/app/guides/self-hosting#caching-and-isr)。

## 版本历史

| 版本    | 变更                                                      |
| --------- | ------------------------------------------------------------ |
| `v16.2.0` | `cacheHandler` 支持图片优化缓存。       |
| `v14.1.0` | 重命名为 `cacheHandler` 并变得稳定。                 |
| `v13.4.0` | `incrementalCacheHandlerPath` 支持 `revalidateTag`。   |
| `v13.4.0` | `incrementalCacheHandlerPath` 支持独立输出。 |
| `v12.2.0` | 实验性 `incrementalCacheHandlerPath` 已添加。            |
