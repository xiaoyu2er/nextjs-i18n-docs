---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:09:07.766Z
title: instrumentation-client.js
description: Aprenda cómo añadir instrumentación del lado del cliente para rastrear y monitorear el rendimiento del frontend en su aplicación Next.js.
---

El archivo `instrumentation-client.js|ts` permite añadir código de monitoreo y analítica que se ejecuta antes de que comience a ejecutarse el código frontend de su aplicación. Esto es útil para configurar seguimiento de rendimiento, monitoreo de errores u otras herramientas de observabilidad del lado del cliente.

Para usarlo, coloque el archivo en la **raíz** de su aplicación o dentro de una carpeta `src`.

## Uso

A diferencia de la [instrumentación del lado del servidor](/docs/app/guides/instrumentation), no necesita exportar funciones específicas. Puede escribir su código de monitoreo directamente en el archivo:

```ts filename="instrumentation-client.ts" switcher
// Set up performance monitoring
performance.mark('app-init')

// Initialize analytics
console.log('Analytics initialized')

// Set up error tracking
window.addEventListener('error', (event) => {
  // Send to your error tracking service
  reportError(event.error)
})
```

```js filename="instrumentation-client.js" switcher
// Set up performance monitoring
performance.mark('app-init')

// Initialize analytics
console.log('Analytics initialized')

// Set up error tracking
window.addEventListener('error', (event) => {
  // Send to your error tracking service
  reportError(event.error)
})
```

## Historial de versiones

| Versión | Cambios                             |
| ------- | ----------------------------------- |
| `v15.3` | Se introdujo `instrumentation-client` |