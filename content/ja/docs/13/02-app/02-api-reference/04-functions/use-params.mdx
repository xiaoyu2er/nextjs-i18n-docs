---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:16:27.099Z
title: useParams
description: useParams フックの API リファレンス。
---

`useParams` は、現在の URL によって埋められたルートの [動的パラメータ (dynamic params)](/docs/app/building-your-application/routing/dynamic-routes) を読み取ることができる **クライアントコンポーネント (Client Component)** 用フックです。

```tsx filename="app/example-client-component.tsx" switcher
'use client'

import { useParams } from 'next/navigation'

export default function ExampleClientComponent() {
  const params = useParams()

  // Route -> /shop/[tag]/[item]
  // URL -> /shop/shoes/nike-air-max-97
  // `params` -> { tag: 'shoes', item: 'nike-air-max-97' }
  console.log(params)

  return <></>
}
```

```jsx filename="app/example-client-component.js" switcher
'use client'

import { useParams } from 'next/navigation'

export default function ExampleClientComponent() {
  const params = useParams()

  // Route -> /shop/[tag]/[item]
  // URL -> /shop/shoes/nike-air-max-97
  // `params` -> { tag: 'shoes', item: 'nike-air-max-97' }
  console.log(params)

  return <></>
}
```

## パラメータ

```tsx
const params = useParams()
```

`useParams` はパラメータを受け取りません。

## 戻り値

`useParams` は、現在のルートの [動的パラメータ (dynamic parameters)](/docs/app/building-your-application/routing/dynamic-routes) が埋められたオブジェクトを返します。

- オブジェクトの各プロパティはアクティブな動的セグメントです。
- プロパティ名はセグメント名、プロパティ値はセグメントに埋め込まれた値です。
- プロパティ値は [動的セグメントのタイプ](/docs/app/building-your-application/routing/dynamic-routes) に応じて `string` または `string` の配列になります。
- ルートに動的パラメータが含まれていない場合、`useParams` は空のオブジェクトを返します。
- `pages` で使用した場合、`useParams` は `null` を返します。

例:

| ルート                           | URL         | `useParams()`             |
| ------------------------------- | ----------- | ------------------------- |
| `app/shop/page.js`              | `/shop`     | `null`                    |
| `app/shop/[slug]/page.js`       | `/shop/1`   | `{ slug: '1' }`           |
| `app/shop/[tag]/[item]/page.js` | `/shop/1/2` | `{ tag: '1', item: '2' }` |
| `app/shop/[...slug]/page.js`    | `/shop/1/2` | `{ slug: ['1', '2'] }`    |

## バージョン履歴

| バージョン   | 変更内容                 |
| --------- | ----------------------- |
| `v13.3.0` | `useParams` が導入されました。 |