---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:31:52.311Z
title: manifest.json
description: manifest.json ファイルのAPIリファレンス
---

[Web Manifest 仕様](https://developer.mozilla.org/docs/Web/Manifest)に準拠した `manifest.(json|webmanifest)` ファイルを `app` ディレクトリの**ルート**に追加または生成することで、Webアプリケーションに関する情報をブラウザに提供できます。

## 静的マニフェストファイル

```json filename="app/manifest.json | app/manifest.webmanifest"
{
  "name": "My Next.js Application",
  "short_name": "Next.js App",
  "description": "An application built with Next.js",
  "start_url": "/"
  // ...
}
```

## マニフェストファイルの生成

[`Manifest` オブジェクト](#manifest-object)を返す `manifest.js` または `manifest.ts` ファイルを追加します。

```ts filename="app/manifest.ts" switcher
import { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'Next.js App',
    short_name: 'Next.js App',
    description: 'Next.js App',
    start_url: '/',
    display: 'standalone',
    background_color: '#fff',
    theme_color: '#fff',
    icons: [
      {
        src: '/favicon.ico',
        sizes: 'any',
        type: 'image/x-icon',
      },
    ],
  }
}
```

```js filename="app/manifest.js" switcher
export default function manifest() {
  return {
    name: 'Next.js App',
    short_name: 'Next.js App',
    description: 'Next.js App',
    start_url: '/',
    display: 'standalone',
    background_color: '#fff',
    theme_color: '#fff',
    icons: [
      {
        src: '/favicon.ico',
        sizes: 'any',
        type: 'image/x-icon',
      },
    ],
  }
}
```

### Manifest オブジェクト

マニフェストオブジェクトには、新しいWeb標準に伴って更新される可能性のある広範なオプションが含まれています。現在利用可能なすべてのオプションについては、[TypeScript](https://nextjs.org/docs/app/building-your-application/configuring/typescript#typescript-plugin)を使用している場合はコードエディタで `MetadataRoute.Manifest` 型を参照するか、[MDN](https://developer.mozilla.org/docs/Web/Manifest)ドキュメントを確認してください。