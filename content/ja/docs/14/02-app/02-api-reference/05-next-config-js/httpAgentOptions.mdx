---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T16:45:21.621Z
title: httpAgentOptions
description: Next.js はデフォルトで HTTP Keep-Alive を自動的に使用します。HTTP Keep-Alive を無効にする方法について詳しく学びましょう。
---

{/* このドキュメントの内容はappとpagesルーターで共有されています。Pages Router固有のコンテンツを追加するには`<PagesOnly>コンテンツ</PagesOnly>`コンポーネントを使用できます。共有コンテンツはコンポーネントでラップしないでください。 */}

Node.js バージョン18より前では、Next.js は自動的に `fetch()` を [undici](/docs/architecture/supported-browsers#polyfills) でポリフィルし、デフォルトで [HTTP Keep-Alive](https://developer.mozilla.org/docs/Web/HTTP/Headers/Keep-Alive) を有効にします。

サーバーサイドのすべての `fetch()` 呼び出しで HTTP Keep-Alive を無効にするには、`next.config.js` を開き、`httpAgentOptions` 設定を追加します:

```js filename="next.config.js"
module.exports = {
  httpAgentOptions: {
    keepAlive: false,
  },
}
```