---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T16:52:21.708Z
title: Частичный предварительный рендеринг (экспериментальная функция)
description: Узнайте, как включить частичный предварительный рендеринг (экспериментальная функция) в Next.js 14.
---

> **Предупреждение**: Частичный предварительный рендеринг является экспериментальной функцией и в настоящее время **не подходит для производственных сред**.

Частичный предварительный рендеринг (Partial Prerendering) — это экспериментальная функция, которая позволяет предварительно отрендерить статические части маршрута и обслуживать их из кэша, одновременно подгружая динамические части в рамках одного HTTP-запроса.

Частичный предварительный рендеринг доступен в `next@canary`:

```bash filename="Terminal"
npm install next@canary
```

Вы можете включить частичный предварительный рендеринг, установив экспериментальный флаг `ppr`:

```js filename="next.config.js"
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    ppr: true,
  },
}

module.exports = nextConfig
```

> **Полезно знать:**
>
> - Частичный предварительный рендеринг пока не применяется к клиентской навигации. Мы активно работаем над этим.
> - Частичный предварительный рендеринг разработан только для [среды выполнения Node.js](/docs/app/building-your-application/rendering/edge-and-nodejs-runtimes). Использование подмножества среды выполнения Node.js не требуется, когда вы можете мгновенно обслуживать статическую оболочку.

Узнайте больше о частичном предварительном рендеринге в [курсе обучения Next.js](/learn/dashboard-app/partial-prerendering).