---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T16:51:39.522Z
title: TypeScript
description: Next.js по умолчанию сообщает об ошибках TypeScript. Узнайте, как отключить это поведение.
---

{/* Содержание этого документа используется как в App Router, так и в Pages Router. Для добавления контента, специфичного для Pages Router, используйте компонент `<PagesOnly>Контент</PagesOnly>`. Общий контент не должен быть обёрнут в компонент. */}

Next.js прерывает **продакшен-сборку** (`next build`), если в вашем проекте присутствуют ошибки TypeScript.

Если вы хотите, чтобы Next.js опасно продолжал сборку продакшен-кода даже при наличии ошибок в приложении, вы можете отключить встроенную проверку типов.

При отключении убедитесь, что проверка типов выполняется как часть процесса сборки или деплоя, иначе это может быть очень опасно.

Откройте `next.config.js` и включите опцию `ignoreBuildErrors` в конфигурации `typescript`:

```js filename="next.config.js"
module.exports = {
  typescript: {
    // !! ВНИМАНИЕ !!
    // Опасное разрешение на успешное завершение продакшен-сборки
    // даже при наличии ошибок типов в проекте.
    // !! ВНИМАНИЕ !!
    ignoreBuildErrors: true,
  },
}
```