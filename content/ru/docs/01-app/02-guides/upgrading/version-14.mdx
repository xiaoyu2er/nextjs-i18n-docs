---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:09:48.882Z
title: Как обновиться до версии 14
nav_title: Версия 14
description: Руководство по обновлению приложения Next.js с версии 13 до 14.
---

{/* Содержание этого документа используется как для App Router, так и для Pages Router. Вы можете использовать компонент `<PagesOnly>Content</PagesOnly>` для добавления контента, специфичного для Pages Router. Общий контент не должен быть обёрнут в компонент. */}

## Обновление с версии 13 до 14

Для обновления до Next.js версии 14 выполните следующую команду, используя предпочитаемый менеджер пакетов:

```bash filename="Терминал"
npm i next@next-14 react@18 react-dom@18 && npm i eslint-config-next@next-14 -D
```

```bash filename="Терминал"
yarn add next@next-14 react@18 react-dom@18 && yarn add eslint-config-next@next-14 -D
```

```bash filename="Терминал"
pnpm i next@next-14 react@18 react-dom@18 && pnpm i eslint-config-next@next-14 -D
```

```bash filename="Терминал"
bun add next@next-14 react@18 react-dom@18 && bun add eslint-config-next@next-14 -D
```

> **Полезно знать:** Если вы используете TypeScript, убедитесь, что также обновили `@types/react` и `@types/react-dom` до их последних версий.

### Краткое содержание v14

- Минимальная версия Node.js увеличена с 16.14 до 18.17, так как версия 16.x достигла конца жизненного цикла.
- Команда `next export` удалена в пользу конфигурации `output: 'export'`. Подробнее см. в [документации](https://nextjs.org/docs/app/guides/static-exports).
- Импорт `ImageResponse` из `next/server` переименован в `next/og`. Доступен [кодмод](/docs/app/guides/upgrading/codemods#next-og-import) для безопасного и автоматического переименования импортов.
- Пакет `@next/font` полностью удалён в пользу встроенного `next/font`. Доступен [кодмод](/docs/app/guides/upgrading/codemods#built-in-next-font) для безопасного и автоматического переименования импортов.
- WASM-таргет для `next-swc` удалён.