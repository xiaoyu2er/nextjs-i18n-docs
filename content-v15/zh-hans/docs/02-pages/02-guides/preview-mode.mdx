---
source-updated-at: 2025-05-21T18:33:43.000Z
translation-updated-at: 2025-05-21T19:16:30.614Z
title: 如何在 Next.js 中使用预览模式查看内容
nav_title: 预览模式
description: Next.js 为静态生成页面提供了预览模式功能。您可以在此了解其工作原理。
version: legacy
---

{/* md5:3b03af21aafc52068742f8e32c06d676 */}
> **注意**：此功能已被 [草稿模式 (Draft Mode)](/docs/pages/guides/draft-mode) 取代。

<details>

{/* md5:4d2de565312c41a1d9dbd79ddb62339b */}
  <summary>示例</summary>

{/* md5:a9967d9a2f4122352f30e4c9116b3df4 */}
- [Agility CMS 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-agilitycms) ([演示](https://next-blog-agilitycms.vercel.app/))
- [Builder.io 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-builder-io) ([演示](https://cms-builder-io.vercel.app/))
- [ButterCMS 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-buttercms) ([演示](https://next-blog-buttercms.vercel.app/))
- [Contentful 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-contentful) ([演示](https://app-router-contentful.vercel.app/))
- [Cosmic 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-cosmic) ([演示](https://next-blog-cosmic.vercel.app/))
- [DatoCMS 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-datocms) ([演示](https://next-blog-datocms.vercel.app/))
- [DotCMS 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-dotcms) ([演示](https://nextjs-dotcms-blog.vercel.app/))
- [Drupal 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-drupal) ([演示](https://cms-drupal.vercel.app/))
- [Enterspeed 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-enterspeed) ([演示](https://next-blog-demo.enterspeed.com/))
- [GraphCMS 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-graphcms) ([演示](https://next-blog-graphcms.vercel.app/))
- [Keystone 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-keystonejs-embedded) ([演示](https://nextjs-keystone-demo.vercel.app/))
- [Kontent.ai 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-kontent-ai) ([演示](https://next-blog-kontent-ai.vercel.app/))
- [Makeswift 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-makeswift) ([演示](https://nextjs-makeswift-example.vercel.app/))
- [Plasmic 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-plasmic) ([演示](https://nextjs-plasmic-example.vercel.app/))
- [Prepr 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-prepr) ([演示](https://next-blog-prepr.vercel.app/))
- [Prismic 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-prismic) ([演示](https://next-blog-prismic.vercel.app/))
- [Sanity 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-sanity) ([演示](https://next-blog.sanity.build/))
- [Sitecore XM Cloud 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-sitecore-xmcloud) ([演示](https://vercel-sitecore-xmcloud-demo.vercel.app/))
- [Storyblok 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-storyblok) ([演示](https://next-blog-storyblok.vercel.app/))
- [Strapi 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-strapi) ([演示](https://next-blog-strapi.vercel.app/))
- [TakeShape 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-takeshape) ([演示](https://next-blog-takeshape.vercel.app/))
- [Tina 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-tina) ([演示](https://cms-tina-example.vercel.app/))
- [Umbraco 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-umbraco) ([演示](https://nextjs-umbraco-sample-blog.vercel.app/))
- [Umbraco Heartcore 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-umbraco-heartcore) ([演示](https://next-blog-umbraco-heartcore.vercel.app/))
- [Webiny 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-webiny) ([演示](https://webiny-headlesscms-nextjs-example.vercel.app/))
- [WordPress 示例](https://github.com/vercel/next.js/tree/canary/examples/cms-wordpress) ([演示](https://next-blog-wordpress.vercel.app/))
- [博客起始示例](https://github.com/vercel/next.js/tree/canary/examples/blog-starter) ([演示](https://next-blog-starter.vercel.app/))

</details>

{/* md5:f186c36c546b52cb6ba7032dd8b560d1 */}
在 [页面文档](/docs/pages/building-your-application/routing/pages-and-layouts) 和 [数据获取文档](/docs/pages/building-your-application/data-fetching) 中，我们讨论了如何使用 `getStaticProps` 和 `getStaticPaths` 在构建时预渲染页面（**静态生成**）。

{/* md5:bf562033a24c807bbd07e6f1508df614 */}
当您的页面从无头 CMS (headless CMS) 获取数据时，静态生成非常有用。但是，当您在无头 CMS 上撰写草稿并希望立即在页面上**预览**草稿时，静态生成并不理想。您会希望 Next.js 在**请求时**而非构建时渲染这些页面，并获取草稿内容而非已发布内容。您会希望 Next.js 仅针对这种情况绕过静态生成。

{/* md5:a71cd1c7b9862b98b691881aee930098 */}
Next.js 提供了称为**预览模式 (Preview Mode)** 的功能来解决此问题。以下是使用方法的说明。

{/* md5:2203ae8b5cfca41a8569f4441ef674a1 */}
## 第一步：创建并访问预览 API 路由

{/* md5:9fd1dde45fd3fb759797e1042184553c */}
> 如果您不熟悉 Next.js 的 API 路由，请先查看 [API 路由文档](/docs/pages/building-your-application/routing/api-routes)。

{/* md5:19895ed135411dd451a53e8443872ba6 */}
首先，创建一个**预览 API 路由**。它可以有任何名称，例如 `pages/api/preview.js`（如果使用 TypeScript，则为 `.ts`）。

{/* md5:dae1dbe0bcbbdbfb89b7b454682ce0e3 */}
在此 API 路由中，您需要在响应对象上调用 `setPreviewData`。`setPreviewData` 的参数应为一个对象，该对象可由 `getStaticProps` 使用（稍后会详细介绍）。现在，我们将使用 `{}`。

```js
export default function handler(req, res) {
  // ...
  res.setPreviewData({})
  // ...
}
```

{/* md5:127f93108c42f2cec9d91982c4afcf1f */}
`res.setPreviewData` 会在浏览器上设置一些 **cookies**，从而启用预览模式。任何包含这些 cookies 的 Next.js 请求都将被视为**预览模式**，静态生成页面的行为将发生变化（稍后会详细介绍）。

{/* md5:2ac5687a1c44dc5b7604a2fefcdab840 */}
您可以通过手动创建如下 API 路由并从浏览器手动访问来进行测试：

```js filename="pages/api/preview.js"
// simple example for testing it manually from your browser.
export default function handler(req, res) {
  res.setPreviewData({})
  res.end('Preview mode enabled')
}
```

{/* md5:c5b1008fc80b325d169fee83778a7ac6 */}
如果您打开浏览器的开发者工具并访问 `/api/preview`，您会注意到此请求会设置 `__prerender_bypass` 和 `__next_preview_data` cookies。

{/* md5:311844498aeb3c63a085e1a865f67de6 */}
### 从无头 CMS 安全访问

{/* md5:57f7aa969e8957d2ff4a8d9311d2511c */}
实际上，您会希望从无头 CMS **安全地**调用此 API 路由。具体步骤会根据您使用的无头 CMS 而有所不同，但以下是一些常见的步骤。

{/* md5:103ab76e22affcc8e11d48e6ee9477e2 */}
这些步骤假设您使用的无头 CMS 支持设置**自定义预览 URL**。如果不支持，您仍然可以使用此方法来保护您的预览 URL，但需要手动构建并访问预览 URL。

{/* md5:d2e81a071097556e82c3087a614d66d1 */}
**首先**，您应使用您选择的令牌生成器创建一个**秘密令牌字符串**。此秘密仅由您的 Next.js 应用和无头 CMS 知晓。此秘密可防止无权访问您的 CMS 的人访问预览 URL。

{/* md5:450c4cbbface8b61d616ec456505a686 */}
**其次**，如果您的无头 CMS 支持设置自定义预览 URL，请将以下内容指定为预览 URL。此假设您的预览 API 路由位于 `pages/api/preview.js`。

```bash filename="Terminal"
https://<your-site>/api/preview?secret=<token>&slug=<path>
```

{/* md5:fd56211740c3eb17be0e8fbd5453e967 */}
- `<your-site>` 应为您的部署域名。
- `<token>` 应替换为您生成的秘密令牌。
- `<path>` 应为要预览的页面的路径。如果要预览 `/posts/foo`，则应使用 `&slug=/posts/foo`。

{/* md5:6c9e3748dd2789017a9a4ce74be8e5cb */}
您的无头 CMS 可能允许您在预览 URL 中包含变量，以便 `<path>` 可以根据 CMS 的数据动态设置，例如：`&slug=/posts/{entry.fields.slug}`

{/* md5:7adda9ecf9c3017e6ceff45e51509ca1 */}
**最后**，在预览 API 路由中：

{/* md5:b3e5267d6ad513b1eb44fa3ca9503d41 */}
- 检查秘密是否匹配以及 `slug` 参数是否存在（如果不存在，请求应失败）。
- 调用 `res.setPreviewData`。
- 然后将浏览器重定向到由 `slug` 指定的路径。（以下示例使用 [307 重定向](https://developer.mozilla.org/docs/Web/HTTP/Status/307)）。

```js
export default async (req, res) => {
  // Check the secret and next parameters
  // This secret should only be known to this API route and the CMS
  if (req.query.secret !== 'MY_SECRET_TOKEN' || !req.query.slug) {
    return res.status(401).json({ message: 'Invalid token' })
  }

  // Fetch the headless CMS to check if the provided `slug` exists
  // getPostBySlug would implement the required fetching logic to the headless CMS
  const post = await getPostBySlug(req.query.slug)

  // If the slug doesn't exist prevent preview mode from being enabled
  if (!post) {
    return res.status(401).json({ message: 'Invalid slug' })
  }

  // Enable Preview Mode by setting the cookies
  res.setPreviewData({})

  // Redirect to the path from the fetched post
  // We don't redirect to req.query.slug as that might lead to open redirect vulnerabilities
  res.redirect(post.slug)
}
```

{/* md5:65c43ccb2d8910ae268cf78fdce982d4 */}
如果成功，浏览器将被重定向到您想要预览的路径，并设置预览模式 cookies。

{/* md5:047b638d5103f03e4854777ad1dbb042 */}
## 第二步：更新 `getStaticProps`

{/* md5:1d5fabbd65ac2d8c0bd62f38c6c24bed */}
下一步是更新 `getStaticProps` 以支持预览模式。

{/* md5:57cb40edda0c63eaee49902a53048485 */}
如果您请求一个具有 `getStaticProps` 的页面，并且设置了预览模式 cookies（通过 `res.setPreviewData`），则 `getStaticProps` 将在**请求时**（而非构建时）被调用。

{/* md5:9afeaadf24a643f1fbc8e55b3110c61c */}
此外，它将使用一个 `context` 对象调用，其中：

{/* md5:c4383e090f510f91ab34b1a5d81f6fc6 */}
- `context.preview` 将为 `true`。
- `context.previewData` 将与 `setPreviewData` 使用的参数相同。

```js
export async function getStaticProps(context) {
  // If you request this page with the preview mode cookies set:
  //
  // - context.preview will be true
  // - context.previewData will be the same as
  //   the argument used for `setPreviewData`.
}
```

{/* md5:a61cc12b8dc527083f47d4049f4f334d */}
我们在预览 API 路由中使用了 `res.setPreviewData({})`，因此 `context.previewData` 将为 `{}`。您可以使用它将会话信息从预览 API 路由传递到 `getStaticProps`（如有必要）。

{/* md5:c7e63c1f68146b8620ea8bf7238aa6de */}
如果您还使用 `getStaticPaths`，则 `context.params` 也将可用。

{/* md5:b95fc04a8fd8d8b3a81b0c5bb7a5e701 */}
### 获取预览数据

{/* md5:1d04ab11087e2dac2f84cdad0f0f5fc7 */}
您可以更新 `getStaticProps` 以根据 `context.preview` 和/或 `context.previewData` 获取不同的数据。

{/* md5:954a991d909e5048888889115b4bcd17 */}
例如，您的无头 CMS 可能有一个用于草稿帖子的不同 API 端点。如果是这样，您可以使用 `context.preview` 修改 API 端点 URL，如下所示：

```js
export async function getStaticProps(context) {
  // If context.preview is true, append "/preview" to the API endpoint
  // to request draft data instead of published data. This will vary
  // based on which headless CMS you're using.
  const res = await fetch(`https://.../${context.preview ? 'preview' : ''}`)
  // ...
}
```

{/* md5:50a477ca9b4e3546e65d6c34156f2879 */}
就是这样！如果您从无头 CMS 或手动访问预览 API 路由（带有 `secret` 和 `slug`），您现在应该能够看到预览内容。如果您更新草稿而不发布，您应该能够预览草稿。

{/* md5:a664c50562a7692883ccd02e865a5329 */}
将此设置为无头 CMS 上的预览 URL 或手动访问，您应该能够看到预览。

```bash filename="Terminal"
https://<your-site>/api/preview?secret=<token>&slug=<path>
```

{/* md5:23bb3c7081fd6e014d15fe6321edd97d */}
## 更多详情

{/* md5:6015e01d5eec3ae920600a7a4ddb2793 */}
> **须知**：在渲染期间，`next/router` 会公开一个 `isPreview` 标志，有关更多信息，请参阅 [路由器对象文档](/docs/pages/api-reference/functions/use-router#router-object)。

{/* md5:d2d2d70285fe0be4cf453c3ef050453d */}
### 指定预览模式的持续时间

{/* md5:254b8630149c20ca21a822c1ca039f10 */}
`setPreviewData` 接受一个可选的第二个参数，该参数应为一个选项对象。它接受以下键：

{/* md5:75ac6a0d8787c98369e5fb3c34bb1679 */}
- `maxAge`：指定预览会话持续的秒数。
- `path`：指定应应用 cookie 的路径。默认为 `/`，为所有路径启用预览模式。

```js
setPreviewData(data, {
  maxAge: 60 * 60, // The preview mode cookies expire in 1 hour
  path: '/about', // The preview mode cookies apply to paths with /about
})
```

{/* md5:0c543ae0050f26a448e290c71cea39e1 */}
### 清除预览模式 cookies

{/* md5:d870873b8b166ad7ee6e3504e726b198 */}
默认情况下，预览模式 cookies 未设置过期日期，因此预览会话在浏览器关闭时结束。

{/* md5:ad801b3ae71023cfd8ddc663082dda8e */}
要手动清除预览模式 cookies，请创建一个调用 `clearPreviewData()` 的 API 路由：

```js filename="pages/api/clear-preview-mode-cookies.js"
export default function handler(req, res) {
  res.clearPreviewData({})
}
```

{/* md5:91faff8db593d1bac8e6c543d7e5d84c */}
然后，向 `/api/clear-preview-mode-cookies` 发送请求以调用 API 路由。如果使用 [`next/link`](/docs/pages/api-reference/components/link) 调用此路由，则必须传递 `prefetch={false}` 以防止在链接预取期间调用 `clearPreviewData`。

{/* md5:2c7f3abcbc3143cf2a93805a61f7dfc1 */}
如果在 `setPreviewData` 调用中指定了路径，则必须将相同的路径传递给 `clearPreviewData`：

```js filename="pages/api/clear-preview-mode-cookies.js"
export default function handler(req, res) {
  const { path } = req.query

  res.clearPreviewData({ path })
}
```

{/* md5:c9b8a23ab074471e063e4dc900dd6061 */}
### `previewData` 大小限制

{/* md5:55cd5b0a94d6a8284a990f718d98692c */}
您可以传递一个对象给 `setPreviewData`，并在 `getStaticProps` 中使用它。但是，由于数据将存储在 cookie 中，因此存在大小限制。目前，预览数据限制为 2KB。

{/* md5:2a963ad9477c85313a9bc7234120e889 */}
### 与 `getServerSideProps` 一起使用

{/* md5:aa61b5624ac8d3de1b679644232fcc55 */}
预览模式也适用于 `getServerSideProps`。它也将出现在包含 `preview` 和 `previewData` 的 `context` 对象上。

{/* md5:73bcfb48c3feb9d7e3f3adbe771e301e */}
> **须知**：使用预览模式时不应设置 `Cache-Control` 标头，因为它无法被绕过。相反，我们建议使用 [增量静态再生 (ISR)](/docs/pages/guides/incremental-static-regeneration)。

{/* md5:091cab05d5f1a6dfbde8d5face32acff */}
### 与 API 路由一起使用

{/* md5:c061ebcfa21dc99b0c4f5b5959fa87ff */}
API 路由将在请求对象下访问 `preview` 和 `previewData`。例如：

```js
export default function myApiRoute(req, res) {
  const isPreview = req.preview
  const previewData = req.previewData
  // ...
}
```

{/* md5:dd9f4a814e2b9f17e6f72208faab35a1 */}
### 每个 `next build` 唯一

{/* md5:2527a42a2f09ad8408df8b163751e5e0 */}
绕过 cookie 值和用于加密 `previewData` 的私钥在完成 `next build` 时会更改。这确保无法猜测绕过 cookie。

{/* md5:f8d30cddc9f2638078c6c0b1d56da7a6 */}
> **须知**：要通过 HTTP 在本地测试预览模式，您的浏览器需要允许第三方 cookies 和本地存储访问。
