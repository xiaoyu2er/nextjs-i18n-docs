---
title: 自动静态优化
description: Next.js 会在可能的情况下自动将您的应用优化为静态 HTML。在此了解其工作原理。
---

Next.js 会在没有阻塞性数据需求时自动确定页面为静态（可预渲染）。这种判定基于页面中缺失 `getServerSideProps` 和 `getInitialProps`。

此功能允许 Next.js 生成混合应用，包含 **既支持服务端渲染 (SSR) 又支持静态生成的页面**。

> **小贴士**：静态生成的页面仍然是响应式的。Next.js 将在客户端对您应用进行水合，以提供完整的交互性。

此功能的主要好处之一是，优化后的页面无需服务器端计算，并且可以从多个 CDN 位置即时流式传输给最终用户。结果是用户将获得 `_ultra fast_` 的加载体验。

## 工作原理

如果页面中包含 `getServerSideProps` 或 `getInitialProps`，Next.js 将切换为按需、每次请求渲染页面（即 [`服务端渲染 (SSR)`](/docs/pages/building-your-application/rendering/server-side-rendering)）。

如果上述情况不成立，Next.js 会通过将页面预渲染为静态 HTML 来自动 **静态优化** 您的页面。

在预渲染期间，路由器的 `query` 对象将为空，因为此阶段我们没有 `query` 信息可供使用。水合后，Next.js 将触发应用更新，以便在 `query` 对象中提供路由参数。

水合后查询将被更新并触发另一次渲染的情况包括：

- 该页面是 [`动态路由`](/docs/pages/building-your-application/routing/dynamic-routes)。
- 页面 URL 中包含查询值。
- 您的 `next.config.js` 中配置了 [`重写`](/docs/pages/api-reference/config/next-config-js/rewrites)，因为这些规则可能包含需要在 `query` 中解析和提供的参数。

为了能够区分查询是否已完全更新并可供使用，您可以利用 [`next/router`](/docs/pages/api-reference/functions/use-router#router-object) 上的 `isReady` 字段。

> **小贴士**：在使用 [`getStaticProps`](/docs/pages/building-your-application/data-fetching/get-static-props) 的页面中，通过 [`动态路由`](/docs/pages/building-your-application/routing/dynamic-routes) 添加的参数将始终在 `query` 对象中可用。

`next build` 将为静态优化的页面发出 `.html` 文件。例如，对于 `pages/about.js` 页面的结果是：

```bash filename="Terminal"
.next/server/pages/about.html
```

And if you add `getServerSideProps` to the page, it will then be JavaScript, like so:

```bash filename="Terminal"
.next/server/pages/about.js
```

## 注意事项

- 如果您有一个带有 `getInitialProps` 的 [`自定义 App`](/docs/pages/building-your-application/routing/custom-app)，那么在未使用 [`静态生成`](/docs/pages/building-your-application/data-fetching/get-static-props) 的页面中，此优化将被关闭。
- 如果您有一个带有 `getInitialProps` 的 [`自定义 Document`](/docs/pages/building-your-application/routing/custom-document)，请务必在假设页面为服务端渲染之前检查 `ctx.req` 是否已定义。对于预渲染的页面，`ctx.req` 将为 `undefined`。
- 在路由器的 `isReady` 字段为 `true` 之前，避免在渲染树中使用 [`next/router`](/docs/pages/api-reference/functions/use-router#router-object) 的 `asPath` 值。静态优化的页面仅在客户端知道 `asPath`，而在服务器端不知道，因此将其作为 prop 使用可能导致不匹配错误。[`active-class-name 示例`](https://github.com/vercel/next.js/tree/canary/examples/active-class-name) 展示了一种将 `asPath` 作为 prop 使用的方法。
- Avoid using the `asPath` value on [`next/router`](/docs/pages/api-reference/functions/use-router#router-object) in the rendering tree until the router's `isReady` field is `true`. Statically optimized pages only know `asPath` on the client and not the server, so using it as a prop may lead to mismatch errors. The [`available-as-prop` example](https://github.com/vercel/next.js/tree/canary/examples/available-as-prop) demonstrates one way to use `asPath`.
- Avoid using the `asPath` value on [`next/router`](/docs/pages/api-reference/functions/use-router#router-object) in the rendering tree until the router's `isReady` field is `true`. Statically optimized pages only know `asPath` on the client and not the server, so using it as a prop may lead to mismatch errors. The [`active-class-name example`](https://github.com/vercel/next.js/tree/canary/examples/active-class-name) demonstrates one way to use `asPath` as a prop.
- [`Active-class-name example`](https://github.com/vercel/next.js/tree/canary/examples/active-class-name) demonstrates one way to use `asPath` as a prop.
- [`Available-as-prop example`](https://github.com/vercel/next.js/tree/canary/examples/available-as-prop) demonstrates one way to use `asPath`.
- [`Next.js Active-class-name Example`](https://github.com/vercel/next.js/tree/canary/examples/active-class-name) demonstrates one way to use `asPath` as a prop.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`Next Router`](/docs/pages/api-reference/functions/use-router#router-object) includes an `isReady` field that indicates if the router is ready.
- [`......Thinking Process:
