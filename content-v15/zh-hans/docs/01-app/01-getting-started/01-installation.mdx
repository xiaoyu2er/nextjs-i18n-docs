---
title: 安装
description: 了解如何使用 `create-next-app` CLI 创建一个新的 Next.js 应用程序，并设置 TypeScript、ESLint 和模块路径别名。
---

{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

<AppOnly>

创建一个新的 Next.js 应用并在本地运行。

## 快速开始

1.  创建一个名为 `my-app` 的新 Next.js 应用
2.  `cd my-app` 并启动开发服务器。
3.  访问 `http://localhost:3000`。

```bash package="pnpm"
pnpm create next-app@latest my-app --yes
cd my-app
pnpm dev
```

```bash package="npm"
npx create-next-app@latest my-app --yes
cd my-app
npm run dev
```

```bash package="yarn"
yarn create next-app@latest my-app --yes
cd my-app
yarn dev
```

```bash package="bun"
bun create next-app@latest my-app --yes
cd my-app
bun dev
```

- `--yes` 跳过使用已保存偏好或默认值的提示。默认设置启用 TypeScript、Tailwind、App Router 和 Turbopack，导入别名为 `@/*`。

</AppOnly>

## 系统要求

在开始之前，请确保您的系统满足以下要求：

- [Node.js 18.18](https://nodejs.org/) 或更高版本。
- macOS、Windows（包括 WSL）或 Linux。

## 使用 CLI 创建

创建新 Next.js 应用最快的方式是使用 [`create-next-app`](/docs/app/api-reference/cli/create-next-app)，它会自动为您设置好一切。要创建项目，请运行：

```bash filename="Terminal"
npx create-next-app@latest
```

安装时，您将看到以下提示：

```txt filename="Terminal"
What is your project named? my-app
Would you like to use TypeScript? No / Yes
Would you like to use ESLint? No / Yes
Would you like to use Tailwind CSS? No / Yes
Would you like your code inside a `src/` directory? No / Yes
Would you like to use App Router? (recommended) No / Yes
Would you like to use Turbopack? (recommended) No / Yes
Would you like to customize the import alias (`@/*` by default)? No / Yes
What import alias would you like configured? @/*
```

提示完成后，[`create-next-app`](/docs/app/api-reference/cli/create-next-app) 将创建一个以您的项目名称命名的文件夹并安装所需的依赖项。

## 手动安装

要手动创建新的 Next.js 应用，请安装所需的包：

```bash package="pnpm"
pnpm i next@latest react@latest react-dom@latest
```

```bash package="npm"
npm i next@latest react@latest react-dom@latest
```

```bash package="yarn"
yarn add next@latest react@latest react-dom@latest
```

```bash package="bun"
bun add next@latest react@latest react-dom@latest
```

> **了解更多信息**：App Router 内置使用 [React canary releases](https://react.dev/blog/2023/05/03/react-canaries)，其中包括所有稳定的 React 19 更新，以及框架正在验证的新功能。Pages Router 使用您在 `package.json` 中安装的 React 版本。

然后，将以下脚本添加到您的 `package.json` 文件中：

```json filename="package.json"
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  }
}
```

这些脚本对应于开发应用程序的不同阶段：

- `next dev --turbopack`：使用 Turbopack 启动开发服务器。
- `next build`：构建用于生产的应用程序。
- `next start`：启动生产服务器。
- `eslint`：运行 ESLint。

Turbopack 在 `dev` 中已稳定。在构建生产版本时，Turbopack 处于 beta 阶段。要尝试它，请运行 `next build --turbopack`。有关状态和注意事项，请参阅 [Turbopack 文档](/docs/app/api-reference/turbopack)。

<AppOnly>

### 创建 `app` 目录

Next.js 使用文件系统路由，这意味着您应用程序中的路由由文件结构决定。

创建一个 `app` 文件夹。然后，在 `app` 内部，创建一个 `layout.tsx` 文件。此文件是[根布局](/docs/app/api-reference/file-conventions/layout#root-layout)。它是必需的，必须包含 `<html>` 和 `<body>` 标签。

```tsx filename="app/layout.tsx" switcher
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

创建一个包含一些初始内容的 `app/page.tsx` 首页：

```tsx filename="app/page.tsx" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

```jsx filename="app/page.js" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

当用户访问您应用程序的根路径 (`/`) 时，`layout.tsx` 和 `page.tsx` 都将被渲染。

<Image
  alt="应用文件夹结构"
  srcLight="/docs/light/app-getting-started.png"
  srcDark="/docs/dark/app-getting-started.png"
  width="1600"
  height="363"
/>

> **需要注意的是**：
>
> - 如果您忘记创建根布局，Next.js 会在使用 `next dev` 运行开发服务器时自动创建此文件。
> - 您可以选择在项目根目录使用 [`src` 文件夹](/docs/app/api-reference/file-conventions/src-folder)，以便将应用程序代码与配置文件分开。

</AppOnly>

<PagesOnly>

### 创建 `pages` 目录

Next.js 使用文件系统路由，这意味着您应用程序中的路由由文件结构决定。

在项目的根目录创建一个 `pages` 目录。然后，在 `pages` 文件夹中添加一个 `index.tsx` 文件。这将是您的首页 (`/`)：

```tsx filename="pages/index.tsx" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

```jsx filename="pages/index.js" switcher
export default function Page() {
  return <h1>Hello, Next.js!</h1>
}
```

接下来，在 `pages/` 内添加一个 `_app.tsx` 文件来定义全局布局。了解更多关于[自定义 App 文件](/docs/pages/building-your-application/routing/custom-app)的信息。

```tsx filename="pages/_app.tsx" switcher
import type { AppProps } from 'next/app'

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />
}
```

```jsx filename="pages/_app.js" switcher
export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}
```

最后，在 `pages/` 内添加一个 `_document.tsx` 文件来控制服务器的初始响应。了解更多关于[自定义 Document 文件](/docs/pages/building-your-application/routing/custom-document)的信息。

```tsx filename="pages/_document.tsx" switcher
import { Html, Head, Main, NextScript } from 'next/document'

export default function Document() {
  return (
    <Html>
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}
```

```jsx filename="pages/_document.js" switcher
import { Html, Head, Main, NextScript } from 'next/document'

export default function Document() {
  return (
    <Html>
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}
```

</PagesOnly>

### 创建 `public` 文件夹（可选）

在项目根目录创建一个 [`public` 文件夹](/docs/app/api-reference/file-conventions/public-folder)来存储静态资源，如图像、字体等。然后，`public` 内的文件就可以通过您的代码从基础 URL (`/`) 开始引用。

然后，您可以使用根路径 (`/`) 来引用这些资源。例如，`public/profile.png` 可以被引用为 `/profile.png`：

```tsx filename="app/page.tsx" highlight={4} switcher
import Image from 'next/image'

export default function Page() {
  return <Image src="/profile.png" alt="Profile" width={100} height={100} />
}
```

```jsx filename="app/page.js" highlight={4} switcher
import Image from 'next/image'

export default function Page() {
  return <Image src="/profile.png" alt="Profile" width={100} height={100} />
}
```

## 运行开发服务器

1.  运行 `npm run dev` 以启动开发服务器。
2.  访问 `http://localhost:3000` 查看您的应用程序。
3.  编辑 <AppOnly>`app/page.tsx`</AppOnly><PagesOnly>`pages/index.tsx`</PagesOnly> 文件并保存，即可在浏览器中查看更新后的结果。

## 配置 TypeScript

> 最低 TypeScript 版本：`v4.5.2`

Next.js 内置了 TypeScript 支持。要将 TypeScript 添加到您的项目中，请将一个文件重命名为 `.ts` / `.tsx` 并运行 `next dev`。Next.js 将自动安装必要的依赖项并添加一个包含推荐配置选项的 `tsconfig.json` 文件。

<AppOnly>

### IDE 插件

Next.js 包含一个自定义的 TypeScript 插件和类型检查器，VSCode 和其他代码编辑器可以使用它进行高级类型检查和自动补全。

您可以通过以下方式在 VS Code 中启用此插件：

1.  打开命令面板 (`Ctrl/⌘` + `Shift` + `P`)
2.  搜索 "TypeScript: Select TypeScript Version"
3.  选择 "Use Workspace Version"

<Image
  alt="TypeScript 命令面板"
  srcLight="/docs/light/typescript-command-palette.png"
  srcDark="/docs/dark/typescript-command-palette.png"
  width="1600"
  height="637"
/>

</AppOnly>

更多信息，请参阅 [TypeScript 参考](/docs/app/api-reference/config/next-config-js/typescript)页面。

## 配置 ESLint

Next.js 内置了 ESLint。当您使用 `create-next-app` 创建新项目时，它会自动安装必要的包并配置正确的设置。

要将 ESLint 手动添加到现有项目中，请将 `next lint` 作为脚本添加到 `package.json`：

```json filename="package.json"
{
  "scripts": {
    "lint": "next lint"
  }
}
```

然后，运行 `npm run lint`，您将被引导完成安装和配置过程。

```bash filename="Terminal"
npm run lint
```

您将看到如下提示：

> ? 您想如何配置 ESLint？
>
> ❯ Strict (recommended)
> Base
> Cancel

-   **Strict**：包含 Next.js 的基础 ESLint 配置以及更严格的 Core Web Vitals 规则集。这是为首次设置 ESLint 的开发人员推荐的配置。
-   **Base**：包含 Next.js 的基础 ESLint 配置。
-   **Cancel**：跳过配置。如果您计划设置自己的自定义 ESLint 配置，请选择此选项。

如果选择 `Strict` 或 `Base`，Next.js 将自动在您的应用程序中安装 `eslint` 和 `eslint-config-next` 作为依赖项，并在项目根目录下创建一个配置文件。

由 `next lint` 生成的 ESLint 配置使用较旧的 `.eslintrc.json` 格式。ESLint 同时支持 [较旧的 `.eslintrc.json` 和较新的 `eslint.config.mjs` 格式](https://eslint.org/docs/latest/use/configure/configuration-files#configuring-eslint)。

您可以手动将 `.eslintrc.json` 替换为 `eslint.config.mjs` 文件，使用我们在 [ESLint API 参考](/docs/app/api-reference/config/eslint#with-core-web-vitals) 中推荐的设置，并安装 [`@eslint/eslintrc`](https://www.npmjs.com/package/@eslint/eslintrc) 包。这更接近于 `create-next-app` 使用的 ESLint 设置。

现在，每当您想运行 ESLint 来捕获错误时，都可以运行 `next lint`。一旦 ESLint 设置完成，它也会在每次构建 (`next build`) 期间自动运行。错误将导致构建失败，而警告则不会。

更多信息，请参阅 [ESLint 插件](/docs/app/api-reference/config/next-config-js/eslint)页面。

## 配置绝对导入和模块路径别名

Next.js 内置了对 `tsconfig.json` 和 `jsconfig.json` 文件中 `"paths"` 和 `"baseUrl"` 选项的支持。

这些选项允许您将项目目录别名为绝对路径，从而使导入模块更加容易和清晰。例如：

```jsx
// Before
import { Button } from '../../../components/button'

// After
import { Button } from '@/components/button'
```

要配置绝对导入，请将 `baseUrl` 配置选项添加到您的 `tsconfig.json` 或 `jsconfig.json` 文件中。例如：

```json filename="tsconfig.json or jsconfig.json"
{
  "compilerOptions": {
    "baseUrl": "src/"
  }
}
```

除了配置 `baseUrl` 路径之外，您还可以使用 `"paths"` 选项来设置模块路径的 `"别名"`。

例如，以下配置将 `@/components/*` 映射到 `components/*`：

```json filename="tsconfig.json or jsconfig.json"
{
  "compilerOptions": {
    "baseUrl": "src/",
    "paths": {
      "@/styles/*": ["styles/*"],
      "@/components/*": ["components/*"]
    }
  }
}
```

每个 `"paths"` 都是相对于 `baseUrl` 位置的。