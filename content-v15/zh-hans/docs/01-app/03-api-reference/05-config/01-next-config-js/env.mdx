---
title: 环境变量 (env)
description: 学习在构建时添加和访问 Next.js 应用中的环境变量。
version: 旧版 (legacy)
---

{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

<AppOnly>

> 随着 [Next.js 9.4](https://nextjs.org/blog/next-9-4) 的发布，我们现在拥有更直观、更便捷的体验来 [添加环境变量](/docs/app/guides/environment-variables)。试试吧！

</AppOnly>

<PagesOnly>

> 随着 [Next.js 9.4](https://nextjs.org/blog/next-9-4) 的发布，我们现在拥有更直观、更便捷的体验来 [添加环境变量](/docs/pages/guides/environment-variables)。试试吧！

</PagesOnly>

<AppOnly>

> **小知识**：以这种方式指定的环境变量 **始终** 会被包含在 JavaScript bundle 中，给环境变量名称加上 `NEXT_PUBLIC_` 前缀仅在通过 [环境或 .env 文件](/docs/app/guides/environment-variables) 指定时才生效。

</AppOnly>

<PagesOnly>

> **小知识**：以这种方式指定的环境变量 **始终** 会被包含在 JavaScript bundle 中，给环境变量名称加上 `NEXT_PUBLIC_` 前缀仅在通过 [环境或 .env 文件](/docs/pages/guides/environment-variables) 指定时才生效。

</PagesOnly>

要将环境变量添加到 JavaScript bundle 中，请打开 `next.config.js` 并添加 `env` 配置：

```js filename="next.config.js"
module.exports = {
  env: {
    customKey: 'my-value',
  },
}
```

现在您可以在代码中访问 `process.env.customKey`。例如：

```jsx
function Page() {
  return <h1>The value of customKey is: {process.env.customKey}</h1>
}

export default Page
```

Next.js 将在构建时将 `process.env.customKey` 替换为 `'my-value'`。由于 webpack [DefinePlugin](https://webpack.js.org/plugins/define-plugin/) 的特性，尝试解构 `process.env` 变量将无法生效。

例如，以下行：

```jsx
return <h1>The value of customKey is: {process.env.customKey}</h1>
```

最终将会变成：

```jsx
return <h1>The value of customKey is: {'my-value'}</h1>
```
