---
title: 环境变量 (env)
description: 学习如何在构建时向您的 Next.js 应用程序添加和访问环境变量。
version: 旧版

{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

<AppOnly>

> 自 [Next.js 9.4](https://nextjs.org/blog/next-9-4) 发布以来，我们现在拥有了更直观、更易用的 [添加环境变量](/docs/app/guides/environment-variables) 体验。去试试吧！

</AppOnly>

<PagesOnly>

> 自 [Next.js 9.4](https://nextjs.org/blog/next-9-4) 发布以来，我们现在拥有了更直观、更易用的 [添加环境变量](/docs/pages/guides/environment-variables) 体验。去试试吧！

</PagesOnly>

<AppOnly>

> **小贴士**：以此方式指定的环境变量**始终**会被包含在 JavaScript 打包文件中，仅为环境变量名称添加 `NEXT_PUBLIC_` 前缀在通过 [环境或 .env 文件](/docs/app/guides/environment-variables) 指定时才有效。

</AppOnly>

<PagesOnly>

> **小贴士**：以此方式指定的环境变量**始终**会被包含在 JavaScript 打包文件中，仅为环境变量名称添加 `NEXT_PUBLIC_` 前缀在通过 [环境或 .env 文件](/docs/pages/guides/environment-variables) 指定时才有效。

</PagesOnly>

要向 JavaScript 打包文件中添加环境变量，请打开 next.config.js 并添加 env 配置：

```js filename="next.config.js"
module.exports = {
  env: {
    customKey: 'my-value',
  },
}
```

现在您可以在代码中访问 `process.env.customKey`。例如：

```jsx
function Page() {
  return <h1>The value of customKey is: {process.env.customKey}</h1>
}

export default Page
```

Next.js 会在构建时将 `process.env.customKey` 替换为 `'my-value'`。由于 webpack [DefinePlugin](https://webpack.js.org/plugins/define-plugin/) 的特性，尝试解构 `process.env` 变量将不起作用。

例如，以下行：

```jsx
return <h1>The value of customKey is: {process.env.customKey}</h1>
```

将会变成：

```jsx
return <h1>The value of customKey is: {'my-value'}</h1>
```
