---
title: PowerByHeader (poweredByHeader)
description: 默认情况下，Next.js 会添加 `x-powered-by` 头。在此了解如何排除它。
---

{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

默认情况下，Next.js 会添加 `x-powered-by` 头。要将其排除，请打开 `next.config.js` 并禁用 `poweredByHeader` 配置：

```js filename="next.config.js"
module.exports = {
  poweredByHeader: false,
}
```
