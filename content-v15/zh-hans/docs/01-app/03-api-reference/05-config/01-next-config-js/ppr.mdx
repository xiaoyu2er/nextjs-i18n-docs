---
title: 部分预渲染 (PPR)
description: 了解如何在 Next.js 中启用 部分预渲染 (PPR)。
version: canary
related:
  title: 了解更多关于 部分预渲染 (PPR) 的信息
  links:
    - app/getting-started/partial-prerendering
---

部分预渲染 (PPR) 允许你在同一路由中组合静态和动态组件。了解更多关于 [PPR](/docs/app/getting-started/partial-prerendering) 的信息。

## 使用 部分预渲染 (PPR)

### 渐进式采用（第 15 版本）

在 Next.js 15 中，你可以通过在 `next.config.js` 中将 [`ppr`](/docs/app/api-reference/config/next-config-js/ppr) 选项设置为 `incremental`，并在文件顶部导出 `experimental_ppr` [路由配置选项](/docs/app/api-reference/file-conventions/route-segment-config)，渐进式地在 [布局](/docs/app/api-reference/file-conventions/layout) 和 [页面](/docs/app/api-reference/file-conventions/page) 中采用 部分预渲染 (PPR)：

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    ppr: 'incremental',
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    ppr: 'incremental',
  },
}

module.exports = nextConfig
```

```tsx filename="app/page.tsx" switcher
import { Suspense } from "react"
import { StaticComponent, DynamicComponent, Fallback } from "@/app/ui"

export const experimental_ppr = true

export default function Page() {
  return {
     <>
      <StaticComponent />
      <Suspense fallback={<Fallback />}>
        <DynamicComponent />
      </Suspense>
     </>
  };
}
```

```jsx filename="app/page.js" switcher
import { Suspense } from "react"
import { StaticComponent, DynamicComponent, Fallback } from "@/app/ui"

export const experimental_ppr = true

export default function Page() {
  return {
     <>
      <StaticComponent />
      <Suspense fallback={<Fallback />}>
        <DynamicComponent />
      </Suspense>
     </>
  };
}
```

> **要知道**:
>
> - 没有 `experimental_ppr` 的路由将默认为 `false`，并且不会使用 PPR 进行预渲染。你需要为每个路由显式选择加入 PPR。
> - `experimental_ppr` 将应用于路由段的所有子项，包括嵌套的布局和页面。你不必在每个文件中添加它，只需在路由的顶层段中添加。
> - 要禁用子段的 PPR，你可以在子段中设置 `experimental_ppr` 为 `false`。

| 版本       | 变更                                          |
| ---------  | -------------------------------------------   |
| `v15.0.0` | 引入了实验性 `incremental` 值                  |
| `v14.0.0` | 引入了实验性 `ppr`                           |
