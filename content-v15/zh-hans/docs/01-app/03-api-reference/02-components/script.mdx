---
title: Script 组件
description: 使用内置的 `next/script` 组件优化 Next.js 应用中的第三方脚本。
---

{/* md5:b1b138c0eed301ff21f10e430f5d189b */}
{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

{/* md5:42fe0178473dbbe7a55fd945e770668d */}
本 API 参考将帮助您了解如何使用脚本组件可用的 [属性](#props)。有关功能和用法，请参阅 [优化脚本](/docs/app/guides/scripts) 页面。

```tsx filename="app/dashboard/page.tsx" switcher
import Script from 'next/script'

export default function Dashboard() {
  return (
    <>
      <Script src="https://example.com/script.js" />
    </>
  )
}
```

```jsx filename="app/dashboard/page.js" switcher
import Script from 'next/script'

export default function Dashboard() {
  return (
    <>
      <Script src="https://example.com/script.js" />
    </>
  )
}
```

{/* md5:7b35b1ab87d09c48f479387727f4c668 */}
## 属性

{/* md5:3856c398e142a28ca09594dbcc2e6e84 */}
以下是脚本组件可用属性的摘要：

{/* md5:afa41a42bceae622601c888a8718bf6b */}
| 属性                    | 示例                           | 类型     | 必需                              |
| ----------------------- | --------------------------------- | -------- | ------------------------------------- |
| [`src`](#src)           | `src="http://example.com/script"` | 字符串   | 除非使用内联脚本，否则必需 |
| [`strategy`](#strategy) | `strategy="lazyOnload"`           | 字符串   | -                                     |
| [`onLoad`](#onload)     | `onLoad={onLoadFunc}`             | 函数 | -                                     |
| [`onReady`](#onready)   | `onReady={onReadyFunc}`           | 函数 | -                                     |
| [`onError`](#onerror)   | `onError={onErrorFunc}`           | 函数 | -                                     |

{/* md5:98b2b7d77b2f49233181ebb9af192c73 */}
## 必需属性

{/* md5:fe7c95922c6ab8184329e77a1f615817 */}
`<Script />` 组件需要以下属性。

{/* md5:cca3a4fe7ae83f35f5f68cee83ed44c3 */}
### `src`

{/* md5:4e05e91a5791ec8f7cb06f3909e5f7eb */}
一个指定外部脚本 URL 的路径字符串。这可以是一个绝对外部 URL 或一个内部路径。除非使用内联脚本，否则 `src` 属性是必需的。

{/* md5:108103de45bb06012eab44abb605ec1d */}
## 可选属性

{/* md5:579a13bef47b8d378ffd79ae00f5a42e */}
`<Script />` 组件除了必需属性外，还接受许多其他属性。

{/* md5:ddc8d5db6c7c580ddd8949793ccf5b12 */}
### `strategy`

{/* md5:85d65e95b611dbb56d857cf9d41d6f2e */}
脚本的加载策略。可以使用四种不同的策略：

{/* md5:0677d06798f2324dc825139aefaa2b99 */}
- `beforeInteractive`：在任何 Next.js 代码和任何页面水合 (hydration) 发生之前加载。
- `afterInteractive`：(**默认**) 早期加载，但在页面发生部分水合之后。
- `lazyOnload`：在浏览器空闲时间加载。
- `worker`：(实验性) 在 Web Worker 中加载。

{/* md5:75599bb112f5ae32f5111439747f9035 */}
### `beforeInteractive`

{/* md5:17c33b5f9f2e3f3467302edd8e681f89 */}
使用 `beforeInteractive` 策略加载的脚本会从服务器注入到初始 HTML 中，在任何 Next.js 模块之前下载，并按其放置顺序执行。

{/* md5:3b4ae8e162ecec8f938e38c99f3a7bc0 */}
使用此策略标记的脚本会在任何第一方代码之前预加载和获取，但它们的执行**不会阻塞页面水合**。

<AppOnly>

{/* md5:73f887ebb0a389b2f30e4a1485100261 */}
`beforeInteractive` 脚本必须放置在根布局 (`app/layout.tsx`) 内，旨在加载整个站点所需的脚本（即当应用中的任何页面在服务器端加载时，该脚本将被加载）。

</AppOnly>

<PagesOnly>

{/* md5:567cb14cbc3beff4da2c86286e10444b */}
`beforeInteractive` 脚本必须放置在 `Document` 组件 (`pages/_document.js`) 内，旨在加载整个站点所需的脚本（即当应用中的任何页面在服务器端加载时，该脚本将被加载）。

</PagesOnly>

{/* md5:09da8380037e3282ef76a3c8f4c62624 */}
**此策略应仅用于需要尽快获取的关键脚本。**

<AppOnly>

```tsx filename="app/layout.tsx" switcher
import Script from 'next/script'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        {children}
        <Script
          src="https://example.com/script.js"
          strategy="beforeInteractive"
        />
      </body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
import Script from 'next/script'

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
        <Script
          src="https://example.com/script.js"
          strategy="beforeInteractive"
        />
      </body>
    </html>
  )
}
```

</AppOnly>

<PagesOnly>

```jsx filename="pages/_document.js"
import { Html, Head, Main, NextScript } from 'next/document'
import Script from 'next/script'

export default function Document() {
  return (
    <Html>
      <Head />
      <body>
        <Main />
        <NextScript />
        <Script
          src="https://example.com/script.js"
          strategy="beforeInteractive"
        />
      </body>
    </Html>
  )
}
```

</PagesOnly>

{/* md5:863f9249472aebba0db7b63cb29d3d09 */}
> **请注意**：无论组件放置在何处，使用 `beforeInteractive` 的脚本将始终注入到 HTML 文档的 `head` 中。

{/* md5:0d788edb7958853ea065488a1643b99c */}
应使用 `beforeInteractive` 尽快获取的一些脚本示例包括：

{/* md5:d6d1936daf8fdce5bcc8025aee60b2b1 */}
- 机器人检测器
- Cookie 同意管理器

{/* md5:b7ec43e3379c5e98d2239b740f7bdfc1 */}
### `afterInteractive`

{/* md5:7b66c42fedb147d3b9329709aea43c3d */}
使用 `afterInteractive` 策略的脚本会在客户端注入到 HTML 中，并在页面发生部分（或全部）水合之后加载。**这是脚本组件的默认策略**，应适用于任何需要尽快加载但不应在任何第一方 Next.js 代码之前加载的脚本。

{/* md5:2b77ca8f3613023d1d309e45ac04c8bb */}
`afterInteractive` 脚本可以放置在任何页面或布局内，并且仅当该页面（或页面组）在浏览器中打开时才会加载和执行。

```jsx filename="app/page.js"
import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script src="https://example.com/script.js" strategy="afterInteractive" />
    </>
  )
}
```

{/* md5:e33c39958d178ebc6c6ccdbb809b883d */}
适用于 `afterInteractive` 的一些脚本示例包括：

{/* md5:3bb285c65a315116672449136d23f5a3 */}
- 标签管理器
- 分析工具

{/* md5:d85b13c9b6275c68df8840bc2d790dbb */}
### `lazyOnload`

{/* md5:74e64cbf8f72e369c6db7b89521bde2e */}
使用 `lazyOnload` 策略的脚本会在浏览器空闲时间注入到客户端 HTML 中，并在页面上所有资源获取完毕后加载。此策略应适用于任何不需要提前加载的后台或低优先级脚本。

{/* md5:a45f0e2077891324830481dd61182450 */}
`lazyOnload` 脚本可以放置在任何页面或布局内，并且仅当该页面（或页面组）在浏览器中打开时才会加载和执行。

```jsx filename="app/page.js"
import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script src="https://example.com/script.js" strategy="lazyOnload" />
    </>
  )
}
```

{/* md5:6b72f1f40f936f0b3cd51efb6c523db5 */}
不需要立即加载并可以使用 `lazyOnload` 获取的脚本示例包括：

{/* md5:aeeff9a303714782d26b17cd924bb693 */}
- 聊天支持插件
- 社交媒体小部件

{/* md5:1714b43e21ef543565daec149fab566f */}
### `worker`

{/* md5:90b5ac03297c0b5e9a27232c797a71be */}
> **警告：** `worker` 策略尚不稳定，并且尚不能与应用路由 (App Router) 一起使用。请谨慎使用。

{/* md5:b91541e85102b895a9aff3c6beb33509 */}
使用 `worker` 策略的脚本会被卸载到 Web Worker 中，以释放主线程并确保只有关键的、第一方资源在主线程上处理。虽然此策略可用于任何脚本，但它是一个高级用例，不保证支持所有第三方脚本。

{/* md5:972bb6d8f5edc75013fdcf52cfb48614 */}
要使用 `worker` 作为策略，必须在 `next.config.js` 中启用 `nextScriptWorkers` 标志：

```js filename="next.config.js"
module.exports = {
  experimental: {
    nextScriptWorkers: true,
  },
}
```

{/* md5:6b6da9763141daa533438db803fe21d8 */}
`worker` 脚本**目前只能在 `pages/` 目录中使用**：

```tsx filename="pages/home.tsx" switcher
import Script from 'next/script'

export default function Home() {
  return (
    <>
      <Script src="https://example.com/script.js" strategy="worker" />
    </>
  )
}
```

```jsx filename="pages/home.js" switcher
import Script from 'next/script'

export default function Home() {
  return (
    <>
      <Script src="https://example.com/script.js" strategy="worker" />
    </>
  )
}
```

{/* md5:d4155f3327ccfd3024271e835df353b1 */}
### `onLoad`

{/* md5:37fb696a5f1d6b9ee99863c4b741cad2 */}
> **警告：** `onLoad` 尚不能与服务器组件 (Server Components) 一起使用，只能用于客户端组件 (Client Components)。此外，`onLoad` 不能与 `beforeInteractive` 一起使用 — 请考虑改用 `onReady`。

{/* md5:c42149629dccad81dad4c29aeceada34 */}
一些第三方脚本要求用户在脚本加载完成后运行一次 JavaScript 代码，以实例化内容或调用函数。如果您使用 `afterInteractive` 或 `lazyOnload` 作为加载策略来加载脚本，则可以使用 `onLoad` 属性在脚本加载后执行代码。

{/* md5:1980c95aac166e24a7af446de23fe2f9 */}
以下是一个仅在 lodash 库加载后才执行 lodash 方法的示例。

```tsx filename="app/page.tsx" switcher
'use client'

import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script
        src="https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.17.20/lodash.min.js"
        onLoad={() => {
          console.log(_.sample([1, 2, 3, 4]))
        }}
      />
    </>
  )
}
```

```jsx filename="app/page.js" switcher
'use client'

import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script
        src="https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.17.20/lodash.min.js"
        onLoad={() => {
          console.log(_.sample([1, 2, 3, 4]))
        }}
      />
    </>
  )
}
```

{/* md5:0bf00e01553c7eb9b327b1a98b76bd47 */}
### `onReady`

{/* md5:f1951fcffd14eb7abdb9852c519f55db */}
> **警告：** `onReady` 尚不能与服务器组件一起使用，只能用于客户端组件。

{/* md5:8629a54cf5f08bdc8cc7ceebb158a39d */}
一些第三方脚本要求用户在脚本加载完成后以及每次组件挂载时（例如，路由导航后）运行 JavaScript 代码。您可以使用 `onReady` 属性在脚本首次加载时执行其加载事件后的代码，然后在每次后续组件重新挂载后执行。

{/* md5:562efe620144e2a5c64e7e4966fad090 */}
以下是一个在每次组件挂载时重新实例化 Google Maps JS 嵌入的示例：

<AppOnly>

```tsx filename="app/page.tsx" switcher
'use client'

import { useRef } from 'react'
import Script from 'next/script'

export default function Page() {
  const mapRef = useRef()

  return (
    <>
      <div ref={mapRef}></div>
      <Script
        id="google-maps"
        src="https://maps.googleapis.com/maps/api/js"
        onReady={() => {
          new google.maps.Map(mapRef.current, {
            center: { lat: -34.397, lng: 150.644 },
            zoom: 8,
          })
        }}
      />
    </>
  )
}
```

```jsx filename="app/page.js" switcher
'use client'

import { useRef } from 'react'
import Script from 'next/script'

export default function Page() {
  const mapRef = useRef()

  return (
    <>
      <div ref={mapRef}></div>
      <Script
        id="google-maps"
        src="https://maps.googleapis.com/maps/api/js"
        onReady={() => {
          new google.maps.Map(mapRef.current, {
            center: { lat: -34.397, lng: 150.644 },
            zoom: 8,
          })
        }}
      />
    </>
  )
}
```

</AppOnly>

<PagesOnly>

```jsx
import { useRef } from 'react'
import Script from 'next/script'

export default function Page() {
  const mapRef = useRef()

  return (
    <>
      <div ref={mapRef}></div>
      <Script
        id="google-maps"
        src="https://maps.googleapis.com/maps/api/js"
        onReady={() => {
          new google.maps.Map(mapRef.current, {
            center: { lat: -34.397, lng: 150.644 },
            zoom: 8,
          })
        }}
      />
    </>
  )
}
```

</PagesOnly>

{/* md5:5fe4fd6105353c823ab35d0efe422986 */}
### `onError`

{/* md5:729701f1c90f5b4b42a976ef2ef88b8a */}
> **警告：** `onError` 尚不能与服务器组件一起使用，只能用于客户端组件。`onError` 不能与 `beforeInteractive` 加载策略一起使用。

{/* md5:5623d49f1c121b4f9dbfe7c9680a2505 */}
有时捕获脚本加载失败的情况很有帮助。这些错误可以用 `onError` 属性处理：

<AppOnly>

```tsx filename="app/page.tsx" switcher
'use client'

import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script
        src="https://example.com/script.js"
        onError={(e: Error) => {
          console.error('Script failed to load', e)
        }}
      />
    </>
  )
}
```

```jsx filename="app/page.js" switcher
'use client'

import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script
        src="https://example.com/script.js"
        onError={(e) => {
          console.error('Script failed to load', e)
        }}
      />
    </>
  )
}
```

</AppOnly>

<PagesOnly>

```jsx
import Script from 'next/script'

export default function Page() {
  return (
    <>
      <Script
        src="https://example.com/script.js"
        onError={(e: Error) => {
          console.error('Script failed to load', e)
        }}
      />
    </>
  )
}
```

</PagesOnly>

{/* md5:bb3b99be8f27f7a60348b868bf1c7f52 */}
## 版本历史

{/* md5:3875bc25dd14f953c3415751f158fcfe */}
| 版本   | 变更                                                                   |
| --------- | ------------------------------------------------------------------------- |
| `v13.0.0` | `beforeInteractive` 和 `afterInteractive` 被修改以支持 `app` 路由。  |
| `v12.2.4` | 添加了 `onReady` 属性。                                                     |
| `v12.2.2` | 允许将带有 `beforeInteractive` 的 `next/script` 放置在 `_document` 中。 |
| `v11.0.0` | 引入 `next/script`。                                                 |