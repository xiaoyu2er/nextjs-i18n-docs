---
title: template.js
description: template.js 文件的 API 参考。
---

{/* md5:d91f3b37f45be313b325e1ace29cd97f */}
**模板**文件类似于[布局](/docs/app/getting-started/layouts-and-pages#creating-a-layout)，它包裹布局或页面。与在不同路由间保持状态且持久存在的布局不同，模板会被赋予一个唯一的键 (key)，这意味着子级客户端组件 (Client Components) 会在导航时重置它们的状态。

{/* md5:b9b838ff1571d547664cfd7fda446e46 */}
当您需要以下情况时，它们非常有用：

{/* md5:5d6c82243e62f1b2bdc8ee232b4ce7d7 */}
- 在导航时重新同步 `useEffect`。
- 在导航时重置子客户端组件的状态。例如，一个输入字段。
- 改变默认的框架行为。例如，布局内的 Suspense 边界仅在首次加载时显示回退 (fallback)，而模板则在每次导航时都显示它。

{/* md5:45dcaa5a371e659f8b3d273badd01cd9 */}
## 约定

{/* md5:32c0959e52bbad82e255467918d6cf4a */}
可以通过从 `template.js` 文件导出一个默认的 React 组件来定义模板。该组件应接受一个 `children` 属性。

{/* md5:f8b16b56db66e71667c62f45fd18976b */}
<Image
  alt="template.js special file"
  srcLight="/docs/light/template-special-file.png"
  srcDark="/docs/dark/template-special-file.png"
  width="1600"
  height="444"
/>

```tsx filename="app/template.tsx" switcher
export default function Template({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>
}
```

```jsx filename="app/template.js" switcher
export default function Template({ children }) {
  return <div>{children}</div>
}
```

{/* md5:651e7d5175b65331bb50f77d8d50ea34 */}
在嵌套关系方面，`template.js` 在布局和其子内容之间渲染。以下是一个简化的输出：

```jsx filename="Output"
<Layout>
  {/* Note that the template is given a unique key. */}
  <Template key={routeParam}>{children}</Template>
</Layout>
```

{/* md5:7b35b1ab87d09c48f479387727f4c668 */}
## 属性

{/* md5:a95ecf88bf0bc4ba341b32d09864bd15 */}
### `children` (必填)

{/* md5:ce1488c670a21abad5ecbbf671c9123d */}
模板接受一个 `children` 属性。

```jsx filename="Output"
<Layout>
  {/* Note that the template is automatically given a unique key. */}
  <Template key={routeParam}>{children}</Template>
</Layout>
```

{/* md5:ac46318ed2f537f320bd9a6b764053eb */}
## 行为

{/* md5:5ab959f29318bd289fde41c38fc11412 */}
- **服务器组件 (Server Components)**：默认情况下，模板是服务器组件。
- **导航时重新挂载**：模板会自动接收一个唯一的键。导航到新路由会导致模板及其子组件重新挂载。
- **状态重置**：模板内的任何客户端组件都会在导航时重置其状态。
- **效果重新运行**：像 `useEffect` 这样的效果会随着组件重新挂载而重新同步。
- **DOM 重置**：模板内的 DOM 元素会被完全重新创建。

{/* md5:bb3b99be8f27f7a60348b868bf1c7f52 */}
## 版本历史

{/* md5:bd2a258e93e5cc8846d51d0c81a93107 */}
| 版本 | 变更 |
| ---- | ---- |
| `v13.0.0` | 引入了 `template`。 |
