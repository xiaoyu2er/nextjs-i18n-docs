---
title: mdx-components.js
description: mdx-components.js 文件的 API 参考
related:
  title: 了解更多关于 MDX 组件的信息
  links:
    - app/guides/mdx
---

{/* md5:57b752ea4d2a1891ee888ec54d539e66 */}
`mdx-components.js|tsx` 文件是**必需的**，用于在 App Router 中使用 [`@next/mdx`](/docs/app/guides/mdx)，没有它将无法工作。此外，你还可以使用它来自定义[样式](/docs/app/guides/mdx#using-custom-styles-and-components)。

{/* md5:c42d702f345e4f2514d4f84550bbefdb */}
在项目的根目录使用 `mdx-components.tsx`（或 `.js`）文件来定义 MDX 组件。例如，放在与 `pages` 或 `app` 同级的位置，如果适用的话，也可以放在 `src` 目录内。

```tsx filename="mdx-components.tsx" switcher
import type { MDXComponents } from 'mdx/types'

const components: MDXComponents = {}

export function useMDXComponents(): MDXComponents {
  return components
}
```

```js filename="mdx-components.js" switcher
const components = {}

export function useMDXComponents() {
  return components
}
```

{/* md5:30774efc95253ea1fb9a2bc2ee562db7 */}
## 导出

{/* md5:baa1927d000b0f70d31ca982b7674948 */}
### `useMDXComponents` 函数

{/* md5:4304105e03b188b5a6e143c0f8e2546e */}
该文件必须导出一个名为 `useMDXComponents` 的函数。此函数不接受任何参数。

```tsx filename="mdx-components.tsx" switcher
import type { MDXComponents } from 'mdx/types'

const components: MDXComponents = {}

export function useMDXComponents(): MDXComponents {
  return components
}
```

```js filename="mdx-components.js" switcher
const components = {}

export function useMDXComponents() {
  return components
}
```

{/* md5:bb3b99be8f27f7a60348b868bf1c7f52 */}
## 版本历史

{/* md5:d10559f20f3669ba3cc16c48b04349d2 */}
| 版本      | 更改               |
| --------- | ------------------ |
| `v13.1.2` | 添加了 MDX 组件功能 |