---
title: page.js
description: page.js 文件的 API 参考。
---

{/* md5:89dabc0565aab40cc5208bd433cbba00 */}
`page` 文件允许您定义**唯一**属于某个路由的 UI。您可以通过从该文件默认导出一个组件来创建页面：

```tsx filename="app/blog/[slug]/page.tsx" switcher
export default function Page({
  params,
  searchParams,
}: {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  return <h1>My Page</h1>
}
```

```jsx filename="app/blog/[slug]/page.js" switcher
export default function Page({ params, searchParams }) {
  return <h1>My Page</h1>
}
```

{/* md5:243c968de1479c7b08ade714c7c04c91 */}
## 小知识

{/* md5:eee56b749cb48385cb31b2127d8724c0 */}
- `.js`、`.jsx` 或 `.tsx` 文件扩展名可用于 `page`。
- `page` 始终是路由子树的**叶子节点**。
- 要使一个路由段**可公开访问**，必须存在 `page` 文件。
- 默认情况下，页面是[服务器组件 (Server Components)](https://react.dev/reference/rsc/server-components)，但也可以设置为[客户端组件 (Client Component)](https://react.dev/reference/rsc/use-client)。

{/* md5:c3399052227a4ce4d5bc2084381aac1f */}
## 参考

{/* md5:bf4247ced2b0ccd1db4173d3ea35981d */}
## 属性

{/* md5:737d845e6561ff914b923bb7f98a23d8 */}
#### `params`（可选）

{/* md5:ad2d0e1f5e7e2533484c860c4dee9023 */}
一个解析后包含从根段到该页面的[动态路由参数 (dynamic route parameters)](/docs/app/api-reference/file-conventions/dynamic-routes) 对象的 Promise。

```tsx filename="app/shop/[slug]/page.tsx" switcher
export default async function Page({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
}
```

```jsx filename="app/shop/[slug]/page.js" switcher
export default async function Page({ params }) {
  const { slug } = await params
}
```

{/* md5:3c7af3dde79061e20ba4deb62a48f1e8 */}
| 示例路由                               | URL         | `params`                                  |
| -------------------------------------- | ----------- | ----------------------------------------- |
| `app/shop/[slug]/page.js`              | `/shop/1`   | `Promise<{ slug: '1' }>`                  |
| `app/shop/[category]/[item]/page.js`   | `/shop/1/2` | `Promise<{ category: '1', item: '2' }>`   |
| `app/shop/[...slug]/page.js`           | `/shop/1/2` | `Promise<{ slug: ['1', '2'] }>`           |

{/* md5:c5973f7f310e5d24ba15c858ede62c4f */}
- 由于 `params` 属性是一个 Promise，您必须使用 `async/await` 或 React 的 [`use`](https://react.dev/reference/react/use) 函数来访问其值。
  - 在版本 14 及更早版本中，`params` 是一个同步属性。为了向后兼容，在 Next.js 15 中您仍然可以同步访问它，但此行为在未来将被弃用。

{/* md5:bee117e04aa0db27f29bd2beb4dceab1 */}
#### `searchParams`（可选）

{/* md5:bb50946841d2fc0ac6d76555831f898f */}
一个解析后包含当前 URL 的[搜索参数 (search parameters)](https://developer.mozilla.org/docs/Learn/Common_questions/What_is_a_URL#parameters) 对象的 Promise。例如：

```tsx filename="app/shop/page.tsx" switcher
export default async function Page({
  searchParams,
}: {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const filters = (await searchParams).filters
}
```

```jsx filename="app/shop/page.js" switcher
export default async function Page({ searchParams }) {
  const filters = (await searchParams).filters
}
```

{/* md5:35e19dbbdf51a9593cc0a39a38afc571 */}
客户端组件的 **页面** 也可以使用 React 的 [`use`](https://react.dev/reference/react/use) Hook 来访问 `searchParams`：

```tsx filename="app/shop/page.tsx" switcher
'use client'
import { use } from 'react'

export default function Page({
  searchParams,
}: {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const filters = use(searchParams).filters
}
```

```jsx filename="app/page.jsx" switcher
'use client'
import { use } from 'react'

export default function Page({ searchParams }) {
  const filters = use(searchParams).filters
}
```

{/* md5:0e89117035dcc8e890e9d9da4b1f190e */}
| 示例 URL       | `searchParams`                  |
| -------------- | ------------------------------- |
| `/shop?a=1`    | `Promise<{ a: '1' }>`           |
| `/shop?a=1&b=2`| `Promise<{ a: '1', b: '2' }>`   |
| `/shop?a=1&a=2`| `Promise<{ a: ['1', '2'] }>`    |

{/* md5:dc313b01c23f50bc249d1cb98fe1fdb7 */}
- 由于 `searchParams` 属性是一个 Promise，您必须使用 `async/await` 或 React 的 [`use`](https://react.dev/reference/react/use) 函数来访问其值。
  - 在版本 14 及更早版本中，`searchParams` 是一个同步属性。为了向后兼容，您仍然可以在 Next.js 15 中同步访问它，但此行为将在未来被弃用。
- `searchParams` 是一个 **[动态 API (Dynamic API)](/docs/app/getting-started/partial-prerendering#dynamic-rendering)**，其值无法预先知晓。使用它会使页面在请求时进入**[动态渲染 (dynamic rendering)](/docs/app/getting-started/partial-prerendering#dynamic-rendering)** 模式。
- `searchParams` 是一个普通 JavaScript 对象，而不是 `URLSearchParams` 实例。

{/* md5:e7d3d9c3dee270021da3367e192dac6e */}
### Page Props 辅助函数

{/* md5:de3941d54ad7a1615a5000f7bc41e78d */}
您可以使用 `PageProps` 来键入页面，从而从路由字面量获取强类型的 `params` 和 `searchParams`。`PageProps` 是一个全局可用的辅助函数。

```tsx filename="app/blog/[slug]/page.tsx" switcher
export default async function Page(props: PageProps<'/blog/[slug]'>) {
  const { slug } = await props.params
  const query = await props.searchParams
  return <h1>Blog Post: {slug}</h1>
}
```

{/* md5:b70183cf931ecd8ba98c3e56f0302d8c */}
> **提示**
>
> - 使用字面量路由（例如 `'/blog/[slug]'`）可以为 `params` 启用自动补全和严格的键名检查。
> - 静态路由会将 `params` 解析为 `{}`。
> - 类型在 `next dev`、`next build` 或 `next typegen` 期间生成。
> - 类型生成后，`PageProps` 辅助函数将全局可用。无需导入。

{/* md5:e57da1656ead1413f55464d7577686d2 */}
## 示例

{/* md5:379eb0ffb7c4e596ed0072c40d28dfb0 */}
### 根据 `params` 显示内容

{/* md5:8dff922a413da417edfeb7c7c8331a7e */}
使用[动态路由段 (dynamic route segments)](/docs/app/api-reference/file-conventions/dynamic-routes)，您可以根据 `params` 属性为页面显示或获取特定内容。

```tsx filename="app/blog/[slug]/page.tsx" switcher
export default async function Page({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  return <h1>Blog Post: {slug}</h1>
}
```

```jsx filename="app/blog/[slug]/page.js" switcher
export default async function Page({ params }) {
  const { slug } = await params
  return <h1>Blog Post: {slug}</h1>
}
```

{/* md5:1c3f095216cd8e718e33add1bedbdf23 */}
### 使用 `searchParams` 处理过滤

{/* md5:eb143f69c3a497db53a04a5e14dd04b7 */}
您可以使用 `searchParams` 属性来根据 URL 的查询字符串处理过滤、分页或排序。

```tsx filename="app/shop/page.tsx" switcher
export default async function Page({
  searchParams,
}: {
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const { page = '1', sort = 'asc', query = '' } = await searchParams

  return (
    <div>
      <h1>Product Listing</h1>
      <p>Search query: {query}</p>
      <p>Current page: {page}</p>
      <p>Sort order: {sort}</p>
    </div>
  )
}
```

```jsx filename="app/shop/page.js" switcher
export default async function Page({ searchParams }) {
  const { page = '1', sort = 'asc', query = '' } = await searchParams

  return (
    <div>
      <h1>Product Listing</h1>
      <p>Search query: {query}</p>
      <p>Current page: {page}</p>
      <p>Sort order: {sort}</p>
    </div>
  )
}
```

{/* md5:027111ca96cf77ffedb5227b36cf574d */}
### 在客户端组件中读取 `searchParams` 和 `params`

{/* md5:5d61caad53dcbd8304e7385a44ea7696 */}
要在客户端组件（不能是 `async` 的）中使用 `searchParams` 和 `params`，您可以使用 React 的 [`use`](https://react.dev/reference/react/use) 函数来读取该 Promise：

```tsx filename="app/page.tsx" switcher
'use client'

import { use } from 'react'

export default function Page({
  params,
  searchParams,
}: {
  params: Promise<{ slug: string }>
  searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
  const { slug } = use(params)
  const { query } = use(searchParams)
}
```

```js filename="app/page.js" switcher
'use client'

import { use } from 'react'

export default function Page({ params, searchParams }) {
  const { slug } = use(params)
  const { query } = use(searchParams)
}
```

{/* md5:bb3b99be8f27f7a60348b868bf1c7f52 */}
## 版本历史

{/* md5:6dbea5abd97f407de09492495b83c901 */}
| 版本          | 更改                                                                                                                |
| ------------- | ------------------------------------------------------------------------------------------------------------------- |
| `v15.0.0-RC` | `params` 和 `searchParams` 现在是 Promise。提供了 [codemod](/docs/app/guides/upgrading/codemods#150)。               |
| `v13.0.0`    | 引入了 `page`。                                                                                                     |
