---
title: next CLI
description: 了解如何使用 Next.js CLI 运行和构建应用程序。
---

{/* md5:b1b138c0eed301ff21f10e430f5d189b */}
{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

{/* md5:0176bd2414e09e27db1debe73e5c5fdd */}
Next.js CLI 允许您开发、构建、启动应用程序等。

{/* md5:ffd610bf3bc0b18ce3a7eb7606809d07 */}
基本用法：

```bash filename="Terminal"
npx next [command] [options]
```

{/* md5:c3399052227a4ce4d5bc2084381aac1f */}
## 参考

{/* md5:d2a8deaee8c2a1602c84d39373374c09 */}
以下是可用的选项：

{/* md5:c68116bbbac903eeb2091e789300a532 */}
| 选项              | 描述                 |
| ----------------- | -------------------- |
| `-h` 或 `--help`  | 显示所有可用选项     |
| `-v` 或 `--version` | 输出 Next.js 版本号 |

{/* md5:64a334f0341efa55ca3d6147b108cbdd */}
### 命令
{/* md5:d159a5fa759d8df0200272f0469723bf */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:9da02556d94f7ce6d471852b5edc319f */}
以下是可用的命令：

{/* md5:76784779e8ce3ed6396995f418746780 */}
| 命令                                | 描述                                                                                                                                                                                                                        |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`dev`](#next-dev-options)             | 以开发模式启动 Next.js，包含热模块重载、错误报告等。                                                                                                                                           |
| [`build`](#next-build-options)         | 创建应用程序的优化生产版本。显示每个路由的信息。                                                                                                                                |
| [`start`](#next-start-options)         | 以生产模式启动 Next.js。应先通过 `next build` 编译应用程序。                                                                                                                                     |
| [`info`](#next-info-options)           | 打印有关当前系统的相关信息，可用于报告 Next.js 错误。                                                                                                                                         |
| [`lint`](#next-lint-options)           | 在 `/src`、`/app`、`/pages`、`/components` 和 `/lib` 目录中的所有文件上运行 ESLint。如果应用程序尚未配置 ESLint，还会提供引导式设置以安装任何所需的依赖项。 |
| [`telemetry`](#next-telemetry-options) | 允许启用或禁用 Next.js 的完全匿名遥测数据收集。                                                                                                                                                |
| [`typegen`](#next-typegen-options)     | 为路由、页面、布局和路由处理程序生成 TypeScript 定义，无需执行完整构建。                                                                                                                      |
{/* md5:085c88b7fafc969715f547c84ea7bbe8 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:12e05f02aa671e2765df69dd2f346e27 */}
> **须知**：运行不带命令的 `next` 是 `next dev` 的别名。

{/* md5:2284eb3e8b5d66fb92e056c7d566aadb */}
### `next dev` 选项
{/* md5:afd08967591a8ff467e3e3a291eada38 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:da4de0971952af2aa4378510ac7dc1e8 */}
`next dev` 以开发模式启动应用程序，包含热模块替换 (HMR)、错误报告等功能。运行 `next dev` 时，以下选项可用：

{/* md5:21a2ac21564ba66b6a964c1628781859 */}
| 选项                                   | 描述                                                                                                                                          |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-h, --help`                             | 显示所有可用选项。                                                                                                                          |
| `[directory]`                            | 构建应用程序的目录。如果未提供，使用当前目录。                                                           |
| `--turbopack`                            | 使用 [Turbopack](/docs/app/api-reference/turbopack) 启动开发模式。也可表示为 `--turbo`。                                           |
| `-p` 或 `--port <port>`                  | 指定启动应用程序的端口号。默认：3000，env: PORT                                                                    |
| `-H`或 `--hostname <hostname>`           | 指定启动应用程序的主机名。有助于使应用程序可被网络上的其他设备访问。默认：0.0.0.0 |
| `--experimental-https`                   | 使用 HTTPS 启动服务器并生成自签名证书。                                                                                |
| `--experimental-https-key <path>`        | HTTPS 密钥文件的路径。                                                                                                                            |
| `--experimental-https-cert <path>`       | HTTPS 证书文件的路径。                                                                                                                    |
| `--experimental-https-ca <path>`         | HTTPS 证书颁发机构文件的路径。                                                                                                          |
| `--experimental-upload-trace <traceUrl>` | 将调试追踪的子集报告到远程 HTTP URL。                                                                                        |
{/* md5:e171cfe8822d9581275fcc92a2ac0bd2 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:677a9c632ed6b3a57aad3f1ba62b8726 */}
### `next build` 选项
{/* md5:e6569cdbc7db946e4d4b8f61f8c5e808 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:f413761b70c8b8591414be8786e9e8af */}
`next build` 创建应用程序的优化生产版本。输出显示每个路由的信息。例如：

```bash filename="Terminal"
Route (app)                              Size     First Load JS
┌ ○ /_not-found                          0 B               0 kB
└ ƒ /products/[id]                       0 B               0 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand
```

{/* md5:89a4d75bbd8a76fa8ce7840874d90059 */}
- **Size (大小)**：客户端导航到页面时下载的资源大小。每个路由的大小仅包括其依赖项。
- **First Load JS (首次加载 JS)**：从服务器访问页面时下载的资源大小。所有页面共享的 JS 量显示为单独的指标。

{/* md5:2e4ec18d153ce42254285b91798418aa */}
这两个值都已[**使用 gzip 压缩**](/docs/app/api-reference/config/next-config-js/compress)。首次加载大小以绿色、黄色或红色表示。对于高性能的应用程序，应以绿色为目标。

{/* md5:9ecf3430a608692e1b6f720c76dc2f5b */}
以下选项可用于 `next build` 命令：

{/* md5:b19d28adc0e5b8e01049b31131e84d82 */}
| 选项                             | 描述                                                                                                                                                                        |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-h, --help`                       | 显示所有可用选项。                                                                                                                                                        |
| `[directory]`                      | 构建应用程序的目录。如果未提供，使用当前目录。                                                                                |
| `--turbopack`                      | 使用 [Turbopack](/docs/app/api-reference/turbopack) 构建（Beta 版）。也可表示为 `--turbo`。                                                                                    |
| `-d` 或 `--debug`                  | 启用更详细的构建输出。启用此标志后，将显示额外的构建输出，如重写、重定向和头信息。                                           |
|                                    |
| `--profile`                        | 启用 React 的 [生产环境分析](https://react.dev/reference/react/Profiler)。                                                                                              |
| `--no-lint`                        | 禁用代码检查。_注意：代码检查功能将在 Next 16 中从 `next build` 中移除。如果您使用的是 Next 15.5+ 且使用了 `eslint` 之外的 linter，构建期间将不会进行代码检查。_ |
| `--no-mangling`                    | 禁用 [名称修饰](https://en.wikipedia.org/wiki/Name_mangling)。这可能会影响性能，仅应用于调试目的。                                      |
| `--experimental-app-only`          | 仅构建 App Router 路由。                                                                                                                                                     |
| `--experimental-build-mode [mode]` | 使用实验性构建模式。（选项："compile"、"generate"，默认："default"）                                                                                              |
| `--debug-prerender`                | 调试开发模式下的预渲染错误。                                                                                                                                             |
{/* md5:4a91463695cd6c9c3510a6fa0bce07c0 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:20eba71da07805cc4623b2a5d8469526 */}
### `next start` 选项
{/* md5:153c346d9ee0ae5bd4b344d97a222e67 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:f7e89046c9a9735bed505b993402f0fc */}
`next start` 以生产模式启动应用程序。应用程序应先通过 [`next build`](#next-build-options) 编译。

{/* md5:149fbcf6cf854054022821eba57ea30e */}
以下选项可用于 `next start` 命令：

{/* md5:5325fbc88fe844494f391d5a8b9c9795 */}
| 选项                                  | 描述                                                                                     |
| ------------------------------------- | ---------------------------------------------------------------------------------------- |
| `-h` 或 `--help`                        | 显示所有可用选项。                                                                       |
| `[directory]`                           | 用于启动应用程序的目录。如果未提供目录，将使用当前目录。                                 |
| `-p` 或 `--port <port>`                 | 指定启动应用程序的端口号。（默认：3000，环境变量：PORT）                                  |
| `-H` 或 `--hostname <hostname>`         | 指定启动应用程序的主机名。（默认：0.0.0.0）                                              |
| `--keepAliveTimeout <keepAliveTimeout>` | 指定关闭非活动连接前等待的最大毫秒数。                                                   |

{/* md5:3e94ce88b330829e1cf17dfb702e495a */}
### `next info` 选项
{/* md5:db70f565772bf592c34be006f50a17b6 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:83cdb7b9815e4240d2a9b4cb970e9378 */}
`next info` 打印有关当前系统的相关详细信息，在创建 [GitHub issue](https://github.com/vercel/next.js/issues) 报告 Next.js 错误时可以使用。此信息包括操作系统平台/架构/版本、二进制文件（Node.js、npm、Yarn、pnpm）、包版本（`next`、`react`、`react-dom`）等。

{/* md5:3be0139541e4cfa2fb2d0c446eb28c50 */}
输出应如下所示：

```bash filename="Terminal"
Operating System:
  Platform: darwin
  Arch: arm64
  Version: Darwin Kernel Version 23.6.0
  Available memory (MB): 65536
  Available CPU cores: 10
Binaries:
  Node: 20.12.0
  npm: 10.5.0
  Yarn: 1.22.19
  pnpm: 9.6.0
Relevant Packages:
  next: 15.0.0-canary.115 // Latest available version is detected (15.0.0-canary.115).
  eslint-config-next: 14.2.5
  react: 19.0.0-rc
  react-dom: 19.0.0
  typescript: 5.5.4
Next.js Config:
  output: N/A
```

{/* md5:6c52d310e63e6638156391f756147218 */}
以下选项可用于 `next info` 命令：

{/* md5:5cfe4c6df3c3a81ad10a8d228546a81b */}
| 选项           | 描述                     |
| -------------- | ------------------------ |
| `-h` 或 `--help` | 显示所有可用选项         |
| `--verbose`      | 收集用于调试的额外信息。 |

{/* md5:72144bce99fa3ac72fadd92eb5ef919e */}
### `next lint` 选项
{/* md5:ef81a0fb912378d1e8930f2533499eaf */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:1547238b6fa758f944c326fa6195dd8d */}
> **警告**：此选项已弃用，将在 Next 16 中移除。有一个 [codemod](/blog/next-15-5#next-lint-deprecation) 可用于迁移到 ESLint CLI。
{/* md5:a9ed07e48bc7f904414e31c665b7b7d3 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:3931f70fd546db73d97580cf2d38b200 */}
`next lint` 为 `pages/`、`app/`、`components/`、`lib/` 和 `src/` 目录中的所有文件运行 ESLint。如果您的应用程序中尚未配置 ESLint，它还会提供引导式设置来安装任何所需的依赖项。

{/* md5:c42965f32900f1551fd2bd9642a6d294 */}
以下选项可用于 `next lint` 命令：

{/* md5:a57460de23ad74043b77e0ecaf93426c */}
| 选项                                                | 描述                                                                                           |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `[directory]`                                         | 用于检查应用程序代码的基础目录。如果未提供，将使用当前目录。                                   |
| `-d, --dir, <dirs...>`                                | 包含要运行 ESLint 的目录。                                                                     |
| `--file, <files...>`                                  | 包含要运行 ESLint 的文件。                                                                     |
| `--ext, [exts...]`                                    | 指定 JavaScript 文件扩展名。（默认：[".js", ".mjs", ".cjs", ".jsx", ".ts", ".mts", ".cts", ".tsx"]) |
| `-c, --config, <config>`                              | 使用此配置文件，覆盖所有其他配置选项。                                                         |
| `--resolve-plugins-relative-to, <rprt>`               | 指定解析插件的目录。                                                                           |
| `--strict`                                            | 使用 Next.js 严格配置创建 `.eslintrc.json` 文件。                                              |
| `--rulesdir, <rulesdir...>`                           | 使用来自此目录的附加规则。                                                                     |
| `--fix`                                               | 自动修复代码检查问题。                                                                         |
| `--fix-type <fixType>`                                | 指定要应用的修复类型（例如，问题、建议、布局）。                                               |
| `--ignore-path <path>`                                | 指定要忽略的文件。                                                                             |
| `--no-ignore <path>`                                  | 禁用 `--ignore-path` 选项。                                                                    |
| `--quiet`                                             | 仅报告错误。                                                                                   |
| `--max-warnings [maxWarnings]`                        | 指定在触发非零退出代码之前的警告数量。（默认：-1）                                             |
| `-o, --output-file, <outputFile>`                     | 指定将报告写入的文件。                                                                         |
| `-f, --format, <format>`                              | 使用特定的输出格式。                                                                           |
| `--no-inline-config`                                  | 防止注释更改配置或规则。                                                                       |
| `--report-unused-disable-directives-severity <level>` | 为未使用的 eslint-disable 指令指定严重级别。（选项："error"、"off"、"warn"）                   |
| `--no-cache`                                          | 禁用缓存。                                                                                     |
| `--cache-location, <cacheLocation>`                   | 指定缓存的位置。                                                                               |
| `--cache-strategy, [cacheStrategy]`                   | 指定用于检测缓存中已更改文件的策略。（默认："metadata"）                                       |
| `--error-on-unmatched-pattern`                        | 当任何文件模式不匹配时报告错误。                                                               |
| `-h, --help`                                          | 显示此消息。                                                                                   |

{/* md5:4b7e7103529f6f0f8bcd998b022098c5 */}
### `next telemetry` 选项
{/* md5:38d9b4d282080bbbeed36ab50d2cb768 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:e57da1656ead1413f55464d7577686d2 */}
Next.js 收集关于一般用法的**完全匿名**的遥测数据。参与此匿名程序是可选的，如果您不想分享信息，可以选择退出。

{/* md5:9fab0c049a1e1832c59d3bab4aebdc1c */}
以下选项可用于 `next telemetry` 命令：

{/* md5:433c6fe3dd77094bac9cdc215ccd59c8 */}
| 选项       | 描述                         |
| ---------- | ---------------------------- |
| `-h, --help` | 显示所有可用选项。           |
| `--enable`   | 启用 Next.js 的遥测数据收集。  |
| `--disable`  | 禁用 Next.js 的遥测数据收集。 |

{/* md5:9d162f2682cd9c170fb5ef7b3c96e461 */}
了解更多关于[遥测](/telemetry)的信息。

{/* md5:797498a37ec130795e7d74c3fd2b79d9 */}
### `next typegen` 选项
{/* md5:765a17aecee976eccf0bd0836f0f9883 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:a34ba8fa3832cb0c90ec34e1efb39501 */}
`next typegen` 为应用程序的路由生成 TypeScript 定义，无需执行完整构建。这对于 IDE 自动补全和 CI 路由类型检查非常有用。

{/* md5:004a17c333340bde0ac9681983b376a0 */}
以前，路由类型只在 `next dev` 或 `next build` 期间生成，这意味着直接运行 `tsc --noEmit` 无法验证您的路由类型。现在，您可以独立生成类型并在外部进行验证：

```bash filename="Terminal"
# Generate route types first, then validate with TypeScript
next typegen && tsc --noEmit

# Or in CI workflows for type checking without building
next typegen && npm run type-check
```

{/* md5:377e36d49f6c31199a69198672ea70c7 */}
以下选项可用于 `next typegen` 命令：

{/* md5:d9a5e2f9d46e43ddc290b93a03530d5d */}
| 选项        | 描述                                                                                  |
| ------------- | -------------------------------------------------------------------------------------------- |
| `-h, --help`  | 显示所有可用选项。                                                                  |
| `[directory]` | 生成类型的目录。如果未提供，则使用当前目录。 |

{/* md5:feae8699814225ad89e9e168944ee220 */}
输出文件将写入 `<distDir>/types`（默认值：`.next/types`）：
{/* md5:e55899c2de1dc75d4b9da17266d70350 */}
<!-- /NEEDS_TRANSLATION -->

```bash filename="Terminal"
next typegen
# or for a specific app
next typegen ./apps/web
```

{/* md5:ce994e0331addf7d8698033e9bfd4059 */}
> **须知**：`next typegen` 使用生产构建阶段加载您的 Next.js 配置（`next.config.js`、`next.config.mjs` 或 `next.config.ts`）。请确保任何所需的环境变量和依赖项可用，以便配置能够正确加载。

{/* md5:bb7e5bc9fd90a312fa24a085660ded46 */}
## 示例

{/* md5:b86ebd5c499f246d1d55bf4ed19d22bc */}
### 调试预渲染错误
{/* md5:2c867825ee3259de844d5178b5ba39d5 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:7dbfed1d36404299de234876e189d058 */}
如果您在 `next build` 期间遇到预渲染错误，可以传递 `--debug-prerender` 标志以获取更详细的输出：

```bash filename="Terminal"
next build --debug-prerender
```

{/* md5:466ed7d14d9a3bf9863e3787051942ac */}
这启用了一些实验性选项以使调试更容易：

{/* md5:372c9af50184345ed7d67f1a6a36ed08 */}
- 禁用服务器代码混淆：
  - `experimental.serverMinification = false`
  - `experimental.turbopackMinify = false`
- 生成服务器打包的源地图：
  - `experimental.serverSourceMaps = true`
- 启用为预渲染所用子进程中的源地图消耗：
  - `experimental.enablePrerenderSourceMaps = true`
- 即使在第一次预渲染错误后也继续构建，以便您可以一次性查看所有问题：
  - `experimental.prerenderEarlyExit = false`
{/* md5:52b4da8669f74cb57b4096325f797d14 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:dfdc0f2f445147ddd70d2a9dc309489d */}
这有助于在构建输出中显示更易读的堆栈跟踪和代码帧。

{/* md5:041fb456a0922bd8a4e1543eee19029b */}
> **警告**：`--debug-prerender` 仅用于开发调试。请勿将使用 `--debug-prerender` 生成的构建部署到生产环境，因为它可能会影响性能。

{/* md5:5e9ba2957c7d0769a70df3a110d4e646 */}
### 更改默认端口
{/* md5:fc2d9740eac2ba806a82490f3bc42eb2 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:cf04f6211c49b94c3be6ed3ccb745cf2 */}
默认情况下，Next.js 在开发期间和使用 `next start` 时使用 `http://localhost:3000`。可以使用 `-p` 选项更改默认端口，如下所示：

```bash filename="Terminal"
next dev -p 4000
```

{/* md5:0ebc313bbed68a646b3e89654d4d817c */}
或使用 `PORT` 环境变量：

```bash filename="Terminal"
PORT=4000 next dev
```

{/* md5:de339f69dd161f1cf4f584dc6cb1157c */}
> **须知**：`PORT` 不能在 `.env` 中设置，因为启动 HTTP 服务器发生在任何其他代码初始化之前。

{/* md5:ea4ba23cca3d02ed4465a592cf59ba27 */}
### 开发期间使用 HTTPS
{/* md5:fc2d9740eac2ba806a82490f3bc42eb2 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:edf005215ede65cd693067b9e9ed6dca */}
对于某些用例，如 Webhook 或身份验证，您可以使用 [HTTPS](https://developer.mozilla.org/en-US/docs/Glossary/HTTPS) 在 `localhost` 上拥有安全环境。Next.js 可以使用 `--experimental-https` 标志通过 `next dev` 生成自签名证书：

```bash filename="Terminal"
next dev --experimental-https
```

{/* md5:0ce44c1605372689bb672ecb8bb1364f */}
使用生成的证书，Next.js 开发服务器将存在于 `https://localhost:3000`。除非通过 `-p`、`--port` 或 `PORT` 指定端口，否则使用默认端口 `3000`。

{/* md5:0cf1d305a0498e0852f4e7147c272900 */}
您还可以使用 `--experimental-https-key` 和 `--experimental-https-cert` 提供自定义证书和密钥。可选地，您也可以使用 `--experimental-https-ca` 提供自定义 CA 证书。

```bash filename="Terminal"
next dev --experimental-https --experimental-https-key ./certificates/localhost-key.pem --experimental-https-cert ./certificates/localhost.pem
```

{/* md5:51e836dcb14d7880a458964e4485fce7 */}
`next dev --experimental-https` 仅适用于开发，并使用 [`mkcert`](https://github.com/FiloSottile/mkcert) 创建本地受信任的证书。在生产环境中，请使用来自受信任颁发机构的正确颁发的证书。

{/* md5:677db2924759d390244204acb6a18720 */}
### 配置下游代理的超时
{/* md5:fc2d9740eac2ba806a82490f3bc42eb2 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:f8e3c3c174e5a5d074ea586caa5b4537 */}
当在下游代理（例如，像 AWS ELB/ALB 这样的负载均衡器）后面部署 Next.js 时，重要的是使用比下游代理的超时时间**更长**的 [keep-alive 超时](https://nodejs.org/api/http.html#http_server_keepalivetimeout) 来配置 Next 的底层 HTTP 服务器。否则，一旦给定的 TCP 连接达到 keep-alive 超时，Node.js 将立即终止该连接而不通知下游代理。这会导致代理在尝试重用 Node.js 已终止的连接时出现错误。

{/* md5:23e54ab374524965bfc5f543860fe1f0 */}
要为生产 Next.js 服务器配置超时值，请将 `--keepAliveTimeout`（以毫秒为单位）传递给 `next start`，如下所示：

```bash filename="Terminal"
next start --keepAliveTimeout 70000
```

{/* md5:cbdde12649e095c5982c14a7451829cf */}
### 传递 Node.js 参数
{/* md5:fc2d9740eac2ba806a82490f3bc42eb2 */}
<!-- /NEEDS_TRANSLATION -->

{/* md5:571201e74284b2a8afc93df1adf012e6 */}
您可以将任何 [node 参数](https://nodejs.org/api/cli.html#cli_node_options_options) 传递给 `next` 命令。例如：

```bash filename="Terminal"
NODE_OPTIONS='--throw-deprecation' next
NODE_OPTIONS='-r esm' next
NODE_OPTIONS='--inspect' next
```

{/* md5:ba243181ebb259977c809efd3efd4482 */}
| 版本   | 更改                                                                         |
| --------- | ------------------------------------------------------------------------------- |
| `v15.5.0` | 添加 `next typegen` 命令                                                  |
| `v15.4.0` | 为 `next build` 添加 `--debug-prerender` 选项以帮助调试预渲染错误。 |
{/* md5:fc2d9740eac2ba806a82490f3bc42eb2 */}
<!-- /NEEDS_TRANSLATION -->