---
title: 创建 Next.js 应用 (create-next-app)
description: 使用一条命令通过 create-next-app CLI 创建 Next.js 应用。
---

{/* md5:b1b138c0eed301ff21f10e430f5d189b */}
{/* 本文档的内容在应用路由和页面路由之间共享。您可以使用 `<PagesOnly>Content</PagesOnly>` 组件来添加仅适用于页面路由的内容。任何共享内容不应包裹在组件中。 */}

{/* md5:3f22c01afbf3236bb09d49af23a2fc3b */}
`create-next-app` 命令行工具允许您使用默认模板或来自公共 GitHub 仓库的[示例](https://github.com/vercel/next.js/tree/canary/examples)来创建一个新的 Next.js 应用。这是开始使用 Next.js 最简单的方式。

{/* md5:ffd610bf3bc0b18ce3a7eb7606809d07 */}
基本用法：

```bash filename="Terminal"
npx create-next-app@latest [project-name] [options]
```

{/* md5:c3399052227a4ce4d5bc2084381aac1f */}
## 参考

{/* md5:d2a8deaee8c2a1602c84d39373374c09 */}
以下是可用的选项：

{/* md5:f0901f5481c1a4299c437db34f719cf4 */}
| Options                                 | Description                                                     |
| --------------------------------------- | --------------------------------------------------------------- |
| `-h` or `--help`                        | 显示所有可用选项                                                |
| `-v` or `--version`                     | 输出版本号                                                      |
| `--no-*`                                | 否定默认选项。例如 `--no-ts`                                    |
| `--ts` or `--typescript`                | 初始化为 TypeScript 项目（默认）                                |
| `--js` or `--javascript`                | 初始化为 JavaScript 项目                                        |
| `--tailwind`                            | 使用 Tailwind CSS 配置初始化（默认）                            |
| `--eslint`                              | 使用 ESLint 配置初始化                                          |
| `--biome`                               | 使用 Biome 配置初始化                                           |
| `--no-linter`                           | 跳过 Linter 配置                                                |
| `--app`                                 | 初始化为 App Router 项目                                        |
| `--api`                                 | 仅使用路由处理器初始化项目                                      |
| `--src-dir`                             | 在 `src/` 目录内初始化                                          |
| `--turbopack`                           | 默认为开发启用 Turbopack                                        |
| `--import-alias <alias-to-configure>`   | 指定使用的导入别名（默认为 "@/\\*"）                            |
| `--empty`                               | 初始化为空项目                                                  |
| `--use-npm`                             | 显式指示 CLI 使用 npm 引导应用程序                              |
| `--use-pnpm`                            | 显式指示 CLI 使用 pnpm 引导应用程序                             |
| `--use-yarn`                            | 显式指示 CLI 使用 Yarn 引导应用程序                             |
| `--use-bun`                             | 显式指示 CLI 使用 Bun 引导应用程序                              |
| `-e` or `--example [name] [github-url]` | 用于引导应用程序的示例                                          |
| `--example-path <path-to-example>`      | 分别指定示例路径                                                |
| `--reset-preferences`                   | 显式指示 CLI 重置任何存储的首选项                               |
| `--skip-install`                        | 显式指示 CLI 跳过安装包                                         |
| `--disable-git`                         | 显式指示 CLI 禁用 git 初始化                                    |
| `--yes`                                 | 对所有选项使用之前的首选项或默认值                              |

{/* md5:e57da1656ead1413f55464d7577686d2 */}
## 示例

{/* md5:916041518c058a1df290c6cd3ff5f121 */}
### 使用默认模板

{/* md5:e7f174832c2b6f51319fe1fa19b4227a */}
要使用默认模板创建新应用，请在终端中运行以下命令：

```bash filename="Terminal"
npx create-next-app@latest
```

{/* md5:316f6a67f56988465d9ab3bd75417c0b */}
然后您将被询问以下提示：

```txt filename="Terminal"
What is your project named?  my-app
Would you like to use TypeScript?  No / Yes
Which linter would you like to use?  ESLint / Biome / None
Would you like to use Tailwind CSS?  No / Yes
Would you like your code inside a `src/` directory?  No / Yes
Would you like to use App Router? (recommended)  No / Yes
Would you like to use Turbopack? (recommended)  No / Yes
Would you like to customize the import alias (`@/*` by default)?  No / Yes
```

{/* md5:3d57c8acd3da70a57e92eb8a894a4190 */}
### Linter 选项

{/* md5:e9415a38976b89a40035449b3a3fa808 */}
**ESLint**：传统且最受欢迎的 JavaScript Linter。包含来自 eslint-plugin-next 的 Next.js 特定规则。

{/* md5:fccff20a93531a6e6af5ad6c5dd01a39 */}
**Biome**：一个快速、现代的 linter 和格式化工具，结合了 ESLint 和 Prettier 的功能。包含内置的 Next.js 和 React 领域支持以实现最佳性能。

{/* md5:2231b02d2cd67223ad01207374fe01e1 */}
**None**：完全跳过 linter 配置。您可以随时稍后添加 linter。

{/* md5:3f0683ff9b83fdc4829013498e7164aa */}
> **注意**：`next lint` 命令将在 Next.js 16 中被弃用。新项目应直接使用所选的 Linter（例如 `biome check` 或 `eslint`）。

{/* md5:b008c1abdbfb28899e908a1f8d7fca41 */}
一旦您回答完提示，一个使用您所选配置的新项目就会被创建。

{/* md5:98386673dc1d000fcded8959d7242408 */}
### 使用官方 Next.js 示例

{/* md5:783d0140f22b0a6511170d5735b22390 */}
要使用官方 Next.js 示例创建新应用，请使用 `--example` 标志。例如：

```bash filename="Terminal"
npx create-next-app@latest --example [example-name] [your-project-name]
```

{/* md5:1db8b57e6f0c20a6e00140ba4675d6f8 */}
您可以在 [Next.js 仓库](https://github.com/vercel/next.js/tree/canary/examples)中查看所有可用示例及其设置说明。

{/* md5:a12844b566e2a0bb75dd3662e950aae5 */}
### 使用任意公共 GitHub 示例

{/* md5:c501d0967397c56e50adea13dfe6c9eb */}
要使用任何公共 GitHub 示例创建新应用，请使用 `--example` 选项并提供 GitHub 仓库的 URL。例如：

```bash filename="Terminal"
npx create-next-app@latest --example "https://github.com/.../" [your-project-name]
```