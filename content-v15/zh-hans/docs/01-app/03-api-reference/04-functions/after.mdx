---
title: after
description: after 函数的 API 参考。
---

`after` 允许您安排在响应（或预渲染）完成后执行的工作。这对于不应阻塞响应的任务和其他副作用（例如日志记录和分析）非常有用。

它可用于 [服务端组件 (Server Components)](/docs/app/getting-started/server-and-client-components)（包括 [`generateMetadata`](/docs/app/api-reference/functions/generate-metadata)）、[服务器操作 (Server Actions)](/docs/app/getting-started/updating-data)、[路由处理程序 (Route Handlers)](/docs/app/api-reference/file-conventions/route) 以及 [中间件 (Middleware)](/docs/app/api-reference/file-conventions/middleware)。

该函数接受一个回调函数，该函数将在响应（或预渲染）完成后执行：

```tsx filename="app/layout.tsx" switcher
import { after } from 'next/server'
// Custom logging function
import { log } from '@/app/utils'

export default function Layout({ children }: { children: React.ReactNode }) {
  after(() => {
    // Execute after the layout is rendered and sent to the user
    log()
  })
  return <>{children}</>
}
```

```jsx filename="app/layout.jsx" switcher
import { after } from 'next/server'
// Custom logging function
import { log } from '@/app/utils'

export default function Layout({ children }) {
  after(() => {
    // Execute after the layout is rendered and sent to the user
    log()
  })
  return <>{children}</>
}
```

> **请注意：** `after` 不是一个[动态 API](/docs/app/getting-started/partial-prerendering#dynamic-rendering)，调用它不会导致路由变为动态。如果在静态页面中使用，该回调将在构建时或页面重新验证时执行。

## 参考

### 参数

- 一个将在响应（或预渲染）完成后执行的回调函数。

### 持续时间

`after` 将运行平台默认或您为路由配置的最长时间。如果您的平台支持，您可以使用 [`maxDuration`](/docs/app/api-reference/file-conventions/route-segment-config#maxduration) 路由段配置来设置超时限制。

## 小知识

- 即使响应没有成功完成，`after` 也会被执行。包括抛出错误或调用 `notFound` 或 `redirect` 的情况。
- 您可以使用 React `cache` 来对 `after` 内部调用的函数进行去重。
- `after` 可以嵌套在其他 `after` 调用中，例如，您可以创建包装 `after` 调用的工具函数以添加额外功能。

## 示例

### 使用请求 API

您可以在 [服务器操作 (Server Actions)](/docs/app/getting-started/updating-data) 和 [路由处理程序 (Route Handlers)](/docs/app/api-reference/file-conventions/route) 中的 `after` 回调里使用请求 API，例如 [`cookies`](/docs/app/api-reference/functions/cookies) 和 [`headers`](/docs/app/api-reference/functions/headers)。这在记录数据变更 (mutation) 后的活动方面非常有用。例如:

```ts filename="app/api/route.ts" highlight={2,7-9} switcher
import { after } from 'next/server'
import { cookies, headers } from 'next/headers'
import { logUserAction } from '@/app/utils'

export async function POST(request: Request) {
  // Perform mutation
  // ...

  // Log user activity for analytics
  after(async () => {
    const userAgent = (await headers().get('user-agent')) || 'unknown'
    const sessionCookie =
      (await cookies().get('session-id'))?.value || 'anonymous'

    logUserAction({ sessionCookie, userAgent })
  })

  return new Response(JSON.stringify({ status: 'success' }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  })
}
```

```js filename="app/api/route.js" highlight={2,7-9} switcher
import { after } from 'next/server'
import { cookies, headers } from 'next/headers'
import { logUserAction } from '@/app/utils'

export async function POST(request) {
  // Perform mutation
  // ...

  // Log user activity for analytics
  after(async () => {
    const userAgent = (await headers().get('user-agent')) || 'unknown'
    const sessionCookie =
      (await cookies().get('session-id'))?.value || 'anonymous'

    logUserAction({ sessionCookie, userAgent })
  })

  return new Response(JSON.stringify({ status: 'success' }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  })
}
```

但是，您不能在[服务器组件](/docs/app/getting-started/server-and-client-components)中的 `after` 内部使用这些请求 API。这是因为 Next.js 需要知道树的哪一部分访问了请求 API 以支持[部分预渲染](/docs/app/getting-started/partial-prerendering)，而 `after` 是在 React 渲染生命周期之后运行的。

## 平台支持

请参阅[自托管指南](/docs/app/guides/self-hosting#streaming-and-suspense)获取详细的配置说明。

了解在自托管 Next.js 时如何[配置 `after`](/docs/app/guides/self-hosting#after)。

<details id="after-serverless">

  <summary>参考：支持 serverless 平台的 `after`</summary>

在 serverless 上下文中使用 `after` 需要在响应发送后等待异步任务完成。在 Next.js 和 Vercel 中，这是通过使用一个名为 `waitUntil(promise)` 的原语来实现的，该原语会延长 serverless 调用的生命周期，直到传递给 [`waitUntil`](https://vercel.com/docs/functions/functions-api-reference#waituntil) 的所有 Promise 都已处理完毕。

如果您希望用户能够运行 `after`，您必须提供自己实现的 `waitUntil`，其行为方式类似。

当 `after` 被调用时，Next.js 将像这样访问 `waitUntil`：

```jsx
const RequestContext = globalThis[Symbol.for('@next/request-context')]
const contextValue = RequestContext?.get()
const waitUntil = contextValue?.waitUntil
```

这意味着 `globalThis[Symbol.for('@next/request-context')]` 预计包含一个像这样的对象：

```tsx
type NextRequestContext = {
  get(): NextRequestContextValue | undefined
}

type NextRequestContextValue = {
  waitUntil?: (promise: Promise<any>) => void
}
```

下面是一个实现示例。

```tsx
import { AsyncLocalStorage } from 'node:async_hooks'

const RequestContextStorage = new AsyncLocalStorage<NextRequestContextValue>()

// Define and inject the accessor that next.js will use
const RequestContext: NextRequestContext = {
  get() {
    return RequestContextStorage.getStore()
  },
}
globalThis[Symbol.for('@next/request-context')] = RequestContext

const handler = (req, res) => {
  const contextValue = { waitUntil: YOUR_WAITUNTIL }
  // Provide the value
  return RequestContextStorage.run(contextValue, () => nextJsHandler(req, res))
}
```

</details>

## 版本历史

| 版本历史          | 描述                         |
| ----------------- | ---------------------------- |
| `v15.1.0`         | `after` 变为稳定版。         |
| `v15.0.0-rc`      | 引入了 `unstable_after`。    |