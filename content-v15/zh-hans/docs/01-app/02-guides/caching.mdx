---
title: Next.js 中的缓存
nav_title: 缓存
description: Next.js 缓存机制概述。
---

{/* md5:966b4a40efdbc163288e2acb2b5d14d6 */}
Next.js 通过缓存渲染工作和数据请求来提高应用程序的性能并降低成本。本页深入探讨了 Next.js 的缓存机制、可用于配置它们的 API 以及它们之间的交互方式。

{/* md5:7c6f2d8003e95b29f9490a74acc78e38 */}
> **有益信息**：本页旨在帮助你理解 Next.js 的内部工作原理，但并非高效使用 Next.js 的必备知识。Next.js 的大多数缓存启发式方法由你的 API 使用情况决定，并且在零配置或最小配置下具有最佳性能的默认设置。如果你更想直接跳转到示例，[请从这里开始](/docs/app/getting-started/fetching-data)。

{/* md5:c576bf2f4a77afe12b882ba265fe07c4 */}
## 概览

{/* md5:c7e76e252d9168e473ce8869b0bb0663 */}
以下是不同缓存机制及其用途的高层概览：

{/* md5:587730665849c38424ad6f97c6c11ebb */}
| 机制 | 内容 | 位置 | 目的 | 持续时间 |
| --- | --- | --- | --- | --- |
| [请求记忆化](#request-memoization) | 函数的返回值 | 服务器 | 在 React 组件树中重用数据 | 每个请求的生命周期 |
| [数据缓存](#data-cache) | 数据 | 服务器 | 跨用户请求和部署存储数据 | 持久化（可重新验证） |
| [完整路由缓存](#full-route-cache) | HTML 和 RSC 载荷 | 服务器 | 降低渲染成本并提升性能 | 持久化（可重新验证） |
| [路由缓存](#client-side-router-cache) | RSC 载荷 | 客户端 | 减少导航时的服务器请求 | 用户会话或基于时间 |

{/* md5:66234f42a7cb6ea727d6b93f6f77eacc */}
默认情况下，Next.js 会尽可能多地进行缓存以提高性能和降低成本。这意味着路由是**静态渲染**的，并且数据请求是**缓存**的，除非你选择退出。下图展示了默认的缓存行为：路由在构建时进行静态渲染，以及静态路由首次被访问时的情况。

{/* md5:15e8e7fcf39b7f50f2f252e51fe061a0 */}
<Image
  alt="展示 Next.js 四种机制默认缓存行为的示意图，显示在构建时以及路由首次访问时的 HIT、MISS 和 SET 状态。"
  srcLight="/docs/light/caching-overview.png"
  srcDark="/docs/dark/caching-overview.png"
  width="1600"
  height="1179"
/>

{/* md5:ca55f68587904a6d2d5ad36eed15813d */}
缓存行为会根据路由是静态还是动态渲染、数据是否已缓存、以及请求是初始访问还是后续导航而变化。根据你的使用场景，你可以为单个路由和数据请求配置缓存行为。

{/* md5:478bd5aaab8793f7700dbbec77300f7a */}
`middleware` 中**不支持**获取缓存。在 `middleware` 中执行的任何获取操作都不会被缓存。

{/* md5:ee075edcaa14bbef683e82768b2bae19 */}
## 请求记忆化

{/* md5:ce1a4aac1410eff73ff10c1edadd5b5c */}
Next.js 扩展了 [`fetch` API](#fetch)，自动对具有相同 URL 和选项的请求进行**记忆化**。这意味着你可以在 React 组件树的多个位置调用同一数据的 fetch 函数，而只执行一次。

{/* md5:6b56a4e601efe41a784961536f9157a2 */}
<Image
  alt="去重的 Fetch 请求"
  srcLight="/docs/light/deduplicated-fetch-requests.png"
  srcDark="/docs/dark/deduplicated-fetch-requests.png"
  width="1600"
  height="857"
/>

{/* md5:deab6280ae94decf8fec4c1f06eea31e */}
例如，如果你需要在路由中使用相同的数据（例如在布局、页面和多个组件中），你不必在组件树顶部获取数据并在组件之间传递 props。相反，你可以在需要数据的组件中获取数据，而无需担心为同一数据在网络上发出多个请求所带来的性能影响。

```tsx filename="app/example.tsx" switcher
async function getItem() {
  // The `fetch` function is automatically memoized and the result
  // is cached
  const res = await fetch('https://.../item/1')
  return res.json()
}

// This function is called twice, but only executed the first time
const item = await getItem() // cache MISS

// The second call could be anywhere in your route
const item = await getItem() // cache HIT
```

```jsx filename="app/example.js" switcher
async function getItem() {
  // The `fetch` function is automatically memoized and the result
  // is cached
  const res = await fetch('https://.../item/1')
  return res.json()
}

// This function is called twice, but only executed the first time
const item = await getItem() // cache MISS

// The second call could be anywhere in your route
const item = await getItem() // cache HIT
```

{/* md5:4540af3dc5791b575fc420a6c01409d5 */}
**请求记忆化的工作原理**

{/* md5:5f6990422a83a1f8ea252fd3c622093f */}
<Image
  alt="展示在 React 渲染期间 fetch 记忆化如何工作的示意图。"
  srcLight="/docs/light/request-memoization.png"
  srcDark="/docs/dark/request-memoization.png"
  width="1600"
  height="742"
/>

{/* md5:4370af6c01a48947dc974cf7deaeb727 */}
- 在渲染路由期间，特定请求第一次被调用时，其结果不会在内存中，这将是一个缓存**未命中 (MISS)**。
- 因此，该函数将被执行，数据将从外部源获取，并将结果存储在内存中。
- 在同一渲染流程中对该请求的后续函数调用将是缓存**命中 (HIT)**，数据将直接从内存返回，而无需执行该函数。
- 一旦路由渲染完成且渲染流程结束，内存将被"重置"，所有请求记忆化条目将被清除。

{/* md5:256e57672eab48ab4ac041763bfe082a */}
> **有益信息**：
>
> - 请求记忆化是 React 的特性，而非 Next.js 的特性。将其包含在此处是为了展示它与其他缓存机制的交互。
> - 记忆化仅适用于 `fetch` 请求中的 `GET` 方法。
> - 记忆化仅适用于 React 组件树，这意味着：
>   - 它适用于 `generateMetadata`、`generateStaticParams`、布局、页面和其他服务器组件中的 `fetch` 请求。
>   - 它不适用于路由处理程序中的 `fetch` 请求，因为它们不属于 React 组件树。
> - 对于不适合使用 `fetch` 的情况（例如某些数据库客户端、CMS 客户端或 GraphQL 客户端），你可以使用 [React `cache` 函数](#react-cache-function) 来记忆化函数。

{/* md5:29738bc4bd7bf0e0954f80bdb004b9e6 */}
### 持续时间

{/* md5:aa7d7f63ba81cc54c8cdeb4b2063ea36 */}
缓存的生命周期与服务器请求的生命周期相同，直到 React 组件树完成渲染为止。

{/* md5:6a05f230f7a107777ef724f3663a6332 */}
### 重新验证

{/* md5:17f18cc1c6cc8b4ed61cf908d5a18d7d */}
由于记忆化不会在服务器请求之间共享，并且仅在渲染期间应用，因此不需要对其进行重新验证。

{/* md5:e823ae07709e9d1b27c1eb26087cd8a4 */}
### 选择退出

{/* md5:8e484bf1ff3021b7da0a60c7393c1fae */}
记忆化仅适用于 `fetch` 请求中的 `GET` 方法，其他方法（如 `POST` 和 `DELETE`）不会被记忆化。此默认行为是 React 的一种优化，我们不建议你选择退出。

{/* md5:fc104fd58ca9ff52f3bcdde9b13511a8 */}
要管理单个请求，你可以使用 [`AbortController`](https://developer.mozilla.org/en-US/docs/Web/API/AbortController) 的 [`signal`](https://developer.mozilla.org/en-US/docs/Web/API/AbortController/signal) 属性。

```js filename="app/example.js"
const { signal } = new AbortController()
fetch(url, { signal })
```

{/* md5:8dd364cf5b73a31098b1cbd5d3c6744a */}
## 数据缓存

{/* md5:da9701946371bfde9b23ea0fa47e0321 */}
Next.js 有一个内置的数据缓存，它能在传入的**服务器请求**和**部署**之间**持久化**数据获取的结果。这是可能的，因为 Next.js 扩展了原生 `fetch` API，允许服务器上的每个请求设置自己的持久缓存语义。

{/* md5:3153ed134eb07901d463460c958f2a5a */}
> **有益信息**：在浏览器中，`fetch` 的 `cache` 选项表示请求将如何与浏览器的 HTTP 缓存交互；在 Next.js 中，`cache` 选项表示服务器端的请求将如何与服务器的数据缓存交互。

{/* md5:b6d38b43fdb5d5585eb41b7d3d444bc0 */}
你可以使用 `fetch` 的 [`cache`](#fetch-optionscache) 和 [`next.revalidate`](#fetch-optionsnextrevalidate) 选项来配置缓存行为。

{/* md5:37f2ed3639cfabe9c66a20855ff06f58 */}
在开发模式下，`fetch` 数据会为 [热模块替换 (HMR)](/docs/app/api-reference/functions/fetch#fetch-default-auto-no-store-and-cache-no-store-not-showing-fresh-data-in-development) 重用，并且对于 [硬刷新](/docs/app/api-reference/functions/fetch#hard-refresh-and-caching-in-development) 会忽略缓存选项。

{/* md5:d7959cf7bae7a33edb47b8f2e20c38e5 */}
**数据缓存的工作原理**

{/* md5:2aa2bfa0b0f91d3c3178b1d1c3cb47e9 */}
<Image
  alt="展示缓存和未缓存的 fetch 请求如何与数据缓存交互的示意图。缓存的请求存储在数据缓存中并被记忆化；未缓存的请求从数据源获取，不存储在数据缓存中，但被记忆化。"
  srcLight="/docs/light/data-cache.png"
  srcDark="/docs/dark/data-cache.png"
  width="1600"
  height="661"
/>

{/* md5:6a997c2ae10057266ce01828aa502ca2 */}
- 当在渲染期间首次调用带有 `'force-cache'` 选项的 `fetch` 请求时，Next.js 会检查数据缓存中是否有缓存的响应。
- 如果找到缓存的响应，它会立即返回并被[记忆化](#request-memoization)。
- 如果未找到缓存的响应，则会向数据源发出请求，结果存储在数据缓存中，并被记忆化。
- 对于未缓存的数据（例如未定义 `cache` 选项或使用 `{ cache: 'no-store' }`），结果总是从数据源获取，并被记忆化。
- 无论数据是缓存还是未缓存，请求总是被记忆化，以避免在 React 渲染流程中为相同数据发出重复请求。

{/* md5:13deaab31b90152e3be9b074417c1835 */}
> **数据缓存与请求记忆化的区别**
>
> 虽然两种缓存机制都通过重用缓存数据来帮助提高性能，但数据缓存在传入请求和部署之间是持久化的，而记忆化仅在请求的生命周期内有效。

{/* md5:29738bc4bd7bf0e0954f80bdb004b9e6 */}
### 持续时间

{/* md5:90990c12fd057ac0fcc90ee66d515894 */}
数据缓存在传入请求和部署之间是持久化的，除非你重新验证或选择退出。

{/* md5:6a05f230f7a107777ef724f3663a6332 */}
### 重新验证

{/* md5:145ead8fd6ce5b0ed801f2012c186d44 */}
缓存的数据可以通过两种方式重新验证：

{/* md5:8a962863f2a29aa037107c6aff039320 */}
- **基于时间的重新验证**：在一定时间过去且有新请求发出后重新验证数据。这对于不经常变化且数据新鲜度不那么关键的数据非常有用。
- **按需重新验证**：基于事件（例如表单提交）重新验证数据。按需重新验证可以使用基于标签或基于路径的方法来一次性重新验证一组数据。当你希望确保尽快显示最新数据时（例如当你的无头 CMS 中的内容更新时），这非常有用。

{/* md5:b5a3aeca0b8051324a3d438d1e7c0cf9 */}
#### 基于时间的重新验证

{/* md5:a90162811ff9b087c8b40996a77d07f0 */}
要按时间间隔重新验证数据，你可以使用 `fetch` 的 `next.revalidate` 选项来设置资源的缓存生命周期（以秒为单位）。

```js
// Revalidate at most every hour
fetch('https://...', { next: { revalidate: 3600 } })
```

{/* md5:266b6ca4ba4c9fccb76afc1dbaa5e366 */}
或者，你可以使用[路由段配置选项](#segment-config-options)来配置段中的所有 `fetch` 请求，或者在无法使用 `fetch` 的情况下使用。

{/* md5:4a98fbecb9a4441a3d9f1576fab85e71 */}
**基于时间的重新验证的工作原理**

{/* md5:424434f45196b5d45a766adac8f95ae1 */}
<Image
  alt="展示基于时间的重新验证如何工作的示意图：在重新验证期之后，第一次请求返回陈旧数据，然后数据被重新验证。"
  srcLight="/docs/light/time-based-revalidation.png"
  srcDark="/docs/dark/time-based-revalidation.png"
  width="1600"
  height="1252"
/>

{/* md5:efcb373fc9207a84cc7a8a48c6d1f306 */}
- 第一次调用带有 `revalidate` 的 fetch 请求时，数据将从外部数据源获取并存储在数据缓存中。
- 在指定时间范围内（例如 60 秒）调用的任何请求都将返回缓存的数据。
- 在时间范围过后，下一个请求仍会返回缓存的（现在已陈旧的）数据。
  - Next.js 将在后台触发数据的重新验证。
  - 一旦数据成功获取，Next.js 将用新数据更新数据缓存。
  - 如果后台重新验证失败，先前的数据将保持不变。

{/* md5:367c4d79161b7af7761c2bd92e9b1b5f */}
这类似于 [**stale-while-revalidate**](https://web.dev/articles/stale-while-revalidate) 行为。

{/* md5:74ac2d0267ddf5cbddf35e2307d15798 */}
#### 按需重新验证

{/* md5:3e5cc419db8b48666ad09c0952bc8e9e */}
可以通过路径 ([`revalidatePath`](#revalidatepath)) 或缓存标签 ([`revalidateTag`](#fetch-optionsnexttags-and-revalidatetag)) 按需重新验证数据。

{/* md5:11bebe1c46331684924ad14c9302e1ff */}
**按需重新验证的工作原理**

{/* md5:09c678f61d027047cc786481e77cf914 */}
<Image
  alt="展示按需重新验证如何工作的示意图：在重新验证请求后，数据缓存用新数据更新。"
  srcLight="/docs/light/on-demand-revalidation.png"
  srcDark="/docs/dark/on-demand-revalidation.png"
  width="1600"
  height="1082"
/>

{/* md5:03f2cda3a61d9049195fa3ba8df750a8 */}
- 第一次调用 `fetch` 请求时，数据将从外部数据源获取并存储在数据缓存中。
- 当触发按需重新验证时，相应的缓存条目将从缓存中清除。
  - 这与基于时间的重新验证不同，后者会将陈旧数据保留在缓存中，直到获取到新数据。
- 下次发出请求时，将再次是一个缓存**未命中 (MISS)**，数据将从外部数据源获取并存储在数据缓存中。

{/* md5:e823ae07709e9d1b27c1eb26087cd8a4 */}
### 选择退出

{/* md5:f3edba3b7064b893dbbef9731371547a */}
如果你**不**想缓存 `fetch` 的响应，你可以执行以下操作：

```js
let data = await fetch('https://api.vercel.app/blog', { cache: 'no-store' })
```

{/* md5:5a34a4af5f7849aaf584065dc9ebf582 */}
## 完整路由缓存

{/* md5:04f622d13d6e9242ac71823caa928bef */}
> **相关术语**：
>
> 你可能会看到 **自动静态优化**、**静态站点生成** 或 **静态渲染** 这些术语被互换使用，指的是在构建时渲染和缓存应用程序路由的过程。

{/* md5:817142d605a98668a81fda1bb3a1f313 */}
Next.js 在构建时自动渲染并缓存路由。这是一种优化，允许你为缓存的路由提供服务，而不是为每个请求在服务器上渲染，从而加快页面加载速度。

{/* md5:a44ea777e5b440b7047486cc70146bc4 */}
要理解完整路由缓存的工作原理，了解 React 如何处理渲染以及 Next.js 如何缓存结果会很有帮助：

{/* md5:430b06439624c10242436cd4d5036b24 */}
### 1. React 在服务器上的渲染

{/* md5:c070b9f2be3cbb4504ec8694ea1a9715 */}
在服务器上，Next.js 使用 React 的 API 来编排渲染。渲染工作被分成多个块：按单个路由段和 Suspense 边界划分。

{/* md5:71033d14c199a54dce994c3f4384edb7 */}
每个块分两步渲染：

{/* md5:89a8df93a3c3910ca74a4f69b499e5cf */}
1. React 将服务器组件渲染为一种特殊的、为流式传输优化的数据格式，称为 **React 服务器组件载荷**。
2. Next.js 使用 React 服务器组件载荷和客户端组件 JavaScript 指令在服务器上渲染 **HTML**。

{/* md5:e8302e1c61c440483a8c800233521a23 */}
这意味着我们不必等待所有内容都渲染完毕后再缓存工作或发送响应。相反，我们可以在工作完成时流式传输响应。

{/* md5:7c4c56ea96c628a122165f93ef30949c */}
> **什么是 React 服务器组件载荷？**
>
> React 服务器组件载荷是已渲染的 React 服务器组件树的紧凑二进制表示。它被客户端的 React 用于更新浏览器的 DOM。React 服务器组件载荷包含：
>
> - 服务器组件的渲染结果
> - 客户端组件应渲染位置的占位符及其 JavaScript 文件的引用
> - 从服务器组件传递给客户端组件的任何 props
>
> 要了解更多信息，请参阅 [服务器组件](/docs/app/getting-started/server-and-client-components) 文档。

{/* md5:a81361d893eebbaa47e86c87f5ca55e9 */}
### 2. Next.js 在服务器上的缓存（完整路由缓存）

{/* md5:171cd26ac2e9eed17212dddac2486188 */}
<Image
  alt="完整路由缓存的默认行为，展示对于静态渲染的路由，React 服务器组件载荷和 HTML 如何在服务器上缓存。"
  srcLight="/docs/light/full-route-cache.png"
  srcDark="/docs/dark/full-route-cache.png"
  width="1600"
  height="888"
/>

{/* md5:cecacb87391a66edce52c684bd39e153 */}
Next.js 的默认行为是在服务器上缓存路由的渲染结果（React 服务器组件载荷和 HTML）。这适用于在构建时或重新验证期间静态渲染的路由。

{/* md5:fdbcd973d84d7690e18fbea5c951bbd5 */}
### 3. React 在客户端上的水合与协调

{/* md5:f295b24435ffe1a67e8b2441b76fca9c */}
在请求时，在客户端：

{/* md5:db0c356624649be0d057a814239c1727 */}
1. HTML 被用于立即显示客户端和服务器组件的快速、非交互式的初始预览。
2. React 服务器组件载荷用于协调客户端和已渲染的服务器组件树，并更新 DOM。
3. JavaScript 指令用于[水合](https://react.dev/reference/react-dom/client/hydrateRoot)客户端组件并使应用程序具有交互性。

{/* md5:15fdbec3904ec876cbffcf2c7c2307a5 */}
### 4. Next.js 在客户端上的缓存（路由缓存）

{/* md5:a9d53599f6b78244ed08cbe7f25b3031 */}
React 服务器组件载荷存储在客户端的[路由缓存](#client-side-router-cache)中——这是一个独立的内存缓存，按单个路由段划分。此路由缓存用于通过存储先前访问过的路由并预取未来可能访问的路由来改善导航体验。

{/* md5:342c2bd5829740fdc5e16d5cd32a3836 */}
### 5. 后续导航

{/* md5:4458a08cf0565f5a7fc497245665d128 */}
在后续导航或预取期间，Next.js 将检查 React 服务器组件载荷是否存储在路由缓存中。如果是，它将跳过向服务器发送新请求。

{/* md5:a8f409d98c18137964c5d5a5a8974ca0 */}
如果路由段不在缓存中，Next.js 将从服务器获取 React 服务器组件载荷，并在客户端填充路由缓存。

{/* md5:32289143984b3f55dd8d5b3a54b668be */}
### 静态与动态渲染

{/* md5:95cd738b24e664a1704e21b5b4ae5e85 */}
路由在构建时是否被缓存取决于它是静态还是动态渲染。静态路由默认被缓存，而动态路由在请求时渲染，不被缓存。

{/* md5:ea199fc6279216311f90c6aba11c6a6a */}
此图显示了静态和动态渲染路由之间的区别，以及缓存和未缓存的数据：

{/* md5:2148e66e8d34b137178c56df1e7524f6 */}
<Image
  alt="静态和动态渲染如何影响完整路由缓存。静态路由在构建时或数据重新验证后被缓存，而动态路由从不被缓存。"
  srcLight="/docs/light/static-and-dynamic-routes.png"
  srcDark="/docs/dark/static-and-dynamic-routes.png"
  width="1600"
  height="1314"
/>

{/* md5:da8a0bfdf2eb5d87c536ed3c1e4251c8 */}
了解更多关于[静态和动态渲染](/docs/app/getting-started/partial-prerendering#static-rendering)的信息。

{/* md5:29738bc4bd7bf0e0954f80bdb004b9e6 */}
### 持续时间

{/* md5:d1758d6e56aa0a88df20c88822f96834 */}
默认情况下，完整路由缓存是持久化的。这意味着渲染输出在用户请求之间被缓存。

{/* md5:44c929f02d00f5bb48354ca224b643ba */}
### 失效

{/* md5:2fa4d8e1dae4e3954fe2c004ed4fcfa6 */}
你可以通过两种方式使完整路由缓存失效：

{/* md5:b60d0c7a63c216089ddabe3fd3b26b56 */}
- **[重新验证数据](/docs/app/guides/caching#revalidating)**: 重新验证[数据缓存](#data-cache)将反过来通过重新渲染服务器上的组件并缓存新的渲染输出来使路由缓存失效。
- **重新部署**: 与跨部署持续存在的数据缓存不同，完整路由缓存在新部署时会被清除。

{/* md5:e823ae07709e9d1b27c1eb26087cd8a4 */}
### 选择退出

{/* md5:6f846e586f34f124ed1590f8bc476483 */}
你可以通过以下方式选择退出完整路由缓存，或者换句话说，为每个传入的请求动态渲染组件：

{/* md5:b0427d62971d883716766ad9439ef5d9 */}
- **使用[动态 API](#dynamic-apis)**: 这将使路由退出完整路由缓存，并在请求时动态渲染它。数据缓存仍可使用。
- **使用 `dynamic = 'force-dynamic'` 或 `revalidate = 0` 路由段配置选项**: 这将跳过完整路由缓存和数据缓存。这意味着组件将在服务器的每个传入请求上渲染，并在每次请求时获取数据。路由缓存仍将适用，因为它是客户端缓存。
- **选择退出[数据缓存](#data-cache)**: 如果路由有未缓存的 `fetch` 请求（即 `no-store`），这将使路由退出完整路由缓存。特定 `fetch` 请求的数据将在每个传入请求上获取。其他明确启用缓存的 `fetch` 请求仍将在数据缓存中缓存。这允许混合使用缓存和未缓存的数据。

{/* md5:e2e7350ab302a41a4287c7c6157fc578 */}
## 客户端路由缓存

{/* md5:573ce0a48fb15cdfabf2f0db1fd64dcd */}
Next.js 有一个内存中的客户端路由缓存，它存储路由段的 RSC 载荷，按布局、加载状态和页面划分。

{/* md5:d4aeec8a4e463aa24a100c0fd664b74c */}
当用户在路由之间导航时，Next.js 会缓存访问过的路由段并[预取](/docs/app/getting-started/linking-and-navigating#prefetching)用户可能导航到的路由。这实现了瞬间的前进/后退导航，导航之间没有整页重新加载，并保持了共享布局中的浏览器状态和 React 状态。

{/* md5:3c0f59597a232ee3ae2585a574d07d4b */}
通过路由缓存：

{/* md5:c0a78950f7cbfd3866305b8adb2a50b9 */}
- **布局**在导航时被缓存和重用（[部分渲染](/docs/app/getting-started/linking-and-navigating#client-side-transitions)）。
- **加载状态**在导航时被缓存和重用，以实现[即时导航](/docs/app/api-reference/file-conventions/loading#instant-loading-states)。
- **页面**默认不缓存，但在浏览器前进和后退导航时会被重用。你可以通过使用实验性的 [`staleTimes`](/docs/app/api-reference/config/next-config-js/staleTimes) 配置选项来启用页面段的缓存。

{/* md5:90c12d7d3f4dc12983e660e547e65356 */}
{/* TODO: 更新示意图以匹配 v15 行为 */}

{/* md5:f0ca50005ef9671d7f92b2023f08098e */}
> **有益信息：** 此缓存专门适用于 Next.js 和服务器组件，与浏览器的 [bfcache](https://web.dev/bfcache/) 不同，尽管结果类似。

{/* md5:29738bc4bd7bf0e0954f80bdb004b9e6 */}
### 持续时间

{/* md5:fe7a818a55a3555c4c8892ad91c46709 */}
缓存存储在浏览器的临时内存中。两个因素决定路由缓存的持续时间：

{/* md5:e5c78dc88fd591fc6488f026cd5a9353 */}
- **会话**：缓存在导航期间持续存在。但是，页面刷新时会被清除。
- **自动失效期**：布局和加载状态的缓存在特定时间后会自动失效。持续时间取决于资源是如何[预取](/docs/app/api-reference/components/link#prefetch)的，以及资源是否是[静态生成](/docs/app/getting-started/partial-prerendering#static-rendering)的：
  - **默认预取** (`prefetch={null}` 或未指定)：对于动态页面不缓存，对于静态页面缓存 5 分钟。
  - **完全预取** (`prefetch={true}` 或 `router.prefetch`)：对于静态和动态页面都缓存 5 分钟。

{/* md5:1f749bf9e26f102f669884c5e6be2e4b */}
页面刷新将清除**所有**缓存的段，而自动失效期仅从该段被预取的时间起影响该特定段。

{/* md5:67216ed49a28bb089a8c09d3a1da50c1 */}
> **有益信息**：实验性的 [`staleTimes`](/docs/app/api-reference/config/next-config-js/staleTimes) 配置选项可用于调整上述自动失效时间。

{/* md5:44c929f02d00f5bb48354ca224b643ba */}
### 失效

{/* md5:d9d7a9801c1445caac77e8a0b8684f17 */}
你可以通过两种方式使路由缓存失效：

{/* md5:fb10628b999d21a68a4c10823e6344ce */}
- 在**服务器操作 (Server Action)** 中：
  - 通过路径 ([`revalidatePath`](/docs/app/api-reference/functions/revalidatePath)) 或缓存标签 ([`revalidateTag`](/docs/app/api-reference/functions/revalidatetag)) 按需重新验证数据。
  - 使用 [`cookies.set`](/docs/app/api-reference/functions/cookies#setting-a-cookie) 或 [`cookies.delete`](/docs/app/api-reference/functions/cookies#deleting-cookies) 会使路由缓存失效，以防止使用 cookie 的路由变得陈旧（例如身份验证）。
- 调用 [`router.refresh`](/docs/app/api-reference/functions/use-router) 将使路由缓存失效，并为当前路由向服务器发出新请求。

{/* md5:e823ae07709e9d1b27c1eb26087cd8a4 */}
### 选择退出

{/* md5:1b730c5b86d289ff250a15bd94a2fe6f */}
在 Next.js 15 中，页面段默认是选择退出的。

{/* md5:65a2048c10026bf926fc84187914acc1 */}
> **有益信息：** 你也可以通过将 `<Link>` 组件的 `prefetch` 属性设置为 `false` 来选择退出[预取](/docs/app/getting-started/linking-and-navigating#prefetching)。

{/* md5:cad309ddd3fe8d13667e71c7ffb70906 */}
## 缓存交互

{/* md5:8b3ca88737f8a7567e4c5651db0d33fc */}
在配置不同的缓存机制时，了解它们之间的交互方式很重要：

{/* md5:d9d27f771e0dd4861a48f27a30f0c090 */}
### 数据缓存与完整路由缓存

{/* md5:35bdd334f57451151e6bab42b8b2b40c */}
- 重新验证或选择退出数据缓存**将**使完整路由缓存失效，因为渲染输出依赖于数据。
- 使完整路由缓存失效或选择退出它**不会**影响数据缓存。你可以动态渲染一个同时具有缓存和未缓存数据的路由。当你的页面大部分使用缓存数据，但有几个组件依赖于需要在请求时获取的数据时，这非常有用。你可以动态渲染，而不必担心重新获取所有数据的性能影响。

{/* md5:2288a71bbd52abd100c92e1a7bf3bf4c */}
### 数据缓存与客户端路由缓存

{/* md5:438bdec562f96945ba3a62a927f4e263 */}
- 为了立即使数据缓存和路由缓存失效，你可以在[服务器操作](/docs/app/getting-started/updating-data)中使用 [`revalidatePath`](#revalidatepath) 或 [`revalidateTag`](#fetch-optionsnexttags-and-revalidatetag)。
- 在[路由处理程序](/docs/app/api-reference/file-conventions/route)中重新验证数据缓存**不会**立即使路由缓存失效，因为路由处理程序不与特定路由绑定。这意味着路由缓存将继续提供先前的载荷，直到硬刷新或自动失效期过去。

{/* md5:fcbae18a6024124ac1b92110f272c8bc */}
## API

{/* md5:701099482918a02378f02d286531c734 */}
下表概述了不同的 Next.js API 如何影响缓存：

{/* md5:1d4655bd67b3f733c638d7469940287d */}
| API                                                                     | Router Cache               | Full Route Cache      | Data Cache            | React Cache          |
| ----------------------------------------------------------------------- | -------------------------- | --------------------- | --------------------- | -------------------- |
| [`<Link prefetch>`](#link)                                              | Cache                      |                       |                       |                      |
| [`router.prefetch`](#routerprefetch)                                    | Cache                      |                       |                       |                      |
| [`router.refresh`](#routerrefresh)                                      | Revalidate                 |                       |                       |                      |
| [`fetch`](#fetch)                                                       |                            |                       | Cache                 | Cache (GET and HEAD) |
| [`fetch` `options.cache`](#fetch-optionscache)                          |                            |                       | Cache or Opt out      |                      |
| [`fetch` `options.next.revalidate`](#fetch-optionsnextrevalidate)       |                            | Revalidate            | Revalidate            |                      |
| [`fetch` `options.next.tags`](#fetch-optionsnexttags-and-revalidatetag) |                            | Cache                 | Cache                 |                      |
| [`revalidateTag`](#fetch-optionsnexttags-and-revalidatetag)             | Revalidate (Server Action) | Revalidate            | Revalidate            |                      |
| [`revalidatePath`](#revalidatepath)                                     | Revalidate (Server Action) | Revalidate            | Revalidate            |                      |
| [`const revalidate`](#segment-config-options)                           |                            | Revalidate or Opt out | Revalidate or Opt out |                      |
| [`const dynamic`](#segment-config-options)                              |                            | Cache or Opt out      | Cache or Opt out      |                      |
| [`cookies`](#cookies)                                                   | Revalidate (Server Action) | Opt out               |                       |                      |
| [`headers`, `searchParams`](#dynamic-apis)                              |                            | Opt out               |                       |                      |
| [`generateStaticParams`](#generatestaticparams)                         |                            | Cache                 |                       |                      |
| [`React.cache`](#react-cache-function)                                  |                            |                       |                       | Cache                |
| [`unstable_cache`](/docs/app/api-reference/functions/unstable_cache)    |                            |                       | Cache                 |                      |

{/* md5:9d8fca5927314b104b684ec7f57529f2 */}
### `<Link>`

{/* md5:3b33c0f6b3f88d61258a47ad8df2092a */}
默认情况下，`<Link>` 组件会自动预取来自完整路由缓存的路由，并将 React 服务器组件载荷添加到路由缓存。

{/* md5:98b914bb2c28d1b90584ccad4fa53344 */}
要禁用预取，你可以将 `prefetch` 属性设置为 `false`。但这不会永久跳过缓存，当用户访问该路由时，该路由段仍将在客户端被缓存。

{/* md5:78efe9d8dc5f8d06e8cb5c870ab6e0b0 */}
了解更多关于 [`<Link>` 组件](/docs/app/api-reference/components/link) 的信息。

{/* md5:70085d8f634c225aae1e53a67defeacb */}
### `router.prefetch`

{/* md5:03d7ca0338f325d45b149057f6c57270 */}
`useRouter` 钩子的 `prefetch` 选项可用于手动预取路由。这会将 React 服务器组件载荷添加到路由缓存。

{/* md5:d11ca33c6d88b4bf0885286a9c31cb56 */}
请参阅 [`useRouter` 钩子](/docs/app/api-reference/functions/use-router) API 参考。

{/* md5:8c03d40bb448b2de7c70a119d96ea8c0 */}
### `router.refresh`

{/* md5:152fcb1aba014c49db4d87365db45818 */}
`useRouter` 钩子的 `refresh` 选项可用于手动刷新路由。这会完全清除路由缓存，并为当前路由向服务器发出新请求。`refresh` 不会影响数据缓存或完整路由缓存。

{/* md5:c11def2b56bf21ea8c234ac29ee380e6 */}
渲染结果将在客户端进行协调，同时保留 React 状态和浏览器状态。

{/* md5:d11ca33c6d88b4bf0885286a9c31cb56 */}
请参阅 [`useRouter` 钩子](/docs/app/api-reference/functions/use-router) API 参考。

{/* md5:030c314fac5e1382a7828bed1850087c */}
### `fetch`

{/* md5:fe3e4f8064cc582ada7ab6a1b1395d3a */}
从 `fetch` 返回的数据**不会**自动缓存在数据缓存中。

{/* md5:1dfef3cc3281e525cedd8b073566a4e1 */}
`fetch` 的默认缓存行为（例如，当未指定 `cache` 选项时）等同于将 `cache` 选项设置为 `no-store`：

```js
let data = await fetch('https://api.vercel.app/blog', { cache: 'no-store' })
```

{/* md5:1c15dc491f9b6ad0e230c18d77788dd4 */}
请参阅 [`fetch` API 参考](/docs/app/api-reference/functions/fetch) 以获取更多选项。

{/* md5:10f030229be4b153fe4f94de4cd99426 */}
### `fetch options.cache`

{/* md5:414e339df42b1f77cc0dbcb116b153f4 */}
你可以通过将 `cache` 选项设置为 `force-cache` 来使单个 `fetch` 请求参与缓存：

```jsx
// Opt into caching
fetch(`https://...`, { cache: 'force-cache' })
```

{/* md5:1c15dc491f9b6ad0e230c18d77788dd4 */}
请参阅 [`fetch` API 参考](/docs/app/api-reference/functions/fetch) 以获取更多选项。

{/* md5:f236ff8a551fbc6079e2f499be752392 */}
### `fetch options.next.revalidate`

{/* md5:1a15d89c4a565aa6ad017502ea41386a */}
你可以使用 `fetch` 的 `next.revalidate` 选项来设置单个 `fetch` 请求的重新验证周期（以秒为单位）。这将重新验证数据缓存，进而重新验证完整路由缓存。将获取新数据，组件将在服务器上重新渲染。

```jsx
// Revalidate at most after 1 hour
fetch(`https://...`, { next: { revalidate: 3600 } })
```

{/* md5:29831530c322814ce3b1dbceaf8399a8 */}
请参阅 [`fetch` API 参考](/docs/app/api-reference/functions/fetch) 以获取更多选项。

{/* md5:ffd4f125252673faeb96943728a12c3d */}
### `fetch options.next.tags` and `revalidateTag`

{/* md5:2e881053d885607ed83ba78cd02bafb3 */}
Next.js 有一个缓存标签系统，用于细粒度的数据缓存和重新验证。

{/* md5:617da8f39cafc51529fd405925133359 */}
1. 当使用 `fetch` 或 [`unstable_cache`](/docs/app/api-reference/functions/unstable_cache) 时，你可以选择用一个或多个标签标记缓存条目。
2. 然后，你可以调用 `revalidateTag` 来清除与该标签关联的缓存条目。

{/* md5:541dad5ab4b9e80f6d5801c78be67d88 */}
例如，你可以在获取数据时设置一个标签：

```jsx
// Cache data with a tag
fetch(`https://...`, { next: { tags: ['a', 'b', 'c'] } })
```

{/* md5:265c973e196d1a48a73a8935c9ce3fdf */}
然后，调用带有标签的 `revalidateTag` 来清除缓存条目：

```jsx
// Revalidate entries with a specific tag
revalidateTag('a')
```

{/* md5:9dbe97a481002def86659a9bc4f3519c */}
有两个地方可以使用 `revalidateTag`，具体取决于你想要实现的目标：

{/* md5:4a4c5e7b7afccb1596e45743f100bf44 */}
1. [路由处理程序](/docs/app/api-reference/file-conventions/route) - 以响应第三方事件（例如 webhook）来重新验证数据。这不会立即使路由缓存失效，因为路由处理程序不与特定路由绑定。
2. [服务器操作](/docs/app/getting-started/updating-data) - 以在用户操作（例如表单提交）后重新验证数据。这将使关联路由的路由缓存失效。

{/* md5:aad045404b5fc758c24cb564f12b0a06 */}
### `revalidatePath`

{/* md5:9f8cac351e1da195041d3fa465959d35 */}
`revalidatePath` 允许你在单个操作中手动重新验证数据**并**重新渲染特定路径下的路由段。调用 `revalidatePath` 方法会重新验证数据缓存，进而使完整路由缓存失效。

```jsx
revalidatePath('/')
```

{/* md5:28911c92f51a1597df2c63a3f3938cc1 */}
有两个地方可以使用 `revalidatePath`，具体取决于你想要实现的目标：

{/* md5:b967de574f0792f6489d450f17735fd1 */}
1. [路由处理程序](/docs/app/api-reference/file-conventions/route) - 以响应第三方事件（例如 webhook）来重新验证数据。
2. [服务器操作](/docs/app/getting-started/updating-data) - 以在用户交互（例如表单提交、点击按钮）后重新验证数据。

{/* md5:74d9da602c6abc9052eba07b05216003 */}
请参阅 [`revalidatePath` API 参考](/docs/app/api-reference/functions/revalidatePath) 以获取更多信息。

{/* md5:55c8d0174c9514fb55f493294992a3f5 */}
> **`revalidatePath`** 与 **`router.refresh`** 的比较：
>
> 调用 `router.refresh` 将清除路由缓存，并在服务器上重新渲染路由段，而不会使数据缓存或完整路由缓存失效。
>
> 区别在于 `revalidatePath` 清除了数据缓存和完整路由缓存，而 `router.refresh()` 不会改变数据缓存和完整路由缓存，因为它是一个客户端 API。

{/* md5:b13352c9ef4f5a28ccec28313c677720 */}
### 动态 API

{/* md5:f8bd590270664983f5484334e7f0f59b */}
像 `cookies` 和 `headers` 这样的动态 API，以及页面中的 `searchParams` 属性依赖于运行时传入的请求信息。使用它们将使路由退出完整路由缓存，换句话说，该路由将被动态渲染。

{/* md5:6ecbeba1e092cd4d12fa6899f9c63a53 */}
#### `cookies`

{/* md5:0f3fc28744044a33908f53b4308ec717 */}
在服务器操作中使用 `cookies.set` 或 `cookies.delete` 会使路由缓存失效，以防止使用 cookie 的路由变得陈旧（例如以反映身份验证更改）。

{/* md5:dc66180eb182b89072ccedbfbed5384a */}
请参阅 [`cookies`](/docs/app/api-reference/functions/cookies) API 参考。

{/* md5:43effc599f07ca28b487c1779daba15c */}
### 路由段配置选项

{/* md5:cd6d43d6ad82dbdb7737bdbd59e226b4 */}
路由段配置选项可用于覆盖路由段的默认值，或者在你无法使用 `fetch` API 时使用（例如数据库客户端或第三方库）。

{/* md5:e9f7c5b9eecbc646c6066dbf94bce7d9 */}
以下路由段配置选项将使路由退出完整路由缓存：

{/* md5:c6fb17d6d95c1dd5e6111d530b9c6e9e */}
- `const dynamic = 'force-dynamic'`

{/* md5:53b3d6262955c1d6da9d6f3f4296a8fe */}
此配置选项将使所有 fetch 请求退出数据缓存（即 `no-store`）：

{/* md5:1ef25e9b217664c00f33459a1e3bc5f8 */}
- `const fetchCache = 'default-no-store'`

{/* md5:f0749d5d779119d966e76aaaf8ee3e36 */}
请参阅 [`fetchCache`](/docs/app/api-reference/file-conventions/route-segment-config#fetchcache) 以查看更多高级选项。

{/* md5:3e5e20e4e70af44b1cbccee06d076302 */}
请参阅[路由段配置](/docs/app/api-reference/file-conventions/route-segment-config)文档以获取更多选项。

{/* md5:ed84cd664c2d6ab43cde54a0306c9a07 */}
### `generateStaticParams`

{/* md5:090def20fa07b6ff655e8a7b0f364b86 */}
对于[动态段](/docs/app/api-reference/file-conventions/dynamic-routes)（例如 `app/blog/[slug]/page.js`），由 `generateStaticParams` 提供的路径在构建时被缓存在完整路由缓存中。在请求时，Next.js 也会缓存那些在构建时未知的路径，即它们在运行时第一次被访问时。

{/* md5:86ce2bf2e34f465640a6e5766760a1d8 */}
要在构建时静态渲染所有路径，请将完整路径列表提供给 `generateStaticParams`：

```jsx filename="app/blog/[slug]/page.js"
export async function generateStaticParams() {
  const posts = await fetch('https://.../posts').then((res) => res.json())

  return posts.map((post) => ({
    slug: post.slug,
  }))
}
```

{/* md5:a954a2136baf897137e4125d1815c9bd */}
要在构建时静态渲染部分路径，其余路径在运行时第一次访问时渲染，请返回部分路径列表：

```jsx filename="app/blog/[slug]/page.js"
export async function generateStaticParams() {
  const posts = await fetch('https://.../posts').then((res) => res.json())

  // Render the first 10 posts at build time
  return posts.slice(0, 10).map((post) => ({
    slug: post.slug,
  }))
}
```

{/* md5:b4172a15f4ebfa24a75ebf472daeb7dd */}
要在路径第一次被访问时静态渲染所有路径，请返回一个空数组（在构建时不会渲染任何路径）或使用 [`export const dynamic = 'force-static'`](/docs/app/api-reference/file-conventions/route-segment-config#dynamic)：

```jsx filename="app/blog/[slug]/page.js"
export async function generateStaticParams() {
  return []
}
```

{/* md5:985c8411da47a91f547bcc8a49e3d586 */}
> **有益信息：** 你必须从 `generateStaticParams` 返回一个数组，即使它是空的。否则，该路由将被动态渲染。

```jsx filename="app/changelog/[slug]/page.js"
export const dynamic = 'force-static'
```

{/* md5:3febf61ed62b32776108e6384b9ffdae */}
要在请求时禁用缓存，请在路由段中添加 `export const dynamicParams = false` 选项。当使用此配置选项时，只有由 `generateStaticParams` 提供的路径会被服务，其他路由将返回 404 或匹配（在[捕获所有路由](/docs/app/api-reference/file-conventions/dynamic-routes#catch-all-segments)的情况下）。

{/* md5:028b0bc6c823c263da78374836bd1b2b */}
### React `cache` 函数

{/* md5:6de300ffd3d997907dcfd0b91fdf600b */}
React `cache` 函数允许你记忆化函数的返回值，使你能够多次调用同一函数而只执行一次。

{/* md5:da982b1189cccf441bc55c0a4c7396e0 */}
使用 `GET` 或 `HEAD` 方法的 `fetch` 请求会自动记忆化，因此你不需要将其包装在 React `cache` 中。但是，对于其他 `fetch` 方法，或者使用本身不记忆化请求的数据获取库（例如某些数据库、CMS 或 GraphQL 客户端）时，你可以使用 `cache` 来手动记忆化数据请求。

```ts filename="utils/get-item.ts" switcher
import { cache } from 'react'
import db from '@/lib/db'

export const getItem = cache(async (id: string) => {
  const item = await db.item.findUnique({ id })
  return item
})
```

```js filename="utils/get-item.js" switcher
import { cache } from 'react'
import db from '@/lib/db'

export const getItem = cache(async (id) => {
  const item = await db.item.findUnique({ id })
  return item
})
```
