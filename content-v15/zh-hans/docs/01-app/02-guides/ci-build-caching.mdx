---
title: 如何配置持续集成 (CI) 构建缓存 (CI build caching)
nav_title: CI 构建缓存
description: 了解如何配置 CI 以缓存 Next.js 构建
---

{/* md5:ad82ceb1bffed365a8a38a667688193d */}
为了提高构建性能，Next.js 会将缓存保存到 `.next/cache`，该缓存可在构建之间共享。

{/* md5:f7948c7aa6bf6cc8a04887e31a84441f */}
为了在持续集成 (CI) 环境中利用此缓存，需要将您的 CI 工作流配置为在构建之间正确地持久保存缓存。

{/* md5:d3d33fc0500b94ff294ab98abf05e0d2 */}
> 如果您的 CI 未配置为在构建之间持久保存 `.next/cache`，您可能会看到 [未检测到缓存](/docs/messages/no-cache) 错误。

{/* md5:e408569034bc023c818190e580e78551 */}
以下是一些常见 CI 提供商的缓存配置示例：

{/* md5:05cabcfbf69e2a39276fb302cfa9605e */}
## Vercel

{/* md5:4e0602b647504d12a80515fa3028a8d7 */}
Next.js 缓存会为您自动配置。您无需执行任何操作。如果您在 Vercel 上使用 Turborepo，请 [在此了解更多](https://vercel.com/docs/monorepos/turborepo)。

{/* md5:8befe0d7f4abb2154d402b906e92d18c */}
## CircleCI

{/* md5:023d25c0aac029ad37ad5edf8abf294c */}
编辑 `.circleci/config.yml` 中的 `save_cache` 步骤以包含 `.next/cache`：

```yaml
steps:
  - save_cache:
      key: dependency-cache-{{ checksum "yarn.lock" }}
      paths:
        - ./node_modules
        - ./.next/cache
```

{/* md5:43ba4fff60c8c029d88ee43cd9363803 */}
如果您没有 `save_cache` 键，请遵循 CircleCI 的 [设置构建缓存文档](https://circleci.com/docs/2.0/caching/)。

{/* md5:0ef37acce36a80eab3aac3cc79554fa4 */}
## Travis CI

{/* md5:502d5cedcd8ebb5e3d13a35823c5b740 */}
在 `.travis.yml` 中添加或合并以下内容：

```yaml
cache:
  directories:
    - $HOME/.cache/yarn
    - node_modules
    - .next/cache
```

{/* md5:eb34a6376f001890bbd7112970b77c0b */}
## GitLab CI

{/* md5:ac74c3ff329ed393078ed59270ecf226 */}
在 `.gitlab-ci.yml` 中添加或合并以下内容：

```yaml
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - .next/cache/
```

{/* md5:6aa56cd439cfa9c7b16fd9958f464a11 */}
## Netlify CI

{/* md5:dead525bac4250291e607f67c4359a9c */}
使用 [Netlify 插件](https://www.netlify.com/products/build/plugins/) 配合 [`@netlify/plugin-nextjs`](https://www.npmjs.com/package/@netlify/plugin-nextjs)。

{/* md5:f8e47fe6abbb4985775681034354674f */}
## AWS CodeBuild

{/* md5:dc1f988089fb319ee5355da809f40572 */}
在 `buildspec.yml` 中添加（或合并）以下内容：

```yaml
cache:
  paths:
    - 'node_modules/**/*' # Cache `node_modules` for faster `yarn` or `npm i`
    - '.next/cache/**/*' # Cache Next.js for faster application rebuilds
```

{/* md5:3c3e62a316d45114542c362b92a6158f */}
## GitHub Actions

{/* md5:658bcb203a2e24f9991227c080cac152 */}
使用 GitHub 的 [actions/cache](https://github.com/actions/cache)，在您的工作流文件中添加以下步骤：

```yaml
uses: actions/cache@v4
with:
  # See here for caching with `yarn`, `bun` or other package managers https://github.com/actions/cache/blob/main/examples.md or you can leverage caching with actions/setup-node https://github.com/actions/setup-node
  path: |
    ~/.npm
    ${{ github.workspace }}/.next/cache
  # Generate a new cache whenever packages or source files change.
  key: ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}-${{ hashFiles('**/*.js', '**/*.jsx', '**/*.ts', '**/*.tsx') }}
  # If source files changed but packages didn't, rebuild from a prior cache.
  restore-keys: |
    ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}-
```

{/* md5:e10a912930829921053d5092baa9cae0 */}
## Bitbucket Pipelines

{/* md5:c590c84af785b48637730a084f77936e */}
在 `bitbucket-pipelines.yml` 的顶层（与 `pipelines` 同级）添加或合并以下内容：

```yaml
definitions:
  caches:
    nextcache: .next/cache
```

{/* md5:78fa9d41f4e473413db6cb0aa3f468a4 */}
然后在管道 `step` 的 `caches` 部分引用它：

```yaml
- step:
    name: your_step_name
    caches:
      - node
      - nextcache
```

{/* md5:876005d30cb9719d615365ff33945445 */}
## Heroku

{/* md5:00a3199cda4bef3c96aee9a0dbdf9883 */}
使用 Heroku 的 [自定义缓存](https://devcenter.heroku.com/articles/nodejs-support#custom-caching)，在顶层 package.json 中添加 `cacheDirectories` 数组：

```javascript
"cacheDirectories": [".next/cache"]
```

{/* md5:91ee5b3b97c4d7be47fa0eb055cf388e */}
## Azure Pipelines

{/* md5:21775d6831649bc2fd14523ce0bf4e23 */}
使用 Azure Pipelines 的 [缓存任务](https://docs.microsoft.com/en-us/azure/devops/pipelines/tasks/utility/cache)，在执行 `next build` 的任务之前，将以下任务添加到您的管道 yaml 文件中：

```yaml
- task: Cache@2
  displayName: 'Cache .next/cache'
  inputs:
    key: next | $(Agent.OS) | yarn.lock
    path: '$(System.DefaultWorkingDirectory)/.next/cache'
```

{/* md5:a6be30ae4c7c5c10a5108894f87b7e0e */}
## Jenkins (Pipeline)

{/* md5:1009e19ad9a86c44921c54bd72502f84 */}
使用 Jenkins 的 [Job Cacher](https://www.jenkins.io/doc/pipeline/steps/jobcacher/) 插件，在通常运行 `next build` 或 `npm install` 的位置，将以下构建步骤添加到您的 `Jenkinsfile` 中：

```yaml
stage("Restore npm packages") {
    steps {
        // Writes lock-file to cache based on the GIT_COMMIT hash
        writeFile file: "next-lock.cache", text: "$GIT_COMMIT"

        cache(caches: [
            arbitraryFileCache(
                path: "node_modules",
                includes: "**/*",
                cacheValidityDecidingFile: "package-lock.json"
            )
        ]) {
            sh "npm install"
        }
    }
}
stage("Build") {
    steps {
        // Writes lock-file to cache based on the GIT_COMMIT hash
        writeFile file: "next-lock.cache", text: "$GIT_COMMIT"

        cache(caches: [
            arbitraryFileCache(
                path: ".next/cache",
                includes: "**/*",
                cacheValidityDecidingFile: "next-lock.cache"
            )
        ]) {
            // aka `next build`
            sh "npm run build"
        }
    }
}
```
