---
title: 如何思考 Next.js 中的数据安全
nav_title: 数据安全
description: 了解 Next.js 内置的数据安全功能，并学习保护应用程序数据的最佳实践。
related:
  title: 后续步骤
  description: 了解更多关于本指南中提及的主题。
  links:
    - app/guides/authentication
    - app/guides/content-security-policy
    - app/guides/forms
---

{/* md5:9ea6de29c7079f7ffcd424374f899b40 */}
[React 服务器组件 (React Server Components)](https://react.dev/reference/rsc/server-components) 提升了性能并简化了数据获取，但也改变数据的访问位置和方式，从而改变了在前端应用中处理数据的传统安全假设。

{/* md5:5c6753b614cc5250a690a6293f7a6fb0 */}
本指南将帮助您理解如何在 Next.js 中思考数据安全以及如何实施最佳实践。

{/* md5:7d6549571ee058df1e143bd30ba25fb0 */}
## 数据获取方法

{/* md5:d9a06d0dfe1e84efab6d147494a7c09f */}
根据项目的大小和阶段，我们推荐在 Next.js 中获取数据的三种主要方法：

{/* md5:d0fcba2ba7e5b93b4be5b8da4c20fe54 */}
- [HTTP APIs](#external-http-apis)：适用于现有的大型应用程序和组织。
- [数据访问层 (Data Access Layer)](#data-access-layer)：适用于新项目。
- [组件级数据访问 (Component-Level Data Access)](#component-level-data-access)：适用于原型和学习。

{/* md5:220597be702087d99fc2f2221fa47c94 */}
我们建议选择一种数据获取方法并避免混用。这使代码库中的开发人员和安全审计人员都能清楚预期。

{/* md5:b91c235c7fe20f38c6bd8eb79144e70a */}
### 外部 HTTP API

{/* md5:6a142b6acf84804d71c490adf209f1df */}
在现有项目中采用服务器组件 (Server Components) 时，您应遵循**零信任 (Zero Trust)** 模型。您可以继续从服务器组件中使用 [`fetch`](/docs/app/api-reference/functions/fetch) 调用现有的 API 端点（如 REST 或 GraphQL），就像在客户端组件 (Client Components) 中一样。

```tsx filename="app/page.tsx"
import { cookies } from 'next/headers'

export default async function Page() {
  const cookieStore = cookies()
  const token = cookieStore.get('AUTH_TOKEN')?.value

  const res = await fetch('https://api.example.com/profile', {
    headers: {
      Cookie: `AUTH_TOKEN=${token}`,
      // Other headers
    },
  })

  // ....
}
```

{/* md5:62818b7fd45cbbf2ac9821ee502cef90 */}
此方法在以下情况下效果很好：

{/* md5:28367cab4bcad52cd59e7620ff17862f */}
- 您已经建立了安全实践。
- 独立的后端团队使用其他语言或独立管理 API。

{/* md5:fbde038e4cc9eab0d71eda214157d095 */}
### 数据访问层

{/* md5:96f6b6d3f95186790abc1bda4ce1da7b */}
对于新项目，我们建议创建一个专用的**数据访问层 (Data Access Layer, DAL)**。这是一个内部控制如何以及何时获取数据以及将什么传递到渲染上下文的内部库。

{/* md5:b66cc63f72a0e38af8ffb167f14bca4b */}
数据访问层 (DAL) 应该：

{/* md5:3c318b92e5803291bf768670c1dea479 */}
- 仅在服务器上运行。
- 执行授权检查。
- 返回安全的、最小化的**数据传输对象 (Data Transfer Objects, DTOs)**。

{/* md5:a6f31eaee28da93032890421af671b16 */}
此方法集中了所有数据访问逻辑，使其更容易强制执行一致的数据访问，并减少授权错误的风险。您还可以获得跨请求不同部分共享内存缓存的好处。

```ts filename="data/auth.ts"
import { cache } from 'react'
import { cookies } from 'next/headers'

// Cached helper methods makes it easy to get the same value in many places
// without manually passing it around. This discourages passing it from Server
// Component to Server Component which minimizes risk of passing it to a Client
// Component.
export const getCurrentUser = cache(async () => {
  const token = cookies().get('AUTH_TOKEN')
  const decodedToken = await decryptAndValidate(token)
  // Don't include secret tokens or private information as public fields.
  // Use classes to avoid accidentally passing the whole object to the client.
  return new User(decodedToken.id)
})
```

```tsx filename="data/user-dto.tsx"
import 'server-only'
import { getCurrentUser } from './auth'

function canSeeUsername(viewer: User) {
  // Public info for now, but can change
  return true
}

function canSeePhoneNumber(viewer: User, team: string) {
  // Privacy rules
  return viewer.isAdmin || team === viewer.team
}

export async function getProfileDTO(slug: string) {
  // Don't pass values, read back cached values, also solves context and easier to make it lazy

  // use a database API that supports safe templating of queries
  const [rows] = await sql`SELECT * FROM user WHERE slug = ${slug}`
  const userData = rows[0]

  const currentUser = await getCurrentUser()

  // only return the data relevant for this query and not everything
  // <https://www.w3.org/2001/tag/doc/APIMinimization>
  return {
    username: canSeeUsername(currentUser) ? userData.username : null,
    phonenumber: canSeePhoneNumber(currentUser, userData.team)
      ? userData.phonenumber
      : null,
  }
}
```

```tsx filename="app/page.tsx"
import { getProfile } from '../../data/user'

export async function Page({ params: { slug } }) {
  // This page can now safely pass around this profile knowing
  // that it shouldn't contain anything sensitive.
  const profile = await getProfile(slug);
  ...
}
```

{/* md5:bb705d4b831d579db85008e12c4fe80a */}
> **须知：** 秘密密钥应存储在环境变量中，但只有数据访问层 (DAL) 应该访问 `process.env`。这可以防止秘密暴露给应用程序的其他部分。

{/* md5:733cb9070bac2a4fb70488a16d551654 */}
### 组件级数据访问

{/* md5:5a62cfed0273d03a5becd9a8150ea013 */}
对于快速原型和迭代，数据库查询可以直接放置在服务器组件中。

{/* md5:8ed6e1b2fbb5537302458dd7f05fa3c4 */}
然而，这种方法更容易意外地将私有数据暴露给客户端，例如：

```tsx filename="app/page.tsx"
import Profile from './components/profile.tsx'

export async function Page({ params: { slug } }) {
  const [rows] = await sql`SELECT * FROM user WHERE slug = ${slug}`
  const userData = rows[0]
  // EXPOSED: This exposes all the fields in userData to the client because
  // we are passing the data from the Server Component to the Client.
  return <Profile user={userData} />
}
```

```tsx filename="app/ui/profile.tsx"
'use client'

// BAD: This is a bad props interface because it accepts way more data than the
// Client Component needs and it encourages server components to pass all that
// data down. A better solution would be to accept a limited object with just
// the fields necessary for rendering the profile.
export default async function Profile({ user }: { user: User }) {
  return (
    <div>
      <h1>{user.name}</h1>
      ...
    </div>
  )
}
```

{/* md5:8d80c0432869106cefddf3cb017ccde0 */}
在将数据传递给客户端组件之前，您应该对其进行清理：

```ts filename="data/user.ts"
import { sql } from './db'

export async function getUser(slug: string) {
  const [rows] = await sql`SELECT * FROM user WHERE slug = ${slug}`
  const user = rows[0]

  // Return only the public fields
  return {
    name: user.name,
  }
}
```

```tsx filename="app/page.tsx"
import { getUser } from '../data/user'
import Profile from './ui/profile'

export default async function Page({
  params: { slug },
}: {
  params: { slug: string }
}) {
  const publicProfile = await getUser(slug)
  return <Profile user={publicProfile} />
}
```

{/* md5:41af972089224fef346685da9ce1554b */}
## 读取数据

{/* md5:f85868811459ceaa76d1011f05eab40c */}
### 从服务器传递数据到客户端

{/* md5:95d6da885d38a67d5f2dbb2ed4739240 */}
在初始加载时，服务器组件和客户端组件都在服务器上运行以生成 HTML。但是，它们在隔离的模块系统中执行。这确保服务器组件可以访问私有数据和 API，而客户端组件不能。

{/* md5:6b1904e8ba27abb06075fccfd87214d1 */}
**服务器组件 (Server Components)：**

{/* md5:6e0994837f9d0942054b1a9f90d988e4 */}
- 仅在服务器上运行。
- 可以安全地访问环境变量、秘密、数据库和内部 API。

{/* md5:0d96b9488e3cb6f5a989e8dfadd05970 */}
**客户端组件 (Client Components)：**

{/* md5:c6aaa0e46673b80b35794733848b04cf */}
- 在预渲染期间在服务器上运行，但必须遵循与在浏览器中运行的代码相同的安全假设。
- 不得访问特权数据或仅服务器模块。

{/* md5:92a5929110f2563fbdf396bdf2e386da */}
这确保了应用程序默认是安全的，但通过数据获取或传递给组件的方式，仍有可能意外暴露私有数据。

{/* md5:da84ccb6547b912501c709b1478d6ef8 */}
### 污点

{/* md5:b8bd2652c23dd9e2619fd7e2eddba143 */}
为了防止私有数据意外暴露给客户端，您可以使用 React 污点 API：

{/* md5:214f88e43a0e2893af0d5ba6a1f1eaf2 */}
- [`experimental_taintObjectReference`](https://react.dev/reference/react/experimental_taintObjectReference) 用于数据对象。
- [`experimental_taintUniqueValue`](https://react.dev/reference/react/experimental_taintUniqueValue) 用于特定值。

{/* md5:71f1bc27203553106f8c58c8e7234e8b */}
您可以在 `next.config.js` 中通过 [`experimental.taint`](/docs/app/api-reference/config/next-config-js/taint) 选项在您的 Next.js 应用中启用使用：

```js filename="next.config.js"
module.exports = {
  experimental: {
    taint: true,
  },
}
```

{/* md5:447683b6445523c7a5f4af061361c1a0 */}
这可以防止被标记的对象或值被传递给客户端。然而，这是一个额外的保护层，您仍然应该在将数据传递给 React 的渲染上下文之前，在您的 [数据访问层 (DAL)](#data-access-layer) 中对数据进行过滤和清理。

{/* md5:a3f7d1e5ccc8be46223deb45d075221e */}
> **须知：**
>
> - 默认情况下，环境变量仅在服务器上可用。Next.js 会将任何前缀为 `NEXT_PUBLIC_` 的环境变量暴露给客户端。[了解更多](/docs/app/guides/environment-variables)。
> - 默认情况下，函数和类已经被阻止传递给客户端组件。

{/* md5:f452c7567b4d32b6e0661634ab42c5d1 */}
### 防止仅限服务器的代码在客户端执行

{/* md5:4b171ee5496311f66f649a4c92237f25 */}
为了防止仅限服务器的代码在客户端上执行，您可以使用 [`server-only`](https://www.npmjs.com/package/server-only) 包标记模块：

```bash package="npm"
npm install server-only
```

```bash package="yarn"
yarn add server-only
```

```bash package="pnpm"
pnpm add server-only
```

```bash package="bun"
bun add server-only
```

```ts filename="lib/data.ts"
import 'server-only'

//...
```

{/* md5:220f18531bbe3de85e84ca386545f6d2 */}
这确保专有代码或内部业务逻辑留在服务器上，因为如果在客户端环境中导入该模块，会导致构建错误。

{/* md5:7b0b013dc04a35716d5101d710922458 */}
## 修改数据

{/* md5:9bc8934a559d1f7f2e9b813ddd5750d9 */}
Next.js 使用 [服务器操作 (Server Actions)](https://react.dev/reference/rsc/server-functions) 处理变更。

{/* md5:b11eea2e85fed8b1ac32a08777310b81 */}
### 内置服务器操作安全功能

{/* md5:eb29cbf0d3f6d336872670744d794b6f */}
默认情况下，当创建并导出服务器操作时，会创建一个公共 HTTP 端点，应视为具有相同的安全假设和授权检查。这意味着，即使服务器操作或工具函数未在其他地方导入，它仍然是公开可访问的。

{/* md5:c45baecfadb32f8a0cd86f7bd522d103 */}
为了提高安全性，Next.js 具有以下内置功能：

{/* md5:2f531f09269644bf741f23aa4459424d */}
- **安全的操作 ID：** Next.js 创建加密的、非确定性的 ID，允许客户端引用和调用服务器操作。这些 ID 会在构建之间定期重新计算，以增强安全性。
- **死代码消除：** 未使用的服务器操作（通过其 ID 引用）将从客户端包中移除，以避免公开访问。

{/* md5:fc4a5594c55b0c9e480bc03e3bb1b9a7 */}
> **须知**：
>
> ID 在编译期间创建，并且最多缓存 14 天。当发起新的构建或构建缓存失效时，它们将被重新生成。
> 此安全改进降低了在缺少身份验证层的情况下的风险。但是，你仍然应该像对待公开的 HTTP 端点一样对待服务器操作。

```jsx
// app/actions.js
'use server'

// If this action **is** used in our application, Next.js
// will create a secure ID to allow the client to reference
// and call the Server Action.
export async function updateUserAction(formData) {}

// If this action **is not** used in our application, Next.js
// will automatically remove this code during `next build`
// and will not create a public endpoint.
export async function deleteUserAction(formData) {}
```

{/* md5:47307d9ca3a09d3430c017d3b9c09092 */}
### 验证客户端输入

{/* md5:f07b8a0192db2cedaf59f429476e0f3c */}
您应始终验证来自客户端的输入，因为它们很容易被修改。例如，表单数据、URL 参数、头信息和 searchParams：

```tsx filename="app/page.tsx"
// BAD: Trusting searchParams directly
export default async function Page({ searchParams }) {
  const isAdmin = searchParams.get('isAdmin')
  if (isAdmin === 'true') {
    // Vulnerable: relies on untrusted client data
    return <AdminPanel />
  }
}

// GOOD: Re-verify every time
import { cookies } from 'next/headers'
import { verifyAdmin } from './auth'

export default async function Page() {
  const token = cookies().get('AUTH_TOKEN')
  const isAdmin = await verifyAdmin(token)

  if (isAdmin) {
    return <AdminPanel />
  }
}
```

{/* md5:d4b9cd71d81bf0b736b8a3d60a5bfb5b */}
### 身份验证和授权

{/* md5:56147200fe762d12b217e27e5826a3db */}
您应始终确保用户已获授权执行操作。例如：

```tsx filename="app/actions.ts"
'use server'

import { auth } from './lib'

export function addItem() {
  const { user } = auth()
  if (!user) {
    throw new Error('You must be signed in to perform this action')
  }

  // ...
}
```

{/* md5:52b30f1c1f1bbf46560e6d3943c0934e */}
了解更多关于 Next.js 中的[身份验证](/docs/app/guides/authentication)。

{/* md5:c8fbcd7af872d07786b3a6d5f73406de */}
### 闭包和加密

{/* md5:4263ab5f6e84a952bee8e7beba7aae81 */}
在组件内部定义服务器操作会创建一个[闭包 (closure)](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures)，其中操作可以访问外部函数的作用域。例如，`publish` 操作可以访问 `publishVersion` 变量：

```tsx filename="app/page.tsx" switcher
export default async function Page() {
  const publishVersion = await getLatestVersion();

  async function publish() {
    "use server";
    if (publishVersion !== await getLatestVersion()) {
      throw new Error('The version has changed since pressing publish');
    }
    ...
  }

  return (
    <form>
      <button formAction={publish}>Publish</button>
    </form>
  );
}
```

```jsx filename="app/page.js" switcher
export default async function Page() {
  const publishVersion = await getLatestVersion();

  async function publish() {
    "use server";
    if (publishVersion !== await getLatestVersion()) {
      throw new Error('The version has changed since pressing publish');
    }
    ...
  }

  return (
    <form>
      <button formAction={publish}>Publish</button>
    </form>
  );
}
```

{/* md5:bdccbd94d44842b7e04ccb0943877e0e */}
当您需要在渲染时捕获数据的**快照**（例如 `publishVersion`），以便稍后在调用操作时使用，闭包非常有用。

{/* md5:233dd6cb1cbe8e6ceac54dc1fca91f23 */}
然而，为了实现这一点，捕获的变量会被发送到客户端，并在调用操作时返回到服务器。为了防止敏感数据暴露给客户端，Next.js 会自动加密闭包中的变量。每次构建 Next.js 应用程序时，都会为每个操作生成一个新的私钥。这意味着操作只能针对特定的构建调用。

{/* md5:ba2f31c9e329a163433ec2f2426f93c3 */}
> **须知：** 我们不建议仅依靠加密来防止敏感值在客户端暴露。

{/* md5:62107b9e7186e334e232077cf5a34383 */}
### 覆盖加密密钥（高级）

{/* md5:17c4b570f09ffb6adb9a7277ed4c6ff7 */}
在跨多个服务器自托管 Next.js 应用程序时，每个服务器实例最终可能会拥有不同的加密密钥，从而导致潜在的不一致。

{/* md5:944995dc2b8da80d63769bf3bedc327a */}
为了缓解此问题，你可以使用 `process.env.NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` 环境变量覆盖加密密钥。指定此变量可确保你的加密密钥在构建之间保持持久，并且所有服务器实例都使用相同的密钥。此变量**必须**是 AES-GCM 加密的。

{/* md5:7692b7691be4cecfe2439ccd6d2ad8cf */}
这是一个高级用例，其中跨多个部署的一致加密行为对你的应用程序至关重要。你应该考虑标准的安全实践，例如密钥轮换和签名。

{/* md5:f117e7186b75ed9fcdcde9379c07bf93 */}
> **须知：** 部署到 Vercel 的 Next.js 应用程序会自动处理此问题。

{/* md5:bbeda739d35ec9faf6aefee69fa85311 */}
### 允许的源（高级）

{/* md5:3f5398659ef7d2915da4c56d858303de */}
由于服务器操作可以在 `<form>` 元素中调用，这使它们容易受到 [CSRF 攻击](https://developer.mozilla.org/en-US/docs/Glossary/CSRF)。

{/* md5:02d07022988f7590c91f6c82ad9f63b8 */}
在后台，服务器操作使用 `POST` 方法，并且只允许此 HTTP 方法调用它们。这防止了现代浏览器中的大多数 CSRF 漏洞，特别是因为 [SameSite cookies](https://web.dev/articles/samesite-cookies-explained) 是默认设置。

{/* md5:dc081a791bd0d79ec52e2e51c81e0084 */}
作为额外的保护，Next.js 中的服务器操作还会比较 [Origin 头](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Origin)和 [Host 头](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Host)（或 `X-Forwarded-Host`）。如果它们不匹配，请求将被中止。换句话说，服务器操作只能在托管它的页面的同一主机上调用。

{/* md5:205300285f3bf9d6dd4a66082f2edff2 */}
对于使用反向代理或多层后端架构（服务器 API 与生产域不同）的大型应用程序，建议使用配置选项 [`serverActions.allowedOrigins`](/docs/app/api-reference/config/next-config-js/serverActions) 来指定一个安全来源列表。该选项接受一个字符串数组。

```js filename="next.config.js"
/** @type {import('next').NextConfig} */
module.exports = {
  experimental: {
    serverActions: {
      allowedOrigins: ['my-proxy.com', '*.my-proxy.com'],
    },
  },
}
```

{/* md5:50c57f32427eb16b0d162427ce26cafa */}
了解更多关于[安全和服务器操作](https://nextjs.org/blog/security-nextjs-server-components-actions)。

{/* md5:f80111ceb8de048a2d8908347ca0e864 */}
### 避免渲染期间的副作用

{/* md5:6c0d38846ae7eca30bb3ae1434493b99 */}
变更（例如注销用户、更新数据库、使缓存失效）绝不应作为副作用，无论是在服务器组件还是客户端组件中。Next.js 显式阻止在渲染方法中设置 cookie 或触发缓存重新验证，以避免意外的副作用。

```tsx filename="app/page.tsx"
// BAD: Triggering a mutation during rendering
export default async function Page({ searchParams }) {
  if (searchParams.get('logout')) {
    cookies().delete('AUTH_TOKEN')
  }

  return <UserProfile />
}
```

{/* md5:e73969fbba530071e007c06949c22af8 */}
相反，您应该使用服务器操作来处理变更。

```tsx filename="app/page.tsx"
// GOOD: Using Server Actions to handle mutations
import { logout } from './actions'

export default function Page() {
  return (
    <>
      <UserProfile />
      <form action={logout}>
        <button type="submit">Logout</button>
      </form>
    </>
  )
}
```

{/* md5:912585e7e36cfa6b7dc0991235f6735c */}
> **须知：** Next.js 使用 `POST` 请求来处理变更。这防止了 GET 请求的意外副作用，降低了跨站请求伪造 (CSRF) 的风险。

{/* md5:ef080e297c4aa816f9bcf408f340116c */}
## 审计

{/* md5:4d5d51270bdd37f21de4945226f27cda */}
如果您正在审计一个 Next.js 项目，以下是我们建议特别关注的几点：

{/* md5:bcc7f212d93a49cb90cb5c6696f96d7d */}
- **数据访问层：** 是否有孤立的数据访问层的既定做法？验证数据库包和环境变量未在数据访问层之外导入。
- **`"use client"` 文件：** 组件属性是否期望接收私有数据？类型签名是否过于宽泛？
- **`"use server"` 文件：** 操作参数是否在操作内部或数据访问层内验证？用户是否在操作内部重新授权？
- **`/[param]/.`** 带括号的文件夹是用户输入。参数是否经过验证？
- **`middleware.ts` 和 `route.ts`**：具有大量权限。花额外时间使用传统技术审计这些文件。定期进行渗透测试或漏洞扫描，或与团队的软件开发生命周期保持一致。
