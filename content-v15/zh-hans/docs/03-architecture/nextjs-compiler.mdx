---
title: Next.js 编译器
description: 用 Rust 编写的 Next.js 编译器，用于转换和压缩您的 Next.js 应用程序。
---

{/* md5:2c859097c2820c91cf44c9fb74b7bc27 */}
用 Rust 编写并使用 [SWC](https://swc.rs/) 的 Next.js 编译器，允许 Next.js 为生产环境转换和压缩您的 JavaScript 代码。这取代了用于单个文件的 Babel 和用于压缩输出包的 Terser。

{/* md5:ae10600e4962d260ba10eca63ef8253e */}
使用 Next.js 编译器进行编译比 Babel 快 17 倍，并且自 Next.js 12 版起默认启用。如果您有现有的 Babel 配置或正在使用 [不支持的功能](#unsupported-features)，您的应用程序将退出 Next.js 编译器并继续使用 Babel。

{/* md5:9aae3160708ba98a215c4b67287a8d8f */}
## 为什么选择 SWC？

{/* md5:e224441823cc9fe1a38e79a70a2938c8 */}
[SWC](https://swc.rs/) 是一个可扩展的基于 Rust 的平台，专为下一代快速开发工具而设计。

{/* md5:bc662488f8d2a5424af402458470836c */}
SWC 可用于编译、压缩、打包等操作，并且设计为可扩展。您可以调用它来执行代码转换（无论是内置的还是自定义的）。这些转换是通过 Next.js 等高级工具运行的。

{/* md5:cd91eb130ab9f8797d10249bb147e5e8 */}
我们选择基于 SWC 的原因有几个：

{/* md5:54b45ec36321e4183171e7a293c7d406 */}
- **可扩展性：** SWC 可以作为 Crate 在 Next.js 内部使用，无需 fork 库或绕过设计限制。
- **性能：** 通过切换到 SWC，我们能够在 Next.js 中实现约 3 倍更快的 Fast Refresh 和约 5 倍更快的构建，并且仍有更多优化空间。
- **WebAssembly：** Rust 对 WASM 的支持对于支持所有可能的平台并在任何地方进行 Next.js 开发至关重要。
- **社区：** Rust 社区和生态系统非常棒且仍在不断发展。

{/* md5:67d4ff6d268b6a4cc6f4143253ba9131 */}
## 支持的功能

{/* md5:b873f4b566d727465ce8782fe75edd76 */}
### 样式组件

{/* md5:e87de51fbd801f8b56c67c0ce1f72dbf */}
我们正在将 `babel-plugin-styled-components` 移植到 Next.js 编译器。

{/* md5:55dc169933d12e5fc0d103fa08d4c6fe */}
首先，更新到最新版本的 Next.js：`npm install next@latest`。然后，更新您的 `next.config.js` 文件：

```js filename="next.config.js"
module.exports = {
  compiler: {
    styledComponents: true,
  },
}
```

{/* md5:d21d857edb0fc0d6d437226e436dbcbb */}
对于高级用例，您可以配置样式组件编译的各个属性。

{/* md5:fa41ddf1946bbeae79ba68c2076c31ec */}
> 注意：`ssr` 和 `displayName` 转换是在 Next.js 中使用 `styled-components` 的主要要求。

```js filename="next.config.js"
module.exports = {
  compiler: {
    // see https://styled-components.com/docs/tooling#babel-plugin for more info on the options.
    styledComponents: {
      // Enabled by default in development, disabled in production to reduce file size,
      // setting this will override the default for all environments.
      displayName?: boolean,
      // Enabled by default.
      ssr?: boolean,
      // Enabled by default.
      fileName?: boolean,
      // Empty by default.
      topLevelImportPaths?: string[],
      // Defaults to ["index"].
      meaninglessFileNames?: string[],
      // Enabled by default.
      minify?: boolean,
      // Enabled by default.
      transpileTemplateLiterals?: boolean,
      // Empty by default.
      namespace?: string,
      // Disabled by default.
      pure?: boolean,
      // Enabled by default.
      cssProp?: boolean,
    },
  },
}
```

{/* md5:5eb768e4caccb30a50710cce8c23f541 */}
### Jest

{/* md5:badb4f0699fce14e3587bc31e40ab2bf */}
Next.js 编译器会转换您的测试，并简化与 Next.js 一起配置 Jest 的操作，包括：

{/* md5:01f896d8907f317d9da89b70052a4c52 */}
- 自动模拟 `.css`、`.module.css`（及其 `.scss` 变体）和图像导入
- 使用 SWC 自动设置 `transform`
- 将 `.env`（及其所有变体）加载到 `process.env`
- 从测试解析和转换中忽略 `node_modules`
- 从测试解析中忽略 `.next`
- 加载 `next.config.js` 以获取启用实验性 SWC 转换的标志

{/* md5:c4559bc0ff7a23aa1a819c041bf996a2 */}
首先，更新到最新版本的 Next.js：`npm install next@latest`。然后，更新您的 `jest.config.js` 文件：

```js filename="jest.config.js"
const nextJest = require('next/jest')

// Providing the path to your Next.js app which will enable loading next.config.js and .env files
const createJestConfig = nextJest({ dir: './' })

// Any custom config you want to pass to Jest
const customJestConfig = {
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
}

// createJestConfig is exported in this way to ensure that next/jest can load the Next.js configuration, which is async
module.exports = createJestConfig(customJestConfig)
```

{/* md5:cec24b93e675b619ced2473a7453cf8b */}
### Relay

{/* md5:5dc009b5a7354b3b3c221103dd3b3114 */}
要启用 [Relay](https://relay.dev/) 支持：

```js filename="next.config.js"
module.exports = {
  compiler: {
    relay: {
      // This should match relay.config.js
      src: './',
      artifactDirectory: './__generated__',
      language: 'typescript',
      eagerEsModules: false,
    },
  },
}
```

{/* md5:10b130febc4eabb58e45648f53f2755a */}
> **须知**：在 Next.js 中，`pages` 目录中的所有 JavaScript 文件都被视为路由。因此，对于 `relay-compiler`，您需要在 `pages` 之外指定 `artifactDirectory` 配置设置，否则 `relay-compiler` 将在源文件旁边的 `__generated__` 目录中生成文件，且该文件将被视为路由，这将破坏生产构建。

{/* md5:8a0efbcda828d9a8318f941fcc1ffef1 */}
### 移除 React 属性

{/* md5:45d72338666294d9aa2071eac228ee9f */}
允许移除 JSX 属性。这通常用于测试。类似于 `babel-plugin-react-remove-properties`。

{/* md5:0041d54e9577cab42af44ea00b2f0187 */}
要移除匹配默认正则表达式 `^data-test` 的属性：

```js filename="next.config.js"
module.exports = {
  compiler: {
    reactRemoveProperties: true,
  },
}
```

{/* md5:6c66cea3174a801c6e538352c4e07a1c */}
要移除自定义属性：

```js filename="next.config.js"
module.exports = {
  compiler: {
    // The regexes defined here are processed in Rust so the syntax is different from
    // JavaScript `RegExp`s. See https://docs.rs/regex.
    reactRemoveProperties: { properties: ['^data-custom$'] },
  },
}
```

{/* md5:98411d1b88bfd0901a6a81425d3e9723 */}
### 移除控制台

{/* md5:a28f6071d0e13e15d893133f364b8286 */}
此转换允许移除应用程序代码（不包括 `node_modules`）中的所有 `console.*` 调用。类似于 `babel-plugin-transform-remove-console`。

{/* md5:b842e87e11862f0942c024ee9270d3cd */}
移除所有 `console.*` 调用：

```js filename="next.config.js"
module.exports = {
  compiler: {
    removeConsole: true,
  },
}
```

{/* md5:98a1c96035856a3ce30d51f57f11b527 */}
移除 `console.*` 输出（`console.error` 除外）：

```js filename="next.config.js"
module.exports = {
  compiler: {
    removeConsole: {
      exclude: ['error'],
    },
  },
}
```

{/* md5:82717a186dfc56c79ef1410fef7ba328 */}
### 传统装饰器

{/* md5:a51e26ea09590900855836ce020882ea */}
Next.js 将自动检测 `jsconfig.json` 或 `tsconfig.json` 中的 `experimentalDecorators`。传统装饰器通常与像 `mobx` 这样的旧版本库一起使用。

{/* md5:bf0abc8fec0c48588f26a4183597c9f9 */}
此标志仅用于支持现有应用程序的兼容性。我们不建议在新应用程序中使用传统装饰器。

{/* md5:55df77d872889c885e6dc6ecc9338383 */}
首先，更新到最新版本的 Next.js：`npm install next@latest`。然后，更新您的 `jsconfig.json` 或 `tsconfig.json` 文件：

```js
{
  "compilerOptions": {
    "experimentalDecorators": true
  }
}
```

{/* md5:edd3f88647c66141d0119495e585191f */}
### importSource

{/* md5:93ef793708f3baac67d2ac5dd54af5b8 */}
Next.js 将自动检测 `jsconfig.json` 或 `tsconfig.json` 中的 `jsxImportSource` 并应用它。这通常与像 [Theme UI](https://theme-ui.com) 这样的库一起使用。

{/* md5:55df77d872889c885e6dc6ecc9338383 */}
首先，更新到最新版本的 Next.js：`npm install next@latest`。然后，更新您的 `jsconfig.json` 或 `tsconfig.json` 文件：

```js
{
  "compilerOptions": {
    "jsxImportSource": "theme-ui"
  }
}
```

{/* md5:601cd5c1063159cf6ced1fda589b6bec */}
### Emotion

{/* md5:06e33c730d0434865252277e71a19486 */}
我们正在将 `@emotion/babel-plugin` 移植到 Next.js 编译器。

{/* md5:55dc169933d12e5fc0d103fa08d4c6fe */}
首先，更新到最新版本的 Next.js：`npm install next@latest`。然后，更新您的 `next.config.js` 文件：

```js filename="next.config.js"

module.exports = {
  compiler: {
    emotion: boolean | {
      // default is true. It will be disabled when build type is production.
      sourceMap?: boolean,
      // default is 'dev-only'.
      autoLabel?: 'never' | 'dev-only' | 'always',
      // default is '[local]'.
      // Allowed values: `[local]` `[filename]` and `[dirname]`
      // This option only works when autoLabel is set to 'dev-only' or 'always'.
      // It allows you to define the format of the resulting label.
      // The format is defined via string where variable parts are enclosed in square brackets [].
      // For example labelFormat: "my-classname--[local]", where [local] will be replaced with the name of the variable the result is assigned to.
      labelFormat?: string,
      // default is undefined.
      // This option allows you to tell the compiler what imports it should
      // look at to determine what it should transform so if you re-export
      // Emotion's exports, you can still use transforms.
      importMap?: {
        [packageName: string]: {
          [exportName: string]: {
            canonicalImport?: [string, string],
            styledBaseImport?: [string, string],
          }
        }
      },
    },
  },
}
```

{/* md5:879642e27da68d8baecf881ef714adc1 */}
### 压缩

{/* md5:1e1f6fb088865d88c89bbcb95ab8e305 */}
自 v13 起，Next.js 的 swc 编译器默认用于压缩。这比 Terser 快 7 倍。

{/* md5:c4390f4987a6c944adcacb7084a5d217 */}
> **须知：** 从 v15 开始，无法使用 `next.config.js` 自定义压缩。对 `swcMinify` 标志的支持已被移除。

{/* md5:e5c9b6036eab54895d24ba9a8c39d3b6 */}
### 模块转译

{/* md5:aa8db8098e8a050f1757296d9219a2bc */}
Next.js 可以自动转换和打包来自本地包（如 monorepos）或外部依赖（`node_modules`）的依赖项。此功能取代了 `next-transpile-modules` 包。

```js filename="next.config.js"
module.exports = {
  transpilePackages: ['@acme/ui', 'lodash-es'],
}
```

{/* md5:764f5be57853aa3869bfb2175d6da952 */}
### 模块化导入

{/* md5:d3c78de49b842b80a368816be25d25f2 */}
此选项已被 Next.js 13.5 中的 [`optimizePackageImports`](/docs/app/api-reference/config/next-config-js/optimizePackageImports) 取代。我们建议升级到新选项，该选项不需要手动配置导入路径。

{/* md5:42ea125365f949e9185040449b55f9dd */}
### 定义（构建期间替换变量）

{/* md5:e1337f5cd4fbd01f67275eba3821418a */}
`define` 选项允许您在构建时静态替换代码中的变量。
该选项接受一个键值对对象，其中键是应替换为相应值的变量。

{/* md5:bd4dc021a6c0b2b3767cf5a7951276d7 */}
使用 `next.config.js` 中的 `compiler.define` 字段为所有环境（server、edge 和 client）定义变量。或者，使用 `compiler.defineServer` 仅为服务器端（server 和 edge）代码定义变量：

```js filename="next.config.js"
module.exports = {
  compiler: {
    define: {
      MY_VARIABLE: 'my-string',
      'process.env.MY_ENV_VAR': 'my-env-var',
    },
    defineServer: {
      MY_SERVER_VARIABLE: 'my-server-var',
    },
  },
}
```

{/* md5:1a0e3569af6f3a9cc099dff85190d12c */}
### 构建生命周期钩子

{/* md5:98ab7a42893f2060fbba84ec646ff505 */}
Next.js 编译器支持生命周期钩子，允许您在构建过程中的特定点运行自定义代码。当前支持以下钩子：

{/* md5:8391e355aaa8ff95a12cc9ab02b2a3e9 */}
#### runAfterProductionCompile

{/* md5:31be69c4ccc7a32ba12bf1a8ffdeb146 */}
在生产构建编译完成后、运行类型检查和静态页面生成等编译后任务之前执行的钩子函数。此钩子提供对项目元数据的访问，包括项目目录和构建输出目录，因此第三方工具可用于收集构建输出（如 sourcemaps）。

```js filename="next.config.js"
module.exports = {
  compiler: {
    runAfterProductionCompile: async ({ distDir, projectDir }) => {
      // Your custom code here
    },
  },
}
```

{/* md5:0e652f71be038855d5f6b159fed0072e */}
钩子接收一个包含以下属性的对象：

{/* md5:c8582dcadeae7da63849ecc6362fb4bf */}
- `distDir`：构建输出目录（默认为 `.next`）
- `projectDir`：项目的根目录

{/* md5:934a45fa4a1c1a296b381e5529e32a6b */}
## 实验性功能

{/* md5:2a2c1ccf72e5ae55145d34bd447e9300 */}
### SWC 跟踪分析

{/* md5:d53a35698f264a8c742ed71ecc975b66 */}
您可以生成 SWC 的内部转换跟踪，格式为 Chromium 的 [track event format](https://docs.google.com/document/d/1CvAClvFfyA5R-PhYUmn5OOQtYMH4h6I0nSsKchNAySU/preview?mode=html#%21=)。

```js filename="next.config.js"
module.exports = {
  experimental: {
    swcTraceProfiling: true,
  },
}
```

{/* md5:f52260e780a2adfc7f22e00135c1c107 */}
启用后，swc 将在 `.next/` 下生成名为 `swc-trace-profile-${timestamp}.json` 的跟踪。Chromium 的跟踪查看器 (chrome://tracing/, https://ui.perfetto.dev/) 或兼容的火焰图查看器 (https://www.speedscope.app/) 可以加载并可视化生成的跟踪。

{/* md5:f43f0bc0d85aced5af46565ee4baa7e7 */}
### SWC 插件（实验性）

{/* md5:6ded96b8a1d895347e125fd549ad7f85 */}
您可以将 swc 的转换配置为使用用 wasm 编写的 SWC 实验性插件支持来自定义转换行为。

```js filename="next.config.js"
module.exports = {
  experimental: {
    swcPlugins: [
      [
        'plugin',
        {
          ...pluginOptions,
        },
      ],
    ],
  },
}
```

{/* md5:8f3f7dc11735a43f67e4bfd749bec3ba */}
`swcPlugins` 接受一个元组数组来配置插件。插件的元组包含插件路径和插件配置对象。插件路径可以是 npm 模块包名称或 `.wasm` 二进制文件的绝对路径。

{/* md5:624f4348d31228db3defd0a919f8c0bc */}
## 不支持的功能

{/* md5:48e669e3ab4deeea930a326f19cc7e90 */}
当您的应用程序具有 `.babelrc` 文件时，Next.js 将自动回退到使用 Babel 转换单个文件。这确保了对利用自定义 Babel 插件的现有应用程序的向后兼容性。

{/* md5:3ccdfd5bdd8b7c5c453aafde04fac393 */}
如果您正在使用自定义 Babel 设置，[请分享您的配置](https://github.com/vercel/next.js/discussions/30174)。我们正在努力移植尽可能多的常用 Babel 转换，并计划在未来支持插件。

{/* md5:bb3b99be8f27f7a60348b868bf1c7f52 */}
## 版本历史

{/* md5:cf0f1ce6073ada3cc6166b525958722a */}
| 版本 | 变更 |
| ---- | ---- |
| `v13.1.0` | [模块转译](https://nextjs.org/blog/next-13-1#built-in-module-transpilation-stable) 和 [模块化导入](https://nextjs.org/blog/next-13-1#import-resolution-for-smaller-bundles) 稳定版。 |
| `v13.0.0` | SWC 压缩器默认启用。 |
| `v12.3.0` | SWC 压缩器 [稳定版](https://nextjs.org/blog/next-12-3#swc-minifier-stable)。 |
| `v12.2.0` | 添加了 [SWC 插件](#swc-plugins-experimental) 实验性支持。 |
| `v12.1.0` | 添加了对样式组件、Jest、Relay、移除 React 属性、传统装饰器、移除控制台和 jsxImportSource 的支持。 |
| `v12.0.0` | Next.js 编译器 [推出](https://nextjs.org/blog/next-12)。 |
