---
title: 建立 Next.js 應用程式 (create-next-app)
description: 使用 create-next-app CLI 透過單一命令建立 Next.js 應用程式。
---

{/* 此文件內容在應用程式路由器和頁面路由器之間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於頁面路由器的內容。任何共享內容都不應包裹在元件中。 */}

`create-next-app` CLI 允許您使用預設範本或來自公共 GitHub 存放庫的 [範例](https://github.com/vercel/next.js/tree/canary/examples) 建立新的 Next.js 應用程式。這是開始使用 Next.js 最簡單的方法。

基本用法：

```bash filename="Terminal"
npx create-next-app@latest [project-name] [options]
```

## 參考

下列選項可用：

| 選項                                      | 說明                                                |
| --------------------------------------- | --------------------------------------------------------------- |
| `-h` or `--help`                        | 顯示所有可用選項                                      |
| `-v` or `--version`                     | 輸出版本號碼                                       |
| `--no-*`                                | 否定預設選項。例如：`--no-ts`                          |
| `--ts` or `--typescript`                | 設定為 TypeScript 專案（預設）                    |
| `--js` or `--javascript`                | 設定為 JavaScript 專案                              |
| `--tailwind`                            | 使用 Tailwind CSS 設定進行設定（預設）                   |
| `--eslint`                              | 使用 ESLint 設定進行設定                                   |
| `--biome`                               | 使用 Biome 設定進行設定                                    |
| `--no-linter`                           | 跳過程式碼檢查器設定                                       |
| `--app`                                 | 設定以 App Router 專案                             |
| `--api`                                 | 建立僅包含路由處理程式的專案                   |
| `--src-dir`                             | 在 `src/` 目錄內部進行設定                            |
| `--turbopack`                           | 預設在開發時啟用 Turbopack                     |
| `--import-alias <alias-to-configure>`   | 指定要使用的匯入別名（預設 "@/\*"）                    |
| `--empty`                               | 建立空白的專案                                     |
| `--use-npm`                             | 明確告知 CLI 使用 npm 建立應用程式  |
| `--use-pnpm`                            | 明確告知 CLI 使用 pnpm 建立應用程式 |
| `--use-yarn`                            | 明確告知 CLI 使用 Yarn 建立應用程式 |
| `--use-bun`                             | 明確告知 CLI 使用 Bun 建立應用程式  |
| `-e` or `--example [name] [github-url]` | 要使用來建立應用程式的範例                            |
| `--example-path <path-to-example>`      | 分別指定範例的路徑                      |
| `--reset-preferences`                   | 明確告知 CLI 還原已儲存的使用者設定         |
| `--skip-install`                        | 明確告知 CLI 跳過套件安裝             |
| `--disable-git`                         | 明確告知 CLI 停用 Git 初始化           |
| `--yes`                                 | 針對所有選項使用先前設定或預設值            |

## 範例

### 使用預設範本

要使用預設範本建立新應用程式，請在終端機中執行以下命令：

```bash filename="Terminal"
npx create-next-app@latest
```

接著您將收到以下提示：

```txt filename="Terminal"
What is your project named?  my-app
Would you like to use TypeScript?  No / Yes
Which linter would you like to use?  ESLint / Biome / None
Would you like to use Tailwind CSS?  No / Yes
Would you like your code inside a `src/` directory?  No / Yes
Would you like to use App Router? (recommended)  No / Yes
Would you like to use Turbopack? (recommended)  No / Yes
Would you like to customize the import alias (`@/*` by default)?  No / Yes
```

### 程式碼檢查器 (Linter) 選項

**ESLint**: 傳統且最受歡迎的 JavaScript 程式碼檢查器。包含來自 `eslint-plugin-next` 的 Next.js 特定規則。

**Biome**: 快速現代的程式碼檢查器與格式化工具，結合 ESLint 和 Prettier 的功能。內建 Next.js 和 React 領域支援，以達到最佳效能。

**無**: 完全跳過程式碼檢查器設定。您隨時都可以稍後新增程式碼檢查器。

> **注意**: `next lint` 命令將在 Next.js 16 中棄用。新專案應直接使用所選的程式碼檢查器（例如：`biome check` 或 `eslint`）。

一旦您回答了這些提示，就會根據您選擇的設定建立新專案。

### 使用官方 Next.js 範例

要使用官方 Next.js 範例建立新應用程式，請使用 `--example` 標誌。例如：

```bash filename="Terminal"
npx create-next-app@latest --example [example-name] [your-project-name]
```

您可以在 [Next.js 存放庫](https://github.com/vercel/next.js/tree/canary/examples) 中查看所有可用範例的清單以及設定指令。

### 使用任何公共 GitHub 範例

要使用任何公共 GitHub 範例建立新應用程式，請使用 `--example` 選項並附上 GitHub 存放庫的 URL。例如：

```bash filename="Terminal"
npx create-next-app@latest --example "https://github.com/.../" [your-project-name]
```
