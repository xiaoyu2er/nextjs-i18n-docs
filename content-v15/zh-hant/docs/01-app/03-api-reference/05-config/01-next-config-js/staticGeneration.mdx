---
title: staticGeneration*
description: 學習如何在您的 Next.js 應用程式中配置靜態生成 (Static Generation)。
version: 實驗性
---

`staticGeneration*` 選項允許您為進階使用案例配置靜態生成 (Static Generation) 流程。

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
const nextConfig = {
  experimental: {
    staticGenerationRetryCount: 1,
    staticGenerationMaxConcurrency: 8,
    staticGenerationMinPagesPerWorker: 25,
  },
}

export default nextConfig
```

## 配置選項

下列選項可用：

- `staticGenerationRetryCount`: 在建置失敗之前，嘗試重新生成失敗頁面的次數。
- `staticGenerationMaxConcurrency`: 每個 worker 處理頁面的最大數量。
- `staticGenerationMinPagesPerWorker`: 在啟動新 worker 之前，必須處理的最小頁面數量。
