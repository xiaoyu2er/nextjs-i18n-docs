---
title: TypeScript
description: 預設情況下，Next.js 會報告 TypeScript 錯誤。請在此處了解如何停用此行為。
---

{/* 此文件內容在應用程式路由器和頁面路由器之間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於頁面路由器的內容。任何共享內容都不應包裹在元件中。 */}

Next.js 在您的專案中存在 TypeScript 錯誤時，會失敗您的**生產建置**（`next build`）。

如果您希望在應用程式有錯誤的情況下仍危險地產出生產代碼，您可以停用內建的類型檢查步驟。

若停用，請確保您正在建置或部署流程中執行類型檢查，否則這可能會非常危險。

開啟 `next.config.js` 並在 `typescript` 配置中啟用 `ignoreBuildErrors` 選項：

```js filename="next.config.js"
module.exports = {
  typescript: {
    // !! WARN !!
    // Dangerously allow production builds to successfully complete even if
    // your project has type errors.
    // !! WARN !!
    ignoreBuildErrors: true,
  },
}
```
