---
title: TypeScript
description: Next.js 提供以 TypeScript 為主的開發體驗，用於建置您的 React 應用程式。
---

{/* 此文件內容在應用程式路由器和頁面路由器之間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於頁面路由器的內容。任何共享內容都不應包裹在元件中。 */}

Next.js 內建 TypeScript，使用 `create-next-app` 建立新專案時，會自動安裝必要的套件並設定正確的設定。

要在現有專案中新增 TypeScript，請將檔案重新命名為 `.ts` / `.tsx`。執行 `next dev` 和 `next build` 以自動安裝必要的相依套件，並新增帶有建議設定選項的 `tsconfig.json` 檔案。

> **值得注意**: 如果您已經有 `jsconfig.json` 檔案，請從舊的 `jsconfig.json` 複製 `paths` 編譯器選項到新 `tsconfig.json` 檔案中，並刪除舊的 `jsconfig.json` 檔案。

<AppOnly>

## IDE 外掛程式

Next.js 包含自定义 TypeScript 外掛程式和類型檢查器，VSCode 和其他程式碼編輯器可用於進階類型檢查和自動完成。

您可以在 VS Code 中透過以下方式啟用外掛程式：

1. 開啟指令選單 (`Ctrl/⌘` + `Shift` + `P`)
2. 搜尋 "TypeScript: Select TypeScript Version"
3. 選擇 "Use Workspace Version"

<Image
  alt="TypeScript 指令選單"
  srcLight="/docs/light/typescript-command-palette.png"
  srcDark="/docs/dark/typescript-command-palette.png"
  width="1600"
  height="637"
/>

現在，當編輯檔案時，將啟用自訂外掛程式。執行 `next build` 時，將使用自訂類型檢查器。

TypeScript 外掛程式可協助以下事項：

- 傳遞無效的 [區塊設定選項](/docs/app/api-reference/file-conventions/route-segment-config) 值時會發出警告。
- 顯示可用選項及上下文中說明文件。
- 確保 `'use client'` 指令使用正確。
- 確保客戶端掛鉤（如 `useState`）僅在 Client Components 中使用。

> **🎥 觀看：** 深入了解內建 TypeScript 外掛程式 → [YouTube (3 分鐘)](https://www.youtube.com/watch?v=pqMqn9fKEf8)

## 端到端類型安全性

Next.js App Router 具有**增強的類型安全性**。這包括：

1. **獲取函式與頁面之間的資料不再需要序列化**：您可以在伺服器的組件、佈局和頁面中直接 `fetch`。此資料_不需要_被序列化（轉換為字串）即可傳遞至客戶端供 React 使用。相反地，由於 `app` 預設使用伺服器組件，因此我們可以無需額外的步驟直接使用 `Date`、`Map`、`Set` 等值。過去，您需要使用 Next.js 專屬類型來手動設定伺服器與客戶端之間的邊界。
2. **簡化組件之間的資料流程**：隨著 `_app` 移除並改用根佈局，現在更容易視覺化組件與頁面之間的資料流程。過去，在個別 `pages` 和 `_app` 之間流動的資料難以類型化，且可能引入令人困惑的錯誤。透過 App Router 中的 [共置資料獲取](/docs/app/getting-started/fetching-data)，此問題已不再存在。

[Next.js 中的資料獲取](/docs/app/getting-started/fetching-data) 現在提供幾乎完整的端到端類型安全性，且無須對您選擇的資料庫或內容提供程式進行規定。

我們能夠按照正常 TypeScript 的預期方式為回應資料進行類型化。例如：

```tsx filename="app/page.tsx" switcher
async function getData() {
  const res = await fetch('https://api.example.com/...')
  // The return value is *not* serialized
  // You can return Date, Map, Set, etc.
  return res.json()
}

export default async function Page() {
  const name = await getData()

  return '...'
}
```

若要達到_完整的端到端類型安全性，這也要求您的資料庫或內容提供者支援 TypeScript。這可以透過使用 [ORM](https://en.wikipedia.org/wiki/Object%E2%80%93relational_mapping) 或型別安全查詢建構器來實現。

## 路由感知類型輔助功能

Next.js 為 App Router 路由類型產生全域輔助功能。這些輔助功能無需匯入即可使用，並在 `next dev`、`next build` 或透過 [`next typegen`](/docs/app/api-reference/cli/next#next-typegen-options) 時產生：

- [`PageProps`](/docs/app/api-reference/file-conventions/page#page-props-helper)
- [`LayoutProps`](/docs/app/api-reference/file-conventions/layout#layout-props-helper)
- [`RouteContext`](/docs/app/api-reference/file-conventions/route#route-context-helper)

</AppOnly>

## 範例

### 型別檢查 `next.config.ts`

您可以使用 `next.config.ts` 在 Next.js 設定中使用 TypeScript 並匯入型別。

```ts filename="next.config.ts"
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig
```

> **值得注意**: Module resolution in `next.config.ts` is currently limited to `CommonJS`. This may cause incompatibilities with ESM only packages being loaded in `next.config.ts`.

```ts filename="tsconfig.build.json"
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "useUnknownInCatchVariables": false
  }
}
```

這可透過 `tsconfig.json` 保持編輯器嚴謹，同時允許生產環境建置使用較寬鬆的設定。

</details>

> **Good to know**:
>
> - IDEs typically read `tsconfig.json` for diagnostics and IntelliSense, so you can still see IDE warnings while production builds use the alternate config. Mirror critical options if you want parity in the editor.
> - In development, only `tsconfig.json` is watched for changes. If you edit a different file name via `typescript.tsconfigPath`, restart the dev server to apply changes.
> - The configured file is used in `next dev`, `next build`, `next lint`, and `next typegen`.

> **Good to know**:
>
> - IDEs typically read `tsconfig.json` for diagnostics and IntelliSense, so you can still see IDE warnings while production builds use the alternate config. Mirror critical options if you want parity in the editor.
> - In development, only `tsconfig.json` is watched for changes. If you edit a different file name via `typescript.tsconfigPath`, restart the dev server to apply changes.
> - The configured file is used in `next dev`, `next build`, `next lint`, and `next typegen`.

### Disabling TypeScript errors in production
### 停用生產環境中的 TypeScript 錯誤

Next.js fails your **production build** (`next build`) when TypeScript errors are present in your project.
Next.js 會導致您的**生產環境建置** (`next build`) 失敗，因為專案中存在 TypeScript 錯誤。

If you'd like Next.js to dangerously produce production code even when your application has errors, you can disable the built-in type checking step.
如果您希望即使應用程式存在錯誤，Next.js 也能危險地產生生產環境程式碼，您可以停用內建類型檢查步驟。

If disabled, be sure you are running type checks as part of your build or deploy process, otherwise this can be very dangerous.
如果已停用，請確保您正在作為建置或部署流程的一部分執行類型檢查，否則這可能會非常危險。

Open `next.config.ts` and enable the `ignoreBuildErrors` option in the `typescript` config:
開啟 `next.config.ts` 並在 `typescript` 設定中啟用 `ignoreBuildErrors` 選項：

```ts filename="next.config.ts"
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  typescript: {
    // !! WARN !!
    // Dangerously allow production builds to successfully complete even if
    // your project has type errors.
    // !! WARN !!
    ignoreBuildErrors: true,
  },
}

export default nextConfig
```

> **Good to know**: You can run `tsc --noEmit` to check for TypeScript errors yourself before building. This is useful for CI/CD pipelines where you'd like to check for TypeScript errors before deploying.
> **值得注意**：您可以在建置前執行 `tsc --noEmit` 自行檢查 TypeScript 錯誤。這對於 CI/CD 流程非常有幫助，您可以在部署前檢查 TypeScript 錯誤。

### Custom type declarations
### 自訂型別宣告

When you need to declare custom types, you might be tempted to modify `next-env.d.ts`. However, this file is automatically generated, so any changes you make will be overwritten. Instead, you should create a new file, let's call it `new-types.d.ts`, and reference it in your `tsconfig.json`:
當您需要宣告自訂型別時，您可能會想要修改 `next-env.d.ts`。然而，此檔案會自動產生，因此您所做的任何變更都可能被覆蓋。相反地，您應該建立一個新檔案，我們稱之為 `new-types.d.ts`，並在您的 `tsconfig.json` 中引用它：

```json filename="tsconfig.json"
{
  "compilerOptions": {
    "skipLibCheck": true
    //...truncated...
  },
  "include": [
    "new-types.d.ts",
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": ["node_modules"]
}
```

## 版本變更

| Version   | Changes                                                                                                                              |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `v15.0.0` | [`next.config.ts`](#type-checking-nextconfigts) support added for TypeScript projects.                                               |
| `v13.2.0` | Statically typed links are available in beta.                                                                                        |
| `v12.0.0` | [SWC](/docs/architecture/nextjs-compiler) is now used by default to compile TypeScript and TSX for faster builds.                    |
| `v10.2.1` | [Incremental type checking](https://www.typescriptlang.org/tsconfig#incremental) support added when enabled in your `tsconfig.json`. |