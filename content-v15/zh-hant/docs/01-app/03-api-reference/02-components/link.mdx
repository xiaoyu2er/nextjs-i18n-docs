---
title: Link 元件
description: 使用內建的 `next/link` 元件啟用快速的前端 (client-side) 導航。
---

{/* 此文件內容在應用程式路由器和頁面路由器之間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於頁面路由器的內容。任何共享內容都不應包裹在元件中。 */}

`Link` 元件是延伸到 HTML `<a>` 元素的 React 元件，可為路由間提供 [預取 (prefetching)](/docs/app/getting-started/linking-and-navigating#prefetching) 和前端 (client-side) 導航。這是 Next.js 中路由之間導航的主要方式。

基本用法：

<AppOnly>

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return <Link href="/dashboard">Dashboard</Link>
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return <Link href="/dashboard">Dashboard</Link>
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return <Link href="/dashboard">Dashboard</Link>
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return <Link href="/dashboard">Dashboard</Link>
}
```

</PagesOnly>

## 參考

屬性可以傳遞給 `<Link>` 元件：

<PagesOnly>

| 屬性                                | 範例                  | 類型              | 必要   |
| ----------------------------------- | ---------------------- | ----------------- | ------ |
| [`href`](#href-required)            | `href="/dashboard"`    | String or Object  | 是     |
| [`as`](#as)                         | `as="/post/abc"`       | String or Object  | -      |
| [`replace`](#replace)               | `replace={false}`      | Boolean           | -      |
| [`scroll`](#scroll)                 | `scroll={false}`       | Boolean           | -      |
| [`prefetch`](#prefetch)             | `prefetch={false}`     | Boolean           | -      |
| [`legacyBehavior`](#legacybehavior) | `legacyBehavior={true}`| Boolean           | -      |
| [`passHref`](#passhref)             | `passHref={true}`      | Boolean           | -      |
| [`shallow`](#shallow)               | `shallow={false}`      | Boolean           | -      |
| [`locale`](#locale)                 | `locale="fr"`          | String or Boolean | -      |
| [`onNavigate`](#onnavigate)         | `onNavigate={(e) => {}}`| Function          | -      |

</PagesOnly>

<AppOnly>

| 屬性                        | 範例                  | 類型             | 必要 |
| --------------------------- | ---------------------- | ---------------- | ---- |
| [`href`](#href-required)    | `href="/dashboard"`    | String or Object | 是   |
| [`replace`](#replace)       | `replace={false}`      | Boolean          | -    |
| [`scroll`](#scroll)         | `scroll={false}`       | Boolean          | -    |
| [`prefetch`](#prefetch)     | `prefetch={false}`     | Boolean or null  | -    |
| [`onNavigate`](#onnavigate) | `onNavigate={(e) => {}}`| Function         | -    |

</AppOnly>

> **小知識**：`<a>` 標籤屬性如 `className` 或 `target="_blank"` 可以作為屬性加入 `<Link>`，並將傳遞給底層 `<a>` 元素。

### `href` (必要)

導航的路徑或 URL。

<AppOnly>

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

// Navigate to /about?name=test
export default function Page() {
  return (
    <Link
      href={{
        pathname: '/about',
        query: { name: 'test' },
      }}
    >
      About
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

// Navigate to /about?name=test
export default function Page() {
  return (
    <Link
      href={{
        pathname: '/about',
        query: { name: 'test' },
      }}
    >
      About
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

// Navigate to /about?name=test
export default function Home() {
  return (
    <Link
      href={{
        pathname: '/about',
        query: { name: 'test' },
      }}
    >
      About
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

// Navigate to /about?name=test
export default function Home() {
  return (
    <Link
      href={{
        pathname: '/about',
        query: { name: 'test' },
      }}
    >
      About
    </Link>
  )
}
```

</PagesOnly>

### `replace` (預設值為 false。)

預設為 `false`。當 `true` 時，`next/link` 將取代目前的歷史記錄狀態，而不是將新的 URL 加入 [瀏覽器的歷史記錄堆疊](https://developer.mozilla.org/docs/Web/API/History_API) 中。

<AppOnly>

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" replace>
      Dashboard
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" replace>
      Dashboard
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" replace>
      Dashboard
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" replace>
      Dashboard
    </Link>
  )
}
```

</PagesOnly>

### `scroll` (預設值為 true。)

Next.js 中的 `<Link>` 預設滾動行為 **是保持滾動位置**，類似瀏覽器處理後退和前進導航的方式。當您導航到新的 [頁面](/docs/app/api-reference/file-conventions/page) 時，只要頁面在視口內，滾動位置將保持不變。但是，如果頁面不在視口中，Next.js 將滾動到第一個頁面元素的頂部。

當 `scroll = {false}` 時，Next.js 將不嘗試滾動到第一個頁面元素。

> **小知識**：Next.js 會在管理滾動行為之前檢查 `scroll: false`。如果啟用了滾動，它會識別導航相關的 DOM 節點並檢查每個頂層元素。所有無法滾動的元素和那些沒有渲染 HTML 的元素都會被跳過，這包括固定或置位元素，以及非可見元素（如那些使用 `getBoundingClientRect` 計算的元素）。接著 Next.js 會遍歷兄弟節點，直到識別出視口中可見的滾動元素。

<AppOnly>

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" scroll={false}>
      Dashboard
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" scroll={false}>
      Dashboard
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" scroll={false}>
      Dashboard
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" scroll={false}>
      Dashboard
    </Link>
  )
}
```

</PagesOnly>

### `prefetch`

<AppOnly>

預取操作會發生在 `<Link />` 元件進入使用者視口時（初始時或透過滾動）。Next.js 會在背景中預取並載入所連結的路由（由 `href` 標記）及其資料，以改善前端導航的效能。如果使用者將滑鼠游標懸停在 `<Link />` 上時預取的資料已過期，Next.js 將嘗試再次預取它。**僅在生產環境中啟用預取**。

以下值可以傳遞給 `prefetch` 屬性：

- **`"auto"` 或 `null`（預設）**：預取行為取決於路由是静态還是動態。對於靜態路由，完整的預取路由（包括所有資料）。對於動態路由，預取將向下預取到具有 [`loading.js`](/docs/app/api-reference/file-conventions/loading#instant-loading-states) 邊界的最近段的動態路線。
- `true`：預取將為靜態和動態路由預取完整的路由。
- `false`：無論是進入視口還是懸停，都不會發生預取。

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" prefetch={false}>
      Dashboard
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/dashboard" prefetch={false}>
      Dashboard
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

預取操作會發生在 `<Link />` 元件進入使用者視口時（初始時或透過滾動）。Next.js 會在背景中預取並載入所連結的路由（由 `href` 標記）和資料，以改善前端導航的效能。**僅在生產環境中啟用預取**。

以下值可以傳遞給 `prefetch` 屬性：

- **`true`（預設）**：完整的預取路由及其資料將被預取。
- `false`：進入視口時不會預取，但懸停時會。如果您也想完全移除懸停時的預取，請考慮使用 `<a>` 標籤或 [逐步採用](/docs/app/guides/migrating/app-router-migration) App Router，它允許停用懸停預取。

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" prefetch={false}>
      Dashboard
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" prefetch={false}>
      Dashboard
    </Link>
  )
}
```

### `legacyBehavior` (已棄用)

> **警告**：`legacyBehavior` 屬性將在 Next.js v16 中移除。要採用新的 `<Link>` 行為，請移除用作 `<Link>` 子元素的任何 `<a>` 標籤。[提供代碼變更工具](/docs/app/guides/upgrading/codemods#new-link) 可協助您自動升級您的程式碼庫。

自第 13 版以來，不再需要 `<a>` 元素作為 `<Link>` 元件的子元素。如果您出於相容性原因仍然需要舊行為，可以新增 `legacyBehavior` 屬性。

> **小知識**：當 `legacyBehavior` 未設定為 `true` 時，所有 [`anchor`](https://developer.mozilla.org/docs/Web/HTML/Element/a) 標籤屬性也可以像 `className`、`onClick` 等一樣傳遞給 `next/link`。

### `passHref`

強制 `Link` 將 `href` 屬性傳送至其子元素。預設值為 `false`。請參閱 [傳遞函式元件](#nesting-a-functional-component) 範例以取得更多資訊。

### `shallow`

更新目前頁面的路徑，而不重新執行 [`getStaticProps`](/docs/pages/building-your-application/data-fetching/get-static-props)、[`getServerSideProps`](/docs/pages/building-your-application/data-fetching/get-server-side-props) 或 [`getInitialProps`](/docs/pages/api-reference/functions/get-initial-props)。預設值為 `false`。

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" shallow={false}>
      Dashboard
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/dashboard" shallow={false}>
      Dashboard
    </Link>
  )
}
```

### `locale` (語言)

預設行為會自動前置當前區域設定。`locale` 允許提供不同的語言。當設定為 `false` 時，`href` 必須包含區域設定，因為預設行為已停用。

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <>
      {/* Default behavior: locale is prepended */}
      <Link href="/dashboard">Dashboard (with locale)</Link>

      {/* Disable locale prepending */}
      <Link href="/dashboard" locale={false}>
        Dashboard (without locale)
      </Link>

      {/* Specify a different locale */}
      <Link href="/dashboard" locale="fr">
        Dashboard (French)
      </Link>
    </>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <>
      {/* Default behavior: locale is prepended */}
      <Link href="/dashboard">Dashboard (with locale)</Link>

      {/* Disable locale prepending */}
      <Link href="/dashboard" locale={false}>
        Dashboard (without locale)
      </Link>

      {/* Specify a different locale */}
      <Link href="/dashboard" locale="fr">
        Dashboard (French)
      </Link>
    </>
  )
}
```

### `as` (裝飾)

顯示在瀏覽器 URL 列中的路徑的選擇性裝飾器。在 Next.js 9.5.3 之前，這用於動態路由，查看我們的 [以前文件](https://github.com/vercel/next.js/blob/v9.5.2/docs/api-reference/next/link.md#dynamic-routes) 以了解其運作方式。

當此路徑與 `href` 中提供的路徑不同時，將使用先前 `href`/`as` 行為，如 [以前的文件](https://github.com/vercel/next.js/blob/v9.5.2/docs/api-reference/next/link.md#dynamic-routes) 所示。

</PagesOnly>

### `onNavigate` (導航時的事件)

在前端導航期間呼叫的事件處理器。該處理器接收一個包含 `preventDefault()` 方法的事件物件，允許您在需要時取消導航。

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link
      href="/dashboard"
      onNavigate={(e) => {
        // Only executes during SPA navigation
        console.log('Navigating...')

        // Optionally prevent navigation
        // e.preventDefault()
      }}
    >
      Dashboard
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link
      href="/dashboard"
      onNavigate={(e) => {
        // Only executes during SPA navigation
        console.log('Navigating...')

        // Optionally prevent navigation
        // e.preventDefault()
      }}
    >
      Dashboard
    </Link>
  )
}
```

> **小知識**：雖然 `onClick` 和 `onNavigate` 看起來相似，但它們服務於不同的目的。`onClick` 用於所有點擊事件，而 `onNavigate` 僅在前端導航期間執行。一些主要差異：
>
> - 使用修正鍵 (`Ctrl`/`Cmd` + Click) 時，`onClick` 會執行，但 `onNavigate` 不會，因為 Next.js 會阻止新標籤頁的預設導航。
> - 外部網址不會觸發 `onNavigate`，因為它僅適用於前端和同來源導航。
> - 具有 `download` 屬性的連結會與 `onClick` 合作，但不與 `onNavigate` 合作，因為瀏覽器將把該網址視為下載。

## 範例

以下範例展示如何在不同情境中使用 `<Link>` 元件。

<AppOnly>

### 連結至動態路由段

當連結至 [動態段](/docs/app/api-reference/file-conventions/dynamic-routes) 時，您可以使用 [模板字串和插值 (interpolation)](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Template_literals) 來產生連結列表。例如，要產生部落格文章列表：

```tsx filename="app/blog/post-list.tsx" switcher
import Link from 'next/link'

interface Post {
  id: number
  title: string
  slug: string
}

export default function PostList({ posts }: { posts: Post[] }) {
  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>
          <Link href={`/blog/${post.slug}`}>{post.title}</Link>
        </li>
      ))}
    </ul>
  )
}
```

```jsx filename="app/blog/post-list.js" switcher
import Link from 'next/link'

export default function PostList({ posts }) {
  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>
          <Link href={`/blog/${post.slug}`}>{post.title}</Link>
        </li>
      ))}
    </ul>
  )
}
```

### 檢查當前連結

您可以使用 [`usePathname()`](/docs/app/api-reference/functions/use-pathname) 來判斷連結是否為當前連結。例如，要為當前連結新增類別，您可以檢查目前的 `pathname` 是否與連結的 `href` 相符：

```tsx filename="app/ui/nav-links.tsx" switcher
'use client'

import { usePathname } from 'next/navigation'
import Link from 'next/link'

export function Links() {
  const pathname = usePathname()

  return (
    <nav>
      <Link className={`link ${pathname === '/' ? 'active' : ''}`} href="/">
        Home
      </Link>

      <Link
        className={`link ${pathname === '/about' ? 'active' : ''}`}
        href="/about"
      >
        About
      </Link>
    </nav>
  )
}
```

```jsx filename="app/ui/nav-links.js" switcher
'use client'

import { usePathname } from 'next/navigation'
import Link from 'next/link'

export function Links() {
  const pathname = usePathname()

  return (
    <nav>
      <Link className={`link ${pathname === '/' ? 'active' : ''}`} href="/">
        Home
      </Link>

      <Link
        className={`link ${pathname === '/about' ? 'active' : ''}`}
        href="/about"
      >
        About
      </Link>
    </nav>
  )
}
```

</AppOnly>

<PagesOnly>

### 連結至動態路由段

對於 [動態路由段](/docs/pages/building-your-application/routing/dynamic-routes#convention)，使用模板字串來建立連結路徑可能很有用。

例如，您可以產生一列連結至動態路由 `pages/blog/[slug].js`。

```tsx filename="pages/blog/index.tsx" switcher
import Link from 'next/link'

function Posts({ posts }) {
  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>
          <Link href={`/blog/${post.slug}`}>{post.title}</Link>
        </li>
      ))}
    </ul>
  )
}
```

```jsx filename="pages/blog/index.js" switcher
import Link from 'next/link'

function Posts({ posts }) {
  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>
          <Link href={`/blog/${post.slug}`}>{post.title}</Link>
        </li>
      ))}
    </ul>
  )
}

export default Posts
```

</PagesOnly>

### 滾動至 `id`

如果您想在導航時滾動到特定的 `id`，您可以在 URL 後加上 `#` 哈希連結，或者只是將哈希連結傳遞給 `href` 屬性。這之所以可能，是因為 `<Link>` 會渲染至 `<a>` 元素。

```jsx
<Link href="/dashboard#settings">Settings</Link>

// Output
<a href="/dashboard#settings">Settings</a>
```

<AppOnly>

> **小知識**：
>
> - 如果在導航時頁面不在視口中，Next.js 將滾動至 [頁面](/docs/app/api-reference/file-conventions/page)。

</AppOnly>

### 若子項為包覆 `<a>` 標籤的自訂元件

<AppOnly>

如果 `Link` 的子項是包覆 `<a>` 標籤的自訂元件，您必須在 `Link` 上新增 `passHref`。如果您正在使用 [styled-components](https://styled-components.com/) 等程式庫，這是必要的。若無此設定，`<a>` 標籤將沒有 `href` 屬性，這會損害您網站的無障礙性，並可能影響 SEO。如果您正在使用 [ESLint](/docs/app/api-reference/config/eslint)，則內建規則 `next/link-passhref` 可確保正確使用 `passHref`。

</AppOnly>

<PagesOnly>

如果 `Link` 的子項是包覆 `<a>` 標籤的自訂元件，您必須在 `Link` 上新增 `passHref`。如果您正在使用 [styled-components](https://styled-components.com/) 等程式庫，這是必要的。若無此設定，`<a>` 標籤將沒有 `href` 屬性，這會損害您網站的無障礙性，並可能影響 SEO。如果您正在使用 [ESLint](/docs/pages/api-reference/config/eslint)，則內建規則 `next/link-passhref` 可確保正確使用 `passHref`。

</PagesOnly>

```tsx filename="components/nav-link.tsx" switcher
import Link from 'next/link'
import styled from 'styled-components'

// This creates a custom component that wraps an <a> tag
const RedLink = styled.a`
  color: red;
`

function NavLink({ href, name }) {
  return (
    <Link href={href} passHref legacyBehavior>
      <RedLink>{name}</RedLink>
    </Link>
  )
}

export default NavLink
```

```jsx filename="components/nav-link.js" switcher
import Link from 'next/link'
import styled from 'styled-components'

// This creates a custom component that wraps an <a> tag
const RedLink = styled.a`
  color: red;
`

function NavLink({ href, name }) {
  return (
    <Link href={href} passHref legacyBehavior>
      <RedLink>{name}</RedLink>
    </Link>
  )
}

export default NavLink
```

- 如果您正在使用 [emotion](https://emotion.sh/) 的 JSX 語法特徵 (`@jsx jsx`)，即使您直接使用 `<a>` 標籤，也必須使用 `passHref`。
- 該元件支援 `onClick` 屬性以正確觸發導航。

### 嵌套函式元件

如果 `Link` 的子項是函式元件，除了使用 `passHref` 和 `legacyBehavior` 外，您還必須將元件包裹在 [`React.forwardRef`](https://react.dev/reference/react/forwardRef) 中：

<AppOnly>

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'
import React from 'react'

// Define the props type for MyButton
interface MyButtonProps {
  onClick?: React.MouseEventHandler<HTMLAnchorElement>
  href?: string
}

// Use React.ForwardRefRenderFunction to properly type the forwarded ref
const MyButton: React.ForwardRefRenderFunction<
  HTMLAnchorElement,
  MyButtonProps
> = ({ onClick, href }, ref) => {
  return (
    <a href={href} onClick={onClick} ref={ref}>
      Click Me
    </a>
  )
}

// Use React.forwardRef to wrap the component
const ForwardedMyButton = React.forwardRef(MyButton)

export default function Page() {
  return (
    <Link href="/about" passHref legacyBehavior>
      <ForwardedMyButton />
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'
import React from 'react'

// `onClick`, `href`, and `ref` need to be passed to the DOM element
// for proper handling
const MyButton = React.forwardRef(({ onClick, href }, ref) => {
  return (
    <a href={href} onClick={onClick} ref={ref}>
      Click Me
    </a>
  )
})

// Add a display name for the component (useful for debugging)
MyButton.displayName = 'MyButton'

export default function Page() {
  return (
    <Link href="/about" passHref legacyBehavior>
      <MyButton />
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'
import React from 'react'

// Define the props type for MyButton
interface MyButtonProps {
  onClick?: React.MouseEventHandler<HTMLAnchorElement>
  href?: string
}

// Use React.ForwardRefRenderFunction to properly type the forwarded ref
const MyButton: React.ForwardRefRenderFunction<
  HTMLAnchorElement,
  MyButtonProps
> = ({ onClick, href }, ref) => {
  return (
    <a href={href} onClick={onClick} ref={ref}>
      Click Me
    </a>
  )
}

// Use React.forwardRef to wrap the component
const ForwardedMyButton = React.forwardRef(MyButton)

export default function Home() {
  return (
    <Link href="/about" passHref legacyBehavior>
      <ForwardedMyButton />
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'
import React from 'react'

// `onClick`, `href`, and `ref` need to be passed to the DOM element
// for proper handling
const MyButton = React.forwardRef(({ onClick, href }, ref) => {
  return (
    <a href={href} onClick={onClick} ref={ref}>
      Click Me
    </a>
  )
})

// Add a display name for the component (useful for debugging)
MyButton.displayName = 'MyButton'

export default function Home() {
  return (
    <Link href="/about" passHref legacyBehavior>
      <MyButton />
    </Link>
  )
}
```

</PagesOnly>

<PagesOnly>

### 傳遞 URL 物件

`Link` 也可以接收 URL 物件，並且它會自動格式化它以建立 URL 字串：

```tsx filename="pages/index.ts" switcher
import Link from 'next/link'

function Home() {
  return (
    <ul>
      <li>
        <Link
          href={{
            pathname: '/about',
            query: { name: 'test' },
          }}
        >
          About us
        </Link>
      </li>
      <li>
        <Link
          href={{
            pathname: '/blog/[slug]',
            query: { slug: 'my-post' },
          }}
        >
          Blog Post
        </Link>
      </li>
    </ul>
  )
}

export default Home
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

function Home() {
  return (
    <ul>
      <li>
        <Link
          href={{
            pathname: '/about',
            query: { name: 'test' },
          }}
        >
          About us
        </Link>
      </li>
      <li>
        <Link
          href={{
            pathname: '/blog/[slug]',
            query: { slug: 'my-post' },
          }}
        >
          Blog Post
        </Link>
      </li>
    </ul>
  )
}

export default Home
```

上述範例包含一個連結至：

- 預先定義的路由：`/about?name=test`
- [動態路由](/docs/pages/building-your-application/routing/dynamic-routes#convention)：`/blog/my-post`

您可以使用 [Node.js URL 模組記錄](https://nodejs.org/api/url.html#url_url_strings_and_url_objects) 中定義的每項屬性。

</PagesOnly>

### 使用 replace 取代 url 而非 push

`Link` 元件的預設行為是 `push` 新的 URL 進入 `history` 堆疊。您可以使用 `replace` 屬性來防止新增項目，如下列範例所示：

<AppOnly>

```tsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/about" replace>
      About us
    </Link>
  )
}
```

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/about" replace>
      About us
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/about" replace>
      About us
    </Link>
  )
}
```

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/about" replace>
      About us
    </Link>
  )
}
```

</PagesOnly>

### 停用頁面頂端的滾動

<AppOnly>

Next.js 中的 `<Link>` 預設滾動行為 **是保持滾動位置**，類似瀏覽器處理後退和前進導航的方式。當您導航到新的 [頁面](/docs/app/api-reference/file-conventions/page) 時，只要頁面在視口中，滾動位置將保持不變。

不過，如果頁面不在視口中，Next.js 將滾動到第一個頁面元素的頂部。如果您想停用此行為，您可以將 `scroll={false}` 傳遞給 `<Link>` 元件，或將 `scroll: false` 傳遞給 `router.push()` 或 `router.replace()`。

```jsx filename="app/page.js" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/#hashid" scroll={false}>
      Disables scrolling to the top
    </Link>
  )
}
```

```tsx filename="app/page.tsx" switcher
import Link from 'next/link'

export default function Page() {
  return (
    <Link href="/#hashid" scroll={false}>
      Disables scrolling to the top
    </Link>
  )
}
```

使用 `router.push()` 或 `router.replace()`：

```jsx
// useRouter
import { useRouter } from 'next/navigation'

const router = useRouter()

router.push('/dashboard', { scroll: false })
```

</AppOnly>

<PagesOnly>

`Link` 的預設行為是滾動到頁面頂部。當定義哈希時，它將滾動到特定的 id，就像一般的 `<a>` 標籤。要防止滾動到頂部/哈希，可以在 `Link` 中新增 `scroll={false}`：

```jsx filename="pages/index.js" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/#hashid" scroll={false}>
      Disables scrolling to the top
    </Link>
  )
}
```

```tsx filename="pages/index.tsx" switcher
import Link from 'next/link'

export default function Home() {
  return (
    <Link href="/#hashid" scroll={false}>
      Disables scrolling to the top
    </Link>
  )
}
```

</PagesOnly>

### 在中間件中預取連結

通常會使用 [中間件 (Middleware)](/docs/app/api-reference/file-conventions/middleware) 進行驗證或其他涉及將使用者重新導向至不同頁面的目的。為了使 `<Link />` 元件能夠透過中間件正確預取重寫後的連結，您需要告訴 Next.js 要顯示的 URL 和要預取的 URL。這是為了避免為了得知預取路由而進行不必要的中繼件取用。

例如，如果您想服務具有驗證和訪客檢視的 `/dashboard` 路由，您可以將以下程式碼新增到您的 Middleware 中以將使用者重新導向到正確的頁面：

```ts filename="middleware.ts" switcher
import { NextResponse } from 'next/server'

export function middleware(request: Request) {
  const nextUrl = request.nextUrl
  if (nextUrl.pathname === '/dashboard') {
    if (request.cookies.authToken) {
      return NextResponse.rewrite(new URL('/auth/dashboard', request.url))
    } else {
      return NextResponse.rewrite(new URL('/public/dashboard', request.url))
    }
  }
}
```

```js filename="middleware.js" switcher
import { NextResponse } from 'next/server'

export function middleware(request) {
  const nextUrl = request.nextUrl
  if (nextUrl.pathname === '/dashboard') {
    if (request.cookies.authToken) {
      return NextResponse.rewrite(new URL('/auth/dashboard', request.url))
    } else {
      return NextResponse.rewrite(new URL('/public/dashboard', request.url))
    }
  }
}
```

在此情況下，您希望使用以下程式碼在您位於 `<Link />` 元件中：

<AppOnly>

```tsx filename="app/page.tsx" switcher
'use client'

import Link from 'next/link'
import useIsAuthed from './hooks/useIsAuthed' // Your auth hook

export default function Page() {
  const isAuthed = useIsAuthed()
  const path = isAuthed ? '/auth/dashboard' : '/public/dashboard'
  return (
    <Link as="/dashboard" href={path}>
      Dashboard
    </Link>
  )
}
```

```js filename="app/page.js" switcher
'use client'

import Link from 'next/link'
import useIsAuthed from './hooks/useIsAuthed' // Your auth hook

export default function Page() {
  const isAuthed = useIsAuthed()
  const path = isAuthed ? '/auth/dashboard' : '/public/dashboard'
  return (
    <Link as="/dashboard" href={path}>
      Dashboard
    </Link>
  )
}
```

</AppOnly>

<PagesOnly>

```tsx filename="pages/index.tsx" switcher
'use client'

import Link from 'next/link'
import useIsAuthed from './hooks/useIsAuthed' // Your auth hook

export default function Home() {
  const isAuthed = useIsAuthed()
  const path = isAuthed ? '/auth/dashboard' : '/public/dashboard'
  return (
    <Link as="/dashboard" href={path}>
      Dashboard
    </Link>
  )
}
```

```js filename="pages/index.js" switcher
'use client'

import Link from 'next/link'
import useIsAuthed from './hooks/useIsAuthed' // Your auth hook

export default function Home() {
  const isAuthed = useIsAuthed()
  const path = isAuthed ? '/auth/dashboard' : '/public/dashboard'
  return (
    <Link as="/dashboard" href={path}>
      Dashboard
    </Link>
  )
}
```

> **小知識**：如果您正在使用 [動態路由 (Dynamic Routes)](/docs/pages/building-your-application/routing/dynamic-routes#convention)，您將需要調整您的 `as` 和 `href` 屬性。例如，如果您有一個動態路由（如 `/dashboard/authed/[user]`）您希望透過中間件以不同方式呈現，您會寫：`<Link href={{ pathname: '/dashboard/authed/[user]', query: { user: username } }} as="/dashboard/[user]">Profile</Link>`。

</PagesOnly>

<AppOnly>

### 封鎖導航

您可以使用 `onNavigate` 屬性在滿足某些條件（例如表單有未儲存變更）時封鎖導航。當您需要在應用程式的多個元件中封鎖導航（例如防止從任何連結進行導航，而表單正在編輯中），React Context 提供了一種清晰的方式來共享此封鎖狀態。首先，建立一個用來追蹤導航封鎖狀態的 Context：

```tsx filename="app/contexts/navigation-blocker.tsx" switcher
'use client'

import { createContext, useState, useContext } from 'react'

interface NavigationBlockerContextType {
  isBlocked: boolean
  setIsBlocked: (isBlocked: boolean) => void
}

export const NavigationBlockerContext =
  createContext<NavigationBlockerContextType>({
    isBlocked: false,
    setIsBlocked: () => {},
  })

export function NavigationBlockerProvider({
  children,
}: {
  children: React.ReactNode
}) {
  const [isBlocked, setIsBlocked] = useState(false)

  return (
    <NavigationBlockerContext.Provider value={{ isBlocked, setIsBlocked }}>
      {children}
    </NavigationBlockerContext.Provider>
  )
}

export function useNavigationBlocker() {
  return useContext(NavigationBlockerContext)
}
```

```jsx filename="app/contexts/navigation-blocker.js" switcher
'use client'

import { createContext, useState, useContext } from 'react'

export const NavigationBlockerContext = createContext({
  isBlocked: false,
  setIsBlocked: () => {},
})

export function NavigationBlockerProvider({ children }) {
  const [isBlocked, setIsBlocked] = useState(false)

  return (
    <NavigationBlockerContext.Provider value={{ isBlocked, setIsBlocked }}>
      {children}
    </NavigationBlockerContext.Provider>
  )
}

export function useNavigationBlocker() {
  return useContext(NavigationBlockerContext)
}
```

建立一個使用 Context 的表單元件：

```tsx filename="app/components/form.tsx" switcher
'use client'

import { useNavigationBlocker } from '../contexts/navigation-blocker'

export default function Form() {
  const { setIsBlocked } = useNavigationBlocker()

  return (
    <form
      onSubmit={(e) => {
        e.preventDefault()
        setIsBlocked(false)
      }}
      onChange={() => setIsBlocked(true)}
    >
      <input type="text" name="name" />
      <button type="submit">Save</button>
    </form>
  )
}
```

```jsx filename="app/components/form.js" switcher
'use client'

import { useNavigationBlocker } from '../contexts/navigation-blocker'

export default function Form() {
  const { setIsBlocked } = useNavigationBlocker()

  return (
    <form
      onSubmit={(e) => {
        e.preventDefault()
        setIsBlocked(false)
      }}
      onChange={() => setIsBlocked(true)}
    >
      <input type="text" name="name" />
      <button type="submit">Save</button>
    </form>
  )
}
```

建立一個封鎖導航的自訂 Link 元件：

```tsx filename="app/components/custom-link.tsx" switcher
'use client'

import Link from 'next/link'
import { useNavigationBlocker } from '../contexts/navigation-blocker'

interface CustomLinkProps extends React.ComponentProps<typeof Link> {
  children: React.ReactNode
}

export function CustomLink({ children, ...props }: CustomLinkProps) {
  const { isBlocked } = useNavigationBlocker()

  return (
    <Link
      onNavigate={(e) => {
        if (
          isBlocked &&
          !window.confirm('You have unsaved changes. Leave anyway?')
        ) {
          e.preventDefault()
        }
      }}
      {...props}
    >
      {children}
    </Link>
  )
}
```

```jsx filename="app/components/custom-link.js" switcher
'use client'

import Link from 'next/link'
import { useNavigationBlocker } from '../contexts/navigation-blocker'

export function CustomLink({ children, ...props }) {
  const { isBlocked } = useNavigationBlocker()

  return (
    <Link
      onNavigate={(e) => {
        if (
          isBlocked &&
          !window.confirm('You have unsaved changes. Leave anyway?')
        ) {
          e.preventDefault()
        }
      }}
      {...props}
    >
      {children}
    </Link>
  )
}
```

建立一個導航元件：

```tsx filename="app/components/nav.tsx" switcher
'use client'

import { CustomLink as Link } from './custom-link'

export default function Nav() {
  return (
    <nav>
      <Link href="/">Home</Link>
      <Link href="/about">About</Link>
    </nav>
  )
}
```

```jsx filename="app/components/nav.js" switcher
'use client'

import { CustomLink as Link } from './custom-link'

export default function Nav() {
  return (
    <nav>
      <Link href="/">Home</Link>
      <Link href="/about">About</Link>
    </nav>
  )
}
```

最後，在根佈局中用 `NavigationBlockerProvider` 包圍您的應用程式，並使用您的表單元件：

```tsx filename="app/layout.tsx" switcher
import { NavigationBlockerProvider } from './contexts/navigation-blocker'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <NavigationBlockerProvider>{children}</NavigationBlockerProvider>
      </body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
import { NavigationBlockerProvider } from './contexts/navigation-blocker'

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <NavigationBlockerProvider>{children}</NavigationBlockerProvider>
      </body>
    </html>
  )
}
```

接著，在您的頁面中使用 `Nav` 和 `Form` 元件：

```tsx filename="app/page.tsx" switcher
import Nav from './components/nav'
import Form from './components/form'

export default function Page() {
  return (
    <div>
      <Nav />
      <main>
        <h1>Welcome to the Dashboard</h1>
        <Form />
      </main>
    </div>
  )
}
```

```jsx filename="app/page.js" switcher
import Nav from './components/nav'
import Form from './components/form'

export default function Page() {
  return (
    <div>
      <Nav />
      <main>
        <h1>Welcome to the Dashboard</h1>
        <Form />
      </main>
    </div>
  )
}
```

當使用者嘗試使用 `CustomLink` 使用表單有未儲存變更時導航離開，他們會在離開前收到確認提示。

</AppOnly>

## 版本歷史

| 版本      | 變更                                                                                                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `v15.4.0` | 將 `auto` 新增為預設 `prefetch` 行為的別名。                                                                                                                            |
| `v15.3.0` | 新增 `onNavigate` API                                                                                                                                                     |
| `v13.0.0` | 不再需要子 `<a>` 標籤。提供 [代碼變更工具 (codemod)](/docs/app/guides/upgrading/codemods#remove-a-tags-from-link-components) 來自動更新您的程式碼庫。          |
| `v10.0.0` | 指向動態路由的 `href` 屬性會自動解析，且不再需要 `as` 屬性。                                                                              |
| `v8.0.0`  | 改善預取效能。                                                                                                                                                          |
| `v1.0.0`  | 引入 `next/link`。                                                                                                                                                      |
