---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-21T19:20:36.623Z
title: trailingSlash
description: 配置 Next.js 页面解析时是否包含尾部斜杠。
source: app/api-reference/next-config-js/trailingSlash
sidebar:
  order: 30203
---




默认情况下，Next.js 会将带有尾部斜杠的 URL 重定向到不带尾部斜杠的对应地址。例如 `/about/` 会被重定向到 `/about`。您可以配置此行为使其反向操作，即将不带尾部斜杠的 URL 重定向到带尾部斜杠的对应地址。

打开 `next.config.js` 文件并添加 `trailingSlash` 配置：

```js filename="next.config.js"
module.exports = {
  trailingSlash: true,
}
```

设置此选项后，类似 `/about` 的 URL 将被重定向到 `/about/`。

当与 [`output: "export"`](/docs/14/app/building-your-application/deploying/static-exports) 配置一起使用时，`/about` 页面将输出为 `/about/index.html`（而非默认的 `/about.html`）。

## 版本历史

| 版本号   | 变更内容                |
| -------- | ---------------------- |
| `v9.5.0` | 新增 `trailingSlash` 配置项。 |