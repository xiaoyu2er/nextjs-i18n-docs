---
title: draftMode
description: API Reference for the draftMode function.
sidebar:
  order: 20204
---


The `draftMode` function allows you to detect [Draft Mode](/docs/14/app/building-your-application/configuring/draft-mode) inside a [Server Component](/docs/14/app/building-your-application/rendering/server-components).

```jsx filename="app/page.js"
import { draftMode } from 'next/headers'

export default function Page() {
  const { isEnabled } = draftMode()
  return (
    <main>
      <h1>My Blog Post</h1>
      <p>Draft Mode is currently {isEnabled ? 'Enabled' : 'Disabled'}</p>
    </main>
  )
}
```

## Version History

| Version   | Changes                 |
| --------- | ----------------------- |
| `v13.4.0` | `draftMode` introduced. |
