---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:19:39.270Z
title: distDir
description: Legt ein benutzerdefiniertes Build-Verzeichnis fest, das anstelle des Standardverzeichnisses `.next` verwendet wird.
source: app/api-reference/next-config-js/distDir
sidebar:
  order: 30203
---




Sie können einen Namen für ein benutzerdefiniertes Build-Verzeichnis festlegen, das anstelle von `.next` verwendet wird.

Öffnen Sie `next.config.js` und fügen Sie die `distDir`-Konfiguration hinzu:

```js filename="next.config.js"
module.exports = {
  distDir: 'build',
}
```

Wenn Sie nun `next build` ausführen, verwendet Next.js `build` anstelle des Standardordners `.next`.

> `distDir` **sollte nicht** Ihr Projektverzeichnis verlassen. Beispielsweise ist `../build` ein **ungültiges** Verzeichnis.