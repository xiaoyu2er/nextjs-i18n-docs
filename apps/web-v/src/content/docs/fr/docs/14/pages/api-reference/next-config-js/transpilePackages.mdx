---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T16:52:56.767Z
title: transpilePackages
description: Transpile et bundle automatiquement les dépendances issues de packages locaux (comme dans les monorepos) ou de dépendances externes (`node_modules`).
source: app/api-reference/next-config-js/transpilePackages
sidebar:
  order: 30203
---




Next.js peut transpiler et regrouper automatiquement les dépendances issues de packages locaux (comme les monorepos) ou de dépendances externes (`node_modules`). Cela remplace le package `next-transpile-modules`.

```js filename="next.config.js"
/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@acme/ui', 'lodash-es'],
}

module.exports = nextConfig
```

## Historique des versions

| Version   | Changements                     |
| --------- | ------------------------------- |
| `v13.0.0` | Ajout de `transpilePackages`.   |