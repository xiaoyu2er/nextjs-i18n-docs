{
  "name": "nextjs-docs-router",
  "private": true,
  "scripts": {
    "dev": "bun run dev-all.ts",
    "dev:proxy": "bun run dev-proxy.ts",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
