{
  "name": "@next-i18n/router",
  "private": true,
  "scripts": {
    "dev": "bun run dev-all.ts",
    "dev:proxy": "bun run dev-proxy.ts",
    "generate-wrangler": "bun run generate-wrangler.ts",
    "deploy": "bun run generate-wrangler && bunx wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "wrangler": "^4.0.0"
  }
}
