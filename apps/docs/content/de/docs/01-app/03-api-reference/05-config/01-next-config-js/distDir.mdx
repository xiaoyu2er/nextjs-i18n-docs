---
source-updated-at: 2025-08-09T22:04:45.323Z
translation-updated-at: 2025-08-09T22:04:45.323Z
title: distDir
description: Legen Sie ein benutzerdefiniertes Build-Verzeichnis fest, das anstelle des Standardverzeichnisses `.next` verwendet werden soll.
---

{/* Der Inhalt dieses Dokuments wird sowohl vom App- als auch vom Pages-Router gemeinsam genutzt. Sie können die `<PagesOnly>Content</PagesOnly>`-Komponente verwenden, um Inhalte hinzuzufügen, die spezifisch für den Pages Router sind. Gemeinsam genutzte Inhalte sollten nicht in eine Komponente eingeschlossen werden. */}

Sie können einen Namen für ein benutzerdefiniertes Build-Verzeichnis angeben, das anstelle von `.next` verwendet werden soll.

Öffnen Sie `next.config.js` und fügen Sie die `distDir`-Konfiguration hinzu:

```js filename="next.config.js"
module.exports = {
  distDir: 'build',
}
```

Wenn Sie nun `next build` ausführen, verwendet Next.js `build` anstelle des Standardordners `.next`.

> `distDir` **sollte nicht** Ihr Projektverzeichnis verlassen. Beispielsweise ist `../build` ein **ungültiges** Verzeichnis.