---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:56:50.607Z
title: onDemandEntries
description: Konfigurieren Sie, wie Next.js in der Entwicklung erstellte Seiten verwaltet und im Speicher behält.
---

{/* Der Inhalt dieses Dokuments wird sowohl für den App- als auch für den Pages-Router verwendet. Sie können die Komponente `<PagesOnly>Inhalt</PagesOnly>` verwenden, um Inhalte hinzuzufügen, die spezifisch für den Pages-Router sind. Gemeinsame Inhalte sollten nicht in eine Komponente eingeschlossen werden. */}

Next.js bietet einige Optionen, mit denen Sie steuern können, wie der Server in der Entwicklung erstellte Seiten verwaltet oder im Speicher behält.

Um die Standardeinstellungen zu ändern, öffnen Sie `next.config.js` und fügen Sie die `onDemandEntries`-Konfiguration hinzu:

```js filename="next.config.js"
module.exports = {
  onDemandEntries: {
    // Zeitraum (in ms), in dem der Server Seiten im Puffer behält
    maxInactiveAge: 25 * 1000,
    // Anzahl der Seiten, die gleichzeitig ohne Verwerfung behalten werden sollen
    pagesBufferLength: 2,
  },
}
```