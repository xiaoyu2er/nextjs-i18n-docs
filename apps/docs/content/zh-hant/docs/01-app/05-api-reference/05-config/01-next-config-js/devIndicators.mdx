---
source-updated-at: 2025-05-19T22:31:51.000Z
translation-updated-at: 2025-05-25T21:57:00.116Z
title: devIndicators
description: 開發期間顯示當前路由相關資訊的畫面指示器設定選項。
---

`devIndicators` 允許您設定開發期間顯示當前路由相關資訊的畫面指示器。

```ts filename="Types"
  devIndicators: false | {
    position?: 'bottom-right'
    | 'bottom-left'
    | 'top-right'
    | 'top-left', // 預設為 'bottom-left',
  },
```

將 `devIndicators` 設為 `false` 會隱藏指示器，但 Next.js 仍會持續顯示遇到的建置或執行階段錯誤。

## 疑難排解

### 指示器未將路由標記為靜態

如果您預期某個路由應為靜態，但指示器將其標記為動態，可能是該路由選擇退出靜態渲染。

您可以透過執行 `next build --debug` 建置應用程式，並檢查終端機輸出，來確認路由是[靜態](/docs/app/getting-started/partial-prerendering#static-rendering)或[動態](/docs/app/getting-started/partial-prerendering#dynamic-rendering)。靜態（或預渲染）路由會顯示 `○` 符號，而動態路由則顯示 `ƒ` 符號。例如：

```bash filename="Build Output"
Route (app)                              Size     First Load JS
┌ ○ /_not-found                          0 B               0 kB
└ ƒ /products/[id]                       0 B               0 kB

○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand
```

<AppOnly>

路由可能選擇退出靜態渲染的原因有二：

- 使用了依賴執行階段資訊的[動態 API](/docs/app/getting-started/partial-prerendering#dynamic-rendering)。
- 進行了[未快取的資料請求](/docs/app/building-your-application/data-fetching/fetching)，例如呼叫 ORM 或資料庫驅動程式。

檢查您的路由是否滿足上述任一條件。若無法靜態渲染路由，可考慮使用 [`loading.js`](/docs/app/api-reference/file-conventions/loading) 或 [`<Suspense />`](https://react.dev/reference/react/Suspense) 來實現[串流](/docs/app/building-your-application/routing/loading-ui-and-streaming#what-is-streaming)。

</AppOnly>

<PagesOnly>

當頁面匯出 [`getServerSideProps`](/docs/pages/building-your-application/data-fetching/get-server-side-props) 或 [`getInitialProps`](/docs/pages/api-reference/functions/get-initial-props) 時，該頁面會被標記為動態。

</PagesOnly>

## 版本歷史

| 版本       | 變更內容                                                                                                                                             |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `v15.2.0`  | 改進畫面指示器，新增 `position` 選項。`appIsrStatus`、`buildActivity` 和 `buildActivityPosition` 選項已被棄用。                                     |
| `v15.0.0`  | 新增靜態畫面指示器，並提供 `appIsrStatus` 選項。                                                                                                    |