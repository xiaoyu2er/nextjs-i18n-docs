---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-25T21:12:50.523Z
title: distDir
description: 設定自訂建置目錄以取代預設的 .next 目錄。
---

{/* 此文件內容由 app 和 pages 路由器共用。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於 Pages 路由器的內容。任何共用內容不應被包裹在元件中。 */}

您可以指定自訂建置目錄的名稱來取代 `.next`。

開啟 `next.config.js` 並新增 `distDir` 設定：

```js filename="next.config.js"
module.exports = {
  distDir: 'build',
}
```

現在當您執行 `next build` 時，Next.js 將會使用 `build` 資料夾而非預設的 `.next` 資料夾。

> `distDir` **不應**指向專案目錄外的位置。例如 `../build` 就是一個**無效**的目錄。