---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-25T21:45:10.271Z
title: src 目錄
description: 將頁面儲存在 `src` 目錄下，作為根目錄 `pages` 的替代方案。
related:
  links:
    - app/building-your-application/routing/colocation
---

{/* 此文件內容在 app 和 pages 路由間共享。您可以使用 `<PagesOnly>內容</PagesOnly>` 元件來新增專屬於 Pages Router 的內容。任何共享內容不應被包裹在元件中。 */}

除了在專案根目錄中建立特殊的 Next.js `app` 或 `pages` 目錄外，Next.js 也支援將應用程式程式碼放在 `src` 目錄下的常見模式。

這種做法能將應用程式程式碼與大多位於專案根目錄的專案設定檔分開，這是某些個人和團隊偏好的方式。

要使用 `src` 目錄，請將 `app` Router 資料夾或 `pages` Router 資料夾分別移動到 `src/app` 或 `src/pages`。

<Image
  alt="一個包含 `src` 目錄的範例資料夾結構"
  srcLight="/docs/light/project-organization-src-directory.png"
  srcDark="/docs/dark/project-organization-src-directory.png"
  width="1600"
  height="687"
/>

> **須知事項**
>
> - `/public` 目錄應保留在專案的根目錄中。
> - 設定檔如 `package.json`、`next.config.js` 和 `tsconfig.json` 應保留在專案的根目錄中。
> - `.env.*` 檔案應保留在專案的根目錄中。
> - 如果根目錄中存在 `app` 或 `pages`，則 `src/app` 或 `src/pages` 將被忽略。
> - 如果您使用 `src`，可能還需要移動其他應用程式資料夾，例如 `/components` 或 `/lib`。
> - 如果您使用 Tailwind CSS，則需要在 `tailwind.config.js` 檔案的 [content 區段](https://tailwindcss.com/docs/content-configuration) 中新增 `/src` 前綴。