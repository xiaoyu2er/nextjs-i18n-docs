---
source-updated-at: 2025-05-29T18:05:49.000Z
translation-updated-at: 2025-05-29T20:09:07.042Z
title: 建立 API 路由
headline: '頁面路由 (Pages Router): 建立 API 路由'
image: 'https://nextjs.org/api/learn-og?title=Creating%20API%20Routes&amp;chapter=39'
---

[API 路由 (API Routes)](/docs/api-routes/introduction) 讓您可以在 Next.js 應用程式中建立 API 端點。您只需在 `pages/api` 目錄中建立一個符合以下格式的**函式 (function)** 即可：

```
// req = HTTP 傳入訊息, res = HTTP 伺服器回應
export default function handler(req, res) {
  // ...
}
```

> 關於上述請求處理器的更多資訊，請參閱 [API 路由文件](/docs/api-routes/introduction)。

這些路由可以部署為無伺服器函式 (Serverless Functions，也稱為 Lambda)。

### [建立簡單的 API 端點](#creating-a-simple-api-endpoint)

讓我們實際嘗試。在 `pages/api` 目錄中建立一個名為 `hello.js` 的檔案，並輸入以下程式碼：

```
export default function handler(req, res) {
  res.status(200).json({ text: 'Hello' });
}
```

嘗試在 [http://localhost:3000/api/hello](http://localhost:3000/api/hello) 存取它。您應該會看到 `{"text":"Hello"}`。請注意：

*   `req` 是 [http.IncomingMessage](https://nodejs.org/api/http.html#http_class_http_incomingmessage) 的實例，並包含一些預建的[中介軟體 (middlewares)](/docs/api-routes/api-middlewares)。
*   `res` 是 [http.ServerResponse](https://nodejs.org/api/http.html#http_class_http_serverresponse) 的實例，並包含一些[輔助函式 (helper functions)](/docs/api-routes/response-helpers)。

就是這麼簡單！在結束本課程之前，讓我們在下一頁討論一些使用 [API 路由 (API Routes)](/docs/api-routes/introduction) 的技巧。