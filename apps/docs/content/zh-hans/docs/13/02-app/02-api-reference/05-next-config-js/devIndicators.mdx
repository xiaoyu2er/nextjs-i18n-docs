---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-05-21T20:56:16.336Z
title: devIndicators
description: 经过优化的页面会包含一个指示器，用于告知该页面是否正在进行静态优化。您可以在此选择禁用该功能。
---

{/* 本文档内容在应用路由和页面路由间共享。您可以使用 `<PagesOnly>内容</PagesOnly>` 组件添加特定于页面路由的内容。所有共享内容不应包裹在任何组件中。*/}

<AppOnly>

当您编辑代码且 Next.js 正在编译应用时，页面右下角会出现一个编译指示器。

> **须知**：该指示器仅在开发模式下显示，生产环境构建和运行应用时不会出现。

某些情况下该指示器可能会在页面上显示错位，例如与聊天启动器冲突时。要更改其位置，请打开 `next.config.js` 并在 `devIndicators` 对象中设置 `buildActivityPosition` 为 `bottom-right`（默认值）、`bottom-left`、`top-right` 或 `top-left`：

```js filename="next.config.js"
module.exports = {
  devIndicators: {
    buildActivityPosition: 'bottom-right',
  },
}
```

某些情况下该指示器可能对您无用。要移除它，请打开 `next.config.js` 并在 `devIndicators` 对象中禁用 `buildActivity` 配置：

```js filename="next.config.js"
module.exports = {
  devIndicators: {
    buildActivity: false,
  },
}
```

</AppOnly>

<PagesOnly>

> **须知**：该指示器已在 Next.js 10.0.1 版本中移除。我们建议升级至最新版 Next.js。

当页面符合[自动静态优化](/docs/pages/building-your-application/rendering/automatic-static-optimization)条件时，我们会显示一个指示器进行提示。

这非常有用，因为自动静态优化能带来显著优势，在开发阶段立即获知页面是否符合条件很有帮助。

某些情况下该指示器可能无用，例如开发 electron 应用时。要移除它，请打开 `next.config.js` 并在 `devIndicators` 中禁用 `autoPrerender` 配置：

```js filename="next.config.js"
module.exports = {
  devIndicators: {
    autoPrerender: false,
  },
}
```

</PagesOnly>