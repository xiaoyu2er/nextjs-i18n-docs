---
source-updated-at: 2025-08-09T22:43:53.000Z
translation-updated-at: 2025-08-09T23:01:03.923Z
title: 使用组件构建用户界面
headline: React 基础 - 使用组件构建用户界面
description: 学习如何使用 React 组件构建可组合的用户界面。
image: >-
  https://nextjs.org/api/learn-og?title=Building%20UI%20with%20Components&amp;chapter=5
---

[React 核心概念](#react-core-concepts)
-------------------------------------------

开始构建 React 应用需要熟悉三个核心概念：

*   组件 (Component)
*   属性 (Props)
*   状态 (State)

在后续章节中，我们将探讨这些概念并提供学习资源。熟悉这些概念后，我们将介绍如何安装 Next.js 并使用服务端组件 (Server Components) 和客户端组件 (Client Components) 等新特性。

[组件](#components)
-------------------------

用户界面可以拆分为称为**组件**的较小构建块。

组件允许您构建自包含、可复用的代码片段。若将组件视为**乐高积木**，您可以将这些独立积木组合成更大结构。当需要更新部分 UI 时，只需更新特定组件或积木。

<Image
  alt="媒体组件示例：由图片、文本和按钮三个子组件构成"
  srcLight="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/light/learn-components.png"
  srcDark="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/dark/learn-components.png"
  width="1600"
  height="798"
/>

这种模块化设计让代码在增长过程中更易维护，因为您可以添加、更新或删除组件而不影响应用其他部分。

React 组件的优势在于它们本质上是 JavaScript。让我们从 JavaScript 角度了解如何编写 React 组件：

### [创建组件](#creating-components)

在 React 中，组件即**函数**。在 `script` 标签内创建名为 `header` 的函数：

```html {4,5} filename="index.html"
<script type="text/jsx">
  const app = document.getElementById('app');
 
  function header() {}
 
  const root = ReactDOM.createRoot(app);
  root.render(<h1>Develop. Preview. Ship.</h1>);
</script>
```

组件是**返回 UI 元素**的函数。在函数的 return 语句中编写 JSX：

```html {5} filename="index.html"
<script type="text/jsx">
  const app = document.getElementById('app');
 
  function header() {
    return <h1>Develop. Preview. Ship.</h1>;
  }
 
  const root = ReactDOM.createRoot(app);
  root.render(<h1>Develop. Preview. Ship.</h1>);
</script>
```

要将此组件渲染到 DOM，将其作为首个参数传入 `root.render()` 方法：

```html {9} filename="index.html"
<script type="text/jsx">
  const app = document.getElementById('app');
 
  function header() {
    return <h1>Develop. Preview. Ship.</h1>;
  }
 
  const root = ReactDOM.createRoot(app);
  root.render(header);
</script>
```

但请注意：在浏览器中运行上述代码会报错。需进行两项修正：

首先，React 组件名称应大写以区别于普通 HTML 和 JavaScript：

```html {1,7} filename="index.html"
function Header() {
  return <h1>Develop. Preview. Ship.</h1>;
}
 
const root = ReactDOM.createRoot(app);
// 大写 React 组件名称
root.render(Header);
```

其次，使用 React 组件的方式与常规 HTML 标签相同，需使用尖括号 `<>`：

```html {6} filename="index.html"
function Header() {
  return <h1>Develop. Preview. Ship.</h1>;
}
 
const root = ReactDOM.createRoot(app);
root.render(<Header />);
```

再次在浏览器中运行代码即可看到效果。

### [嵌套组件](#nesting-components)

应用通常包含多个组件。您可以像嵌套 HTML 元素那样**嵌套** React 组件。

创建名为 `HomePage` 的新组件：

```html {5,6,7} filename="index.html"
function Header() {
  return <h1>Develop. Preview. Ship.</h1>;
}
 
function HomePage() {
  return <div></div>;
}
 
const root = ReactDOM.createRoot(app);
root.render(<Header />);
```

将 `<Header>` 组件嵌套在新组件 `<HomePage>` 中：

```html {7,8,9,10} filename="index.html"
function Header() {
  return <h1>Develop. Preview. Ship.</h1>;
}
 
function HomePage() {
  return (
    <div>
      {/* 嵌套 Header 组件 */}
      <Header />
    </div>
  );
}
 
const root = ReactDOM.createRoot(app);
root.render(<Header />);
```

### [组件树](#component-trees)

通过这种方式持续嵌套 React 组件，可形成组件树。

<Image
  alt="展示组件嵌套关系的组件树"
  srcLight="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/light/learn-component-tree.png"
  srcDark="https://h8DxKfmAPhn8O0p3.public.blob.vercel-storage.com/learn/dark/learn-component-tree.png"
  width="1600"
  height="617"
/>

例如，顶层 `HomePage` 组件可包含 `Header`、`Article` 和 `Footer` 组件。这些组件又可拥有各自的子组件（如 `Header` 可包含 `Logo`、`Title` 和 `Navigation` 组件）。

这种模块化格式支持在应用不同位置复用组件。

在项目中，由于 `<HomePage>` 现为顶层组件，可将其传入 `root.render()` 方法：

```html {14} filename="index.html"
function Header() {
  return <h1>Develop. Preview. Ship.</h1>;
}
 
function HomePage() {
  return (
    <div>
      <Header />
    </div>
  );
}
 
const root = ReactDOM.createRoot(app);
root.render(<HomePage />);
```

> **扩展资源：**
> 
> *   [创建第一个组件](https://react.dev/learn/your-first-component)
> *   [组件的导入与导出](https://react.dev/learn/importing-and-exporting-components)