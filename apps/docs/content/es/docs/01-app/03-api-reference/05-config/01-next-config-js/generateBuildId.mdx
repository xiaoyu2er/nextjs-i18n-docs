---
source-updated-at: 2025-08-09T22:04:45.323Z
translation-updated-at: 2025-08-09T22:04:45.323Z
title: generateBuildId
description: Configura el ID de compilación, que se utiliza para identificar la compilación actual en la que se está sirviendo su aplicación.
---

{/* El contenido de este documento es compartido entre el enrutador de la aplicación y el de páginas. Puede usar el componente `<PagesOnly>Contenido</PagesOnly>` para agregar contenido específico del Enrutador de Páginas. Cualquier contenido compartido no debe estar envuelto en un componente. */}

Next.js genera un ID durante `next build` para identificar qué versión de su aplicación se está sirviendo. La misma compilación debe usarse e iniciarse en múltiples contenedores.

Si está recompilando para cada etapa de su entorno, necesitará generar un ID de compilación consistente para usar entre contenedores. Utilice el comando `generateBuildId` en `next.config.js`:

```jsx filename="next.config.js"
module.exports = {
  generateBuildId: async () => {
    // Esto puede ser cualquier cosa, usando el último hash de git
    return process.env.GIT_HASH
  },
}
```