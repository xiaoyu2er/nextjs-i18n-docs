---
source-updated-at: 2025-05-21T18:33:43.000Z
translation-updated-at: 2025-06-02T20:13:05.516Z
title: htmlLimitedBots
description: ブロッキングメタデータを受信すべきユーザーエージェントのリストを指定します。
---

`htmlLimitedBots` 設定を使用すると、[ストリーミングメタデータ](/docs/app/api-reference/functions/generate-metadata#streaming-metadata)の代わりにブロッキングメタデータを受信すべきユーザーエージェントのリストを指定できます。

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const config: NextConfig = {
  htmlLimitedBots: /MySpecialBot|MyAnotherSpecialBot|SimpleCrawler/,
}

export default config
```

```js filename="next.config.js" switcher
module.exports = {
  htmlLimitedBots: /MySpecialBot|MyAnotherSpecialBot|SimpleCrawler/,
}
```

## デフォルトリスト

Next.jsには[HTML限定ボットのデフォルトリスト](https://github.com/vercel/next.js/blob/canary/packages/next/src/shared/lib/router/utils/html-bots.ts)が含まれています。

`htmlLimitedBots`設定を指定すると、Next.jsのデフォルトリストが上書きされ、この動作を適用するユーザーエージェントを完全に制御できます。ただし、これは高度な動作であり、ほとんどの場合はデフォルトで十分です。

## バージョン履歴

| バージョン | 変更内容                              |
| ------- | ------------------------------------ |
| 15.2.0  | `htmlLimitedBots` オプションが導入されました。 |