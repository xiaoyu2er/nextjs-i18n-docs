---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:14:56.733Z
title: eslint
description: يُبلغ Next.js عن أخطاء وتحذيرات ESLint أثناء عمليات البناء بشكل افتراضي. تعلم هنا كيفية إلغاء تفعيل هذا السلوك.
---

{/* المحتوى الخاص بهذا المستند مشترك بين موجه التطبيق وموجه الصفحات. يمكنك استخدام مكون `<PagesOnly>المحتوى</PagesOnly>` لإضافة محتوى خاص بموجه الصفحات. أي محتوى مشترك لا يجب أن يكون مغلفًا داخل مكون. */}

عند اكتشاف ESLint في مشروعك، يفشل Next.js في **بناء الإنتاج** (`next build`) عند وجود أخطاء.

إذا كنت ترغب في أن يقوم Next.js بإنتاج كود الإنتاج حتى عندما يحتوي تطبيقك على أخطاء ESLint، يمكنك تعطيل خطوة الفحص المضمنة تمامًا. لا يُنصح بهذا إلا إذا كنت قد قمت بالفعل بتكوين ESLint ليعمل في جزء منفصل من سير العمل الخاص بك (على سبيل المثال، في CI أو خطاف ما قبل الالتزام).

افتح ملف `next.config.js` وقم بتمكين خيار `ignoreDuringBuilds` في تكوين `eslint`:

```js filename="next.config.js"
module.exports = {
  eslint: {
    // تحذير: هذا يسمح لبناءات الإنتاج بالإكمال بنجاح حتى إذا
    // كان مشروعك يحتوي على أخطاء ESLint.
    ignoreDuringBuilds: true,
  },
}
```