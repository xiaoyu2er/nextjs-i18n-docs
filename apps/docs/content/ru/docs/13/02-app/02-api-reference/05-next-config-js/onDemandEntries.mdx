---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-06T17:21:32.167Z
title: onDemandEntries
description: Настройка управления памятью страниц в режиме разработки в Next.js. Узнайте, как сервер освобождает или сохраняет в памяти созданные страницы.
---

{/* Содержание этого документа является общим для маршрутизаторов app и pages. Вы можете использовать компонент `<PagesOnly>Содержание</PagesOnly>` для добавления контента, специфичного для маршрутизатора Pages. Любой общий контент не должен быть обёрнут в компонент. */}

Next.js предоставляет несколько опций, которые позволяют контролировать, как сервер будет освобождать или сохранять в памяти собранные страницы в режиме разработки.

Для изменения настроек по умолчанию откройте файл `next.config.js` и добавьте конфигурацию `onDemandEntries`:

```js filename="next.config.js"
module.exports = {
  onDemandEntries: {
    // период (в мс), в течение которого сервер будет хранить страницы в буфере
    maxInactiveAge: 25 * 1000,
    // количество страниц, которые должны одновременно сохраняться в памяти без освобождения
    pagesBufferLength: 2,
  },
}
```