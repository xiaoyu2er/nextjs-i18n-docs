---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:13:35.457Z
title: Логирование
description: Настройка логирования запросов данных в консоль при работе Next.js в режиме разработки.
---

{/* Содержание этого документа является общим для маршрутизаторов app и pages. Вы можете использовать компонент `<PagesOnly>Content</PagesOnly>` для добавления контента, специфичного для маршрутизатора Pages. Любой общий контент не должен быть обернут в компонент. */}

## Опции

### Запросы данных (Fetching)

Вы можете настроить уровень логирования и отображение полного URL в консоли при работе Next.js в режиме разработки.

В настоящее время `logging` применяется только к запросам данных с использованием API `fetch`. Оно еще не применяется к другим логам внутри Next.js.

```js filename="next.config.js"
module.exports = {
  logging: {
    fetches: {
      fullUrl: true,
    },
  },
}
```

Любые `fetch`-запросы, восстановленные из [кэша HMR Server Components](/docs/app/api-reference/config/next-config-js/serverComponentsHmrCache), по умолчанию не логируются. Однако это можно включить, установив `logging.fetches.hmrRefreshes` в `true`.

```js filename="next.config.js"
module.exports = {
  logging: {
    fetches: {
      hmrRefreshes: true,
    },
  },
}
```

### Входящие запросы (Incoming Requests)

По умолчанию все входящие запросы будут логироваться в консоли во время разработки. Вы можете использовать опцию `incomingRequests` для указания, какие запросы следует игнорировать.
Поскольку это логируется только в режиме разработки, данная опция не влияет на продакшен-сборки.

```js filename="next.config.js"
module.exports = {
  logging: {
    incomingRequests: {
      ignore: [/\api\/v1\/health/],
    },
  },
}
```

Или вы можете отключить логирование входящих запросов, установив `incomingRequests` в `false`.

```js filename="next.config.js"
module.exports = {
  logging: {
    incomingRequests: false,
  },
}
```

### Отключение логирования

Кроме того, вы можете отключить логирование в режиме разработки, установив `logging` в `false`.

```js filename="next.config.js"
module.exports = {
  logging: false,
}
```