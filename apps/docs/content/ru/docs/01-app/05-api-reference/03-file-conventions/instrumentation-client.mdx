---
source-updated-at: 2025-05-16T04:52:11.000Z
translation-updated-at: 2025-06-02T20:08:36.715Z
title: instrumentation-client.js
description: Узнайте, как добавить клиентскую инструментацию для отслеживания и мониторинга производительности фронтенда вашего приложения Next.js.
---

Файл `instrumentation-client.js|ts` позволяет добавить код мониторинга и аналитики, который выполняется до запуска фронтенд-кода вашего приложения. Это полезно для настройки отслеживания производительности, мониторинга ошибок или любых других инструментов клиентской наблюдаемости.

Для использования поместите файл в **корень** вашего приложения или в папку `src`.

## Использование

В отличие от [серверной инструментации](/docs/app/guides/instrumentation), вам не нужно экспортировать какие-либо специальные функции. Вы можете написать код мониторинга непосредственно в файле:

```ts filename="instrumentation-client.ts" switcher
// Настройка мониторинга производительности
performance.mark('app-init')

// Инициализация аналитики
console.log('Analytics initialized')

// Настройка отслеживания ошибок
window.addEventListener('error', (event) => {
  // Отправка в сервис отслеживания ошибок
  reportError(event.error)
})
```

```js filename="instrumentation-client.js" switcher
// Настройка мониторинга производительности
performance.mark('app-init')

// Инициализация аналитики
console.log('Analytics initialized')

// Настройка отслеживания ошибок
window.addEventListener('error', (event) => {
  // Отправка в сервис отслеживания ошибок
  reportError(event.error)
})
```

## История версий

| Версия  | Изменения                           |
| ------- | ----------------------------------- |
| `v15.3` | Добавлена поддержка `instrumentation-client` |